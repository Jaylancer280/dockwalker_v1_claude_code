# DockWalker Web

Next.js 16 app for the DockWalker daywork marketplace. This app contains:

- App Router pages in `src/app`
- API route handlers in `src/app/api`
- Supabase SSR/auth helpers in `src/lib/supabase`
- Vitest tests in `__tests__`

## Prerequisites

- Node.js 20+
- npm
- Supabase CLI available through `npx supabase`

## Environment

Copy the example and fill in your values:

```bash
cp .env.example .env.local
```

Required variables (see `.env.example`):

```env
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
```

`SUPABASE_SERVICE_ROLE_KEY` is required because server routes append ledger events through RPCs.

## Local Development

> **This project does NOT use local Docker / local Supabase.** All development runs against the **linked remote Supabase project**. After any new migration in `supabase/migrations/`, apply it with `npx supabase db push`. Do not attempt `npx supabase db reset` — it requires Docker, which is not part of the supported workflow. Same applies to the integration test path below.

From the repo root:

```bash
npm install
npx supabase db push      # apply any pending migrations to the linked remote
npm run dev
```

App URL: `http://localhost:3000`

The `supabase/config.toml` ports listed below describe the local-stack defaults if you ever opt into local Docker, but the canonical workflow is remote-only:

- API: `http://127.0.0.1:54321`
- DB: `127.0.0.1:54322`
- Studio: `http://127.0.0.1:54323`

## Database Notes

Current schema version: see [BUILD_STATE.md](../../BUILD_STATE.md)

The current app expects the following RPCs to exist in `public`:

- `append_event` — accepts an optional `p_idempotency_key text` (since migration 00124 / Fix 257). When supplied, a retry of the same event from the same person resolves to the original event id without re-running the projection. The `appendEvent` helper in `@dockwalker/db` exposes this as an optional `idempotencyKey?: string` parameter. Recommended pattern: derive the key deterministically from request context (e.g. `DAYWORK.ACCEPTED:${crewId}:${dayworkId}`). Required for any state-mutating event whose projection (a) increments a counter, (b) inserts without ON CONFLICT, or (c) lacks a WHERE-clause guard that would make a duplicate run a no-op. Currently wired in `/api/daywork/[id]/applicants/[crewId]/accept`, `/api/daywork/invitations/[id]/respond` (accept branch), and `/api/engagements/[id]/rate` (all four rating events).
- `check_no_overlap`
- `check_no_overlap_excluding`
- `onboard_person`
- `clear_availability_dates`
- `get_vessel_public`
- `get_vessels_public_batch` (batch version — used by discover, applications, invitations routes). Both functions filter `source = 'pending' OR hidden_at IS NOT NULL` rows out of the result for non-owner non-engaged callers (since 00122 — Vessels V2 Wave F). Owner exception (`v.owner_person_id = auth.uid()`) and active-engagement exception (daywork or permanent, crew or employer side) preserve operational visibility; existing NDA / owner / engagement reveal logic for `imo_number` + `name` masking is unchanged. Mirrors the front-door `/api/vessels/lookup` filter (Fix 253).
- `increment_advisor_usage`
- `search_locations` / `get_locations_by_ids` / `top_locations` (pg_trgm + unaccent fuzzy location search — used by `/api/locations/search`, `/api/locations/by-ids`, `/api/locations/top` and consumed by `LocationPicker` + `CitiesPicker`). `search_locations` and `top_locations` filter cities + ports to `source IN ('curated', 'osm')` AND `hidden_at IS NULL` (since 00118) so manually-requested pending rows stay invisible to other users' searches; `get_locations_by_ids` is intentionally unfiltered so the submitting user's own FK still resolves. The `LocationPicker` falls back to `/api/locations/search-external` (live OSM Nominatim lookup, 1-req/sec rate-limited per function instance) and `/api/locations/canonicalize` (idempotent service-role upsert into `regions` + `cities` with `source='osm'`) when canonical search returns zero hits in `port-optional` mode, and to `/api/locations/request` + `LocationRequestModal` (with `/api/locations/regions` populating the country picker) as a final manual fallback that inserts `source='pending'` rows visible only to the submitter until an admin acts via the `/admin/locations/pending` curation queue (`/api/admin/locations/pending` GET + `/api/admin/locations/pending/{cities,ports}/[id]` POST with action `approve` | `merge` | `hide`).
- `custom_access_token_hook` (Auth hook — injects person_id/current_hat/identity_type into JWT; enable in Dashboard → Auth → Hooks)

If you see errors like:

```text
Could not find the function public.onboard_person(...) in the schema cache
```

your database is behind the code. Apply migrations to the linked remote:

```bash
npx supabase db push
```

## Available Scripts

From the repo root:

```bash
npm run dev
npm run build
npm run lint
npm run type-check
npm test
```

From `apps/web` directly:

```bash
npm run dev
npm run build
npm run lint
npm run type-check
npm run test
npm run test:integration   # requires local Supabase running
```

## Testing

Primary checks:

```bash
npm run lint
npm run type-check
npm test
```

The API tests mock `@/lib/supabase/server` and call route handlers directly. Component tests mock `@/lib/supabase/client` and render pages to verify UI behaviour (e.g. canonical dropdown population). Both are fast unit tests that do not require a running database.

Integration tests (`npm run test:integration`) verify that events emitted with the same payload shape as the API routes are correctly projected into materialised tables. They were authored against a local Supabase instance (`npx supabase start` + `db reset`) but the project no longer uses local Docker — running them requires either spinning Docker up ad-hoc or pointing the test harness at a linked-remote test schema. Excluded from the default `npm test` run.

## Infrastructure

- **Health check:** `GET /api/health` — public, no auth, no DB. Returns `{ status: 'ok', timestamp }`. Use for uptime monitoring and load balancer checks.
- **Body size limit:** Server actions body size is set to 1MB in `next.config.ts` (`experimental.serverActions.bodySizeLimit`). Route handlers use the Next.js default (also 1MB).
- **Avatar upload:** MIME type validation + magic byte validation (JPEG/PNG/WebP). Max 2MB. Spoofed Content-Type headers are rejected.
- **Stripe subscriptions:** Checkout, portal, status, and webhook routes at `/api/billing/*` and `/api/webhooks/stripe`. Requires `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_CREW_PRO`, `STRIPE_PRICE_EMPLOYER_PRO`. Graceful no-op when not configured. Stripe setup: create products/prices in the Stripe dashboard, copy price IDs to env, set webhook endpoint to `/api/webhooks/stripe` with events `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`. Subscription check helper at `src/lib/require-subscription.ts` for gating premium features.
- **Rate limiting:** Upstash Redis via Next.js middleware (`proxy.ts`). Global: 100 requests/60s per IP for all API routes. Write routes (POST/PATCH/DELETE): 30 requests/60s per IP. Exempt: `/api/health`, `/api/webhooks/*`. Graceful no-op when `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` are not configured (local dev). Set up: create an Upstash Redis store at [upstash.com](https://upstash.com) or via the Vercel integration, copy REST URL + token to env vars.
- **Docky (AI advisor):** Maritime career advisor at `/docky`. Uses Claude Haiku 4.5 for generation + OpenAI `text-embedding-3-small` for embeddings. RAG pipeline searches `mca_document_chunks` via pgvector (deployed empty — MCA corpus ingestion deferred). Conversations persisted in `advisor_conversations` + `advisor_messages`. API routes at `/api/advisor/conversations` (CRUD) and `/api/advisor/conversations/[id]/messages` (send + get). Crew-only (403 for employer hat). Requires `ANTHROPIC_API_KEY` and optionally `OPENAI_API_KEY` + `DOCKY_MODEL`. Graceful degradation: tab visible when Anthropic key configured; RAG skipped when OpenAI key missing (answers from LLM general knowledge only). **Personalisation (Phase 2):** crew context builder (`src/lib/advisor/crew-context.ts`) reads crew profile, certs, experiences, and vessel exposure to inject into LLM prompts. Cert gap analysis (`src/lib/advisor/cert-analysis.ts`) cross-references crew's certs against MCA chunk content to identify progression gaps. System prompt extended with personalisation block when crew context is available. Dynamic suggestion chips on Docky pages adapt to crew's role and location. Staged thinking indicator shows "reading your profile" → "thinking" phases. **Monetisation gating (Phase 3):** free tier limited to 3 questions/month tracked in `advisor_usage` table. Crew Pro subscribers get unlimited access. Usage check runs before saving user message (no orphaned messages on limit hit). `GET /api/advisor/usage` returns current usage or Pro status. Paywall card in chat when limit reached, usage pill in Docky list header. Billing page at `/billing` with Free/Crew Pro plan cards, Stripe checkout integration, subscription management portal.

## Architectural Notes

- Core domain state is event-sourced through `append_event`.
- `daywork_templates`, `user_preferences`, and `device_tokens` are intentional exceptions: they are owner-scoped CRUD utility data and not part of the ledger.
- Push notifications are delivered via `src/lib/push-delivery.ts` (FCM HTTP v1 + APNs HTTP/2) and triggered by `src/lib/push-triggers.ts` which maps domain events to notifications. `notifyOnEvent()` is called fire-and-forget after `appendEvent`/`appendEvents` in 12 API routes. Each in-app notification is written with a `role_context` (crew/employer) so badge counts are hat-scoped. Requires env vars: `FCM_PROJECT_ID`, `FCM_SERVICE_ACCOUNT_KEY`, `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_BUNDLE_ID`, `APNS_KEY_PATH`. Gracefully no-ops when not configured. Foreground notifications shown via `PushToast` component (auto-dismiss 5s banner); tap navigation uses `resolveDeepLinkUrl()` for deep linking (chat, discover, review screens). Badge management deferred pending plugin install.
- NDA vessel exposure for crew goes through `get_vessel_public`, not direct vessel table reads. **Vessel-read policy (S-1 audit, 2026-04-28):** any non-owner non-admin read of `vessels` MUST go through `get_vessel_public(uuid)` or `get_vessels_public_batch(uuid[])`. Direct `from('vessels').select(...)` is reserved for: owner-scoped reads filtered by `owner_person_id = auth.uid()` (vessel CRUD, account export, FK uniqueness checks); admin-scoped reads under `requireAdmin` (canonical, pending queue); id-only or nda_flag-only reads where no name/IMO/LOA can leak (size-band filter on discover, the chat-header NDA mask check itself). Public job page (`lib/jobs/get-public-job.ts`) and IMO lookup (`/api/vessels/lookup`) both go through the RPCs as of S-1. When in doubt: use the RPC. Re-audit periodically by running `grep -rn "from('vessels')" apps/web/src` and re-classifying.
- Onboarding is atomic through `onboard_person`, which appends `PERSON.CREATED` and `PROFILE.CREATED` together. The onboarding UI is a 6-step flow: welcome → identity → experience fork (green/experienced) → profile → vessel experience (experienced only) → hat selection. Green crew manually declare experience bracket and vessel size exposure. Experienced crew add vessel experience entries (IMO lookup, vessel details, role, dates, salary, rotation) — experience bracket and vessel size exposure are auto-derived.
- All domain routes (read and write) use `requireDomainUser()` from `src/lib/auth/require-domain-user.ts`. This guard returns 401 for unauthenticated users and 409 for users missing `persons` or `profiles` rows. Only `auth/me` and `onboarding` are excluded.
- Discovery (`GET /api/daywork/discover`) is open: crew see all active postings. Filters are explicit: `roleId`, `portId`, `startDate`, `endDate`, `certificationId`, `experienceBracketId`, `sizeBandId`. Optional `cursor` param (ISO timestamp) for cursor-based pagination — returns `has_more` boolean and `next_cursor` for stable infinite scroll. Ordering is recency only. Own postings and previously-interacted postings are excluded at the DB level (not post-fetch) so the 50-row batch size applies to genuinely discoverable jobs. `certificationId` uses `.contains()` on the `required_certification_ids` array (special value `none` filters for jobs with no cert requirements via `.eq('required_certification_ids', '{}')`); `experienceBracketId` uses `.eq()` — both are DB-level. `sizeBandId` is a post-fetch filter on vessel data (from `get_vessel_public` RPC) since size band is resolved server-side. Applying is gated end-to-end: the UI blocks apply actions when availability is not set or explicitly not-available, and the apply API (`POST /api/daywork/:id/apply`) enforces the same check server-side, returning 403 for crew without active availability windows.
- My Applications (`GET /api/daywork/applications`): crew-only route returning pending applications (`applied`, `viewed`, `shortlisted`) with hydrated daywork details and NDA-safe vessel names. Displayed in the "Applied" tab on the Discover page. Withdraw uses the existing `POST /api/daywork/:id/withdraw` which appends `APPLICATION.WITHDRAWN` to the ledger.
- Applicants (`GET /api/daywork/:id/applicants`): employer-only route returning enriched applicants for a posting. Optional query params: `certificationId` (uuid — filters applicants whose profile `certification_ids` includes this cert), `minAvailableDays` (number — filters applicants with `available_days >= N`). Filters are post-enrichment since availability overlap is computed in JS. Enrichment adds `available_days`, `availability_city`, `availability_not_available`, `past_daywork_count`, and `shore_experience_categories` to each applicant. When the posting requires certs, also adds `cert_match: { ok, matched, total, missing_count } | null` (bundle-aware via `meetsRequirements`) and `cert_extras_ids: string[]` (certs the candidate holds that aren't covering any required slot — surfaces over-qualification on the review card).
- Available Crew (`GET /api/daywork/:id/available-crew`): employer-only route returning crew with matching availability in the same city who haven't applied or been invited. Requires active daywork owned by the caller. Default: filters by daywork's `role_id`; pass `allRoles=true` to skip role filter. Excludes: employer themselves, crew who already applied, crew already invited. Returns `{ crew, invitation_count, invitation_limit: 2 }`. Sorted by `available_days` DESC, limited to 50.
- Invite (`POST /api/daywork/:id/invite`): employer-only route to send a daywork invitation. Body: `{ crewPersonId }`. Validates crew exists, has no existing application or invitation, and pending invitation count < 2. Appends `DAYWORK.INVITED` event. Returns `{ invitation: { id, status } }` with 201.
- Invitations (`GET /api/daywork/invitations`): crew-only route returning pending invitations with hydrated daywork details (role, vessel, location, dates, rate, job number), employer display name, and NDA-safe vessel data. Batch-fetches dayworks, employer profiles, and vessels via `get_vessel_public` RPC.
- Invitation Respond (`POST /api/daywork/invitations/:id/respond`): crew-only route. Body: `{ action: 'accept' | 'decline' }`. Accept validates availability, checks positions not full (409 if full), appends `DAYWORK.INVITATION_ACCEPTED` which creates engagement directly via projection (no application). Returns `{ success, engagementId }`. Decline appends `DAYWORK.INVITATION_DECLINED`. Guards: crew hat, invitation ownership, pending status, active daywork, overlap check.
- My Jobs (`GET /api/daywork/mine`) supports `status`, `roleId`, and `portId` query params. Returns `applicant_count` per posting (pending applications in `applied` / `viewed` / `shortlisted`) for the Review applicants badge, and `is_overdue` for active postings past end_date.
- Daywork postings require `dayRate` (positive number) and `currency` (EUR/USD/GBP/AED, defaults to EUR). Day rate is NOT optional.
- Employer cancellation (`POST /api/engagements/:id/cancel-employer`) requires structured reason: `reason_category`, optional `reason_text`, `relist_requested`, optional `relist_reason_category`/`relist_reason_text`. Relist reasons are employer-private.
- Crew cancellation (`POST /api/engagements/:id/cancel-crew`) requires structured reason: `reason_category` (`personal_reasons`, `found_other_work`, `unsafe_conditions`, `other`), optional `reason_text`. Daywork stays `in_progress` — employer decides relist vs cancel.
- Respond to crew cancel (`POST /api/engagements/:id/respond-crew-cancel`): employer chooses `{ action: 'relist' }` or `{ action: 'cancel' }` after crew-initiated cancellation.
- Postponement proposal (`POST /api/engagements/:id/propose-postponement`): once-only per engagement. Blocked when work has started (`work_started_status = 'confirmed'`). Returns `{ outcome: 'conflict' }` without acting when crew has scheduling conflict; employer must confirm with `confirm_conflict: true` to cancel and relist.
- Postponement response (`POST /api/engagements/:id/respond-postponement`): crew accepts or rejects proposed dates. Rejection cancels engagement but does NOT auto-relist.
- Relist with dates (`POST /api/engagements/:id/relist-with-dates`): employer manually relists after crew rejection using proposed dates stored on the engagement.
- Work started (`POST /api/engagements/:id/work-started`): mutual confirmation that work has begun. Either party initiates (`action: 'initiate'`), the other confirms (`action: 'confirm'`). Once confirmed, postponement is blocked.
- Rating (`POST /api/engagements/:id/rate`) now accepts both `completed` and `cancelled` engagements. Cancelled context has a lighter rating form.
- Checklist set (`POST /api/engagements/:id/checklist`): employer sets or updates pre-arrival checklist. Body: `{ items: [{ id, label, value }] }`. Emits `CHECKLIST.SET` + system message. Resets crew acknowledgements on update.
- Checklist toggle (`POST /api/engagements/:id/checklist/toggle`): crew toggles acknowledgement of a single item. Body: `{ item_id, checked }`. Emits `CHECKLIST.ITEM_TOGGLED`.
- Availability (`POST /api/availability`): crew sets availability. Normal mode requires `startDate`, `endDate`, and `cityId` (town-level FK to cities). Optional `portId` (FK to ports, validated to belong to cityId). Enforces 14-day rolling window (today through today+13). Hard 7-day expiry. "Not available" mode: `{ notAvailable: true, cityId }` — skips date validation, writes single marker row. `DELETE /api/availability` clears specific dates (`{ dates: [...] }`) or all availability (`{ clearAll: true }`). `GET /api/availability` returns windows with `city_id`, `port_id`, resolved city and port names, and `status` field (`available` | `not_available` | `null`).
- Settings page (`/settings`): accessible via gear icon on profile header. Account management (change password/email, sign out), appearance (distance & size units with km/mi+ft/NM options, currency display — localStorage), privacy & data (GDPR data export as JSON, account deletion via `PERSON.DEACTIVATED`), and about info. Sign out moved from profile to settings.
- Unit conversion system (`src/lib/units.ts`): centralized currency symbols (`currencySymbol()`), distance conversion (km/mi/NM via `convertDistance()`), length conversion (m/ft via `metersToFeet()`/`feetToMeters()`), vessel size band label converter (`convertSizeBandLabel()`). `src/hooks/use-preferences.ts` reads localStorage preferences (defaults: EUR, km). Forms default currency to user preference. Vessel sizes display in user's preferred unit (metres or feet for imperial users).
- Vessel creation (`POST /api/vessels`): accepts `loaMeters` (exact LOA in metres) instead of `sizeBandId`. Size band is auto-derived from LOA by querying `vessel_size_bands` ordered by `min_meters`. Returns 400 if LOA doesn't match any band. The create form accepts LOA in the user's preferred unit (m or ft) and shows the auto-derived band as a hint. Open to all authenticated users (crew can create vessels for experience entries). IMO uniqueness is per-registrant (`UNIQUE(imo_number, owner_person_id)`).
- Vessel update (`PATCH /api/vessels/[id]`): updates vessel fields (name, vesselType, loaMeters, ndaFlag). LOA changes auto-derive size band. NDA immutability guard: returns 400 if trying to set `ndaFlag: false` when current value is `true`. Owner-only. Note: `vesselOperation` removed from vessels (Stage 69 — soft data lives on crew_experiences).
- Vessel lookup (`GET /api/vessels/lookup?imo=`): returns existing vessel data for IMO suggestion during experience entry. Any authenticated user can look up. Returns `{ found: boolean, vessel? }`.
- Crew experiences (`GET/POST /api/experiences`, `PATCH/DELETE /api/experiences/[id]`): CRUD for crew experience history. Salary fields stored server-side but NEVER returned in API responses. POST validates required fields (vesselId, roleId, startDate, vesselOperation), vessel operation (private/charter), contract type, end-date >= start-date, description length (250 chars). PATCH/DELETE verify ownership. GET response augments each row with `historical_vessel_name` — set when the vessel was renamed AFTER this experience ended, surfacing the name the user knew it as during the experience window via `vessel_names` interval lookup (Fix 255 — Vessels V2 Wave F display); null when the current name was already in effect during `[start_date, end_date]`. Shared helper `apps/web/src/lib/vessels/historical-names.ts` does the overlap math in one DB roundtrip. **References extension (Phase 3 references feature):** GET also returns `references_active_count` per experience (count of `references` rows with status pending or accepted) AND a top-level `subscription_plan` field (the caller's plan from `subscriptions`, defaults to `'free'`) — together they power the per-experience "Add reference (n/cap)" counter on profile experience cards. PATCH adds the P0-A snapshot-field edit-lock: returns 409 with `{ locked_fields, active_references }` when locked fields change while pending+accepted references exist on the experience (route check + projection-layer guard for defence in depth). `role`/`start_date` are unconditionally locked; `end_date` and `is_current` are locked EXCEPT for the one-time null→date / true→false transition that closes a currently-onboard experience (00129) — when that closing transition is detected, the projection auto-propagates the new end_date to references whose `snapshot_end_date` was captured null so referee-verified records show the closed period instead of "Jan 2024 — Present" forever. **NDA visibility (00130):** references on NDA vessels are allowed at write time (the referee was the captain/HOD on that vessel — they already know the IMO). Display-layer masking in `/api/messages/[engagementId]/context` replaces `snapshot_vessel_name` with `'NDA Vessel'` and nulls `snapshot_vessel_imo` when the caller is the employer side of a reference-contact chat, the vessel is currently NDA, and the caller has no separate active daywork/permanent engagement on that vessel with the crew (existing 00083 trust-boundary precedent). Referee always sees full snapshot; profile API already strips snapshot fields from references[]; party-only surfaces (`/api/references/mine`, `/ref/[token]`, `/api/account/export`, `/messages` consent_prompts) stay unmasked. DELETE captures the affected accepted-referee set BEFORE firing EXPERIENCE.REMOVED so it can fan out a Fix A in-app-only `reference_auto_revoked` notification per referee post-commit (direct insert into `notifications`, bypassing notifyOnEvent because that helper has no channel override).
- Onboarding (`POST /api/onboarding`): extended to accept `experiences` array for experienced crew. Each entry triggers vessel creation (if needed) + `EXPERIENCE.ADDED` event. Green crew fields (`shoreExperience`, `motivation`, `languages`, `availableToStart`) passed through to `PROFILE.CREATED` payload. `onboarding_version: 2` set for all new onboardings. **Re-onboarding path**: if the caller's `persons` row already exists with `current_hat IS NULL` (admin-scrubbed users per migration 00108), the route fires `PERSON.HAT_CHANGED` + `PROFILE.UPDATED` via `appendEvents` instead of the `onboard_person` RPC — avoids the PK conflict on the existing (nulled) profile row and keeps the event ledger coherent.
- Notification preferences (`GET /api/preferences`): returns current user's notification preference toggles (email_enabled, push_jobs, push_applications, push_messages, push_reminders). Upserts row if none exists. (`PATCH /api/preferences`): partial update of boolean preference fields.
- Push tokens (`POST /api/push-tokens`): upserts a device token for push notifications. Body: `{ token: string, platform: 'apns' | 'fcm' | 'web' }`. Returns 201. (`DELETE /api/push-tokens`): removes token. Body: `{ token: string }`. Returns 200. Both require authentication. Currently dormant — the web client does not register tokens (no native shell since Capacitor was retired); the table, dispatcher, and trigger handlers stay live so a future web-push or background-worker client can populate it without server changes. `lib/push-notifications.ts` now exposes only `resolveDeepLinkUrl`, the pure URL mapper used by trigger handlers and tests.
- Daywork extend (`POST /api/daywork/:id/extend`): extends an active posting's end_date. Body: `{ endDate: string, workingDays?: number, workingDayDates?: string[] }`. Validates ownership, active status, endDate >= today. Appends `DAYWORK.EXTENDED` event.
- Daywork post (`POST /api/daywork`): now accepts optional `workingDayDates` string array and optional `positionsAvailable` (default 1, range 1-20). When `workingDayDates` provided, `workingDays` is derived from array length. Validates dates within range and no duplicates.
- Permanent post (`POST /api/permanent`): employer/agent-only. Creates a permanent job posting via `PERMANENT.POSTED` event. Body: vesselId, roleId, locationPortId, startDate, salaryMin, salaryMax, salaryCurrency, salaryPeriod, liveAboard, plus optional requiredCertificationIds, experienceBracketId, shortlistCap (1-20, default 5), notes (max 500). FK validation on vessel (ownership), role, port, certs. Returns `{ id }` with 201.
- Permanent templates (`GET/POST /api/permanent/templates`, `GET/PATCH/DELETE /api/permanent/templates/[id]`): CRUD for permanent posting templates. Owner-scoped via RLS. Same fields as permanent postings plus `templateName`.
- Permanent discover (`GET /api/permanent/discover`): crew-only. Returns active/in_negotiation permanent postings with filters (roleId, portId, certificationId, experienceBracketId, salaryMin, liveAboard, sizeBandId) and cursor-based pagination (20 per page). Excludes already-applied and own postings. Hydrates vessel data, poster names, cert names. Response: `{ postings, has_more, next_cursor }`.
- Permanent apply (`POST /api/permanent/:id/apply`): crew-only. Cert hard-gate: blocks if crew is missing any required cert (returns 403 with `missing_certs` array, appends `PERMANENT.APPLICATION_BLOCKED` intelligence event). No availability gate. Body: `{ message?: string }`. Creates application via `PERMANENT.APPLIED` event. Returns `{ success: true }`.
- Profile PATCH now accepts `permanentAvailability` (`'immediate'`/`'after_notice'`/`'not_looking'`/null), `noticePeriodDays` (positive int or null), `currentlyEmployed` (boolean). Profile GET now returns these fields.
- Profile GET returns `placement_city_ids` (string[]) for agents — cities where they actively place crew. Profile PATCH accepts `placementCityIds` (string[]) for agents — replaces all placement cities atomically (delete + re-insert on `agent_placement_cities` table).
- Telegram notifications (`/api/notifications/telegram/*` + `/api/webhooks/telegram`): first-class rich external channel alongside WhatsApp, prioritised above it in the dispatcher. `POST /init` mints a one-time link token (192 random bits, 10-min TTL, 3 active-token cap per user) and returns `https://t.me/<bot>?start=<token>`. `GET /status` returns `{ connected }`. `DELETE /` removes the channel row + flips `telegram_enabled` off + invalidates pending tokens. `POST /api/webhooks/telegram` (Telegram → us, no auth guard — verified via `X-Telegram-Bot-Api-Secret-Token` against `TELEGRAM_WEBHOOK_SECRET`) consumes `/start <token>` messages to attach the Telegram chat_id (AES-256-GCM encrypted via the same `NOTIFICATION_ENCRYPTION_KEY` as WhatsApp) to the linking person, handles `/stop` to disconnect, replies with help text on any other input. Webhook always responds 200 to prevent Telegram retry storms. Send path: `src/lib/telegram.ts` `sendTelegramMessage(chatId, html)` calls `https://api.telegram.org/bot<TOKEN>/sendMessage` with `parse_mode: 'HTML'` and disables link previews. Graceful no-op when `TELEGRAM_BOT_TOKEN` is unset. Env vars: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME`, `TELEGRAM_WEBHOOK_SECRET`. Setup: create bot via @BotFather, set env vars in Vercel, `curl https://api.telegram.org/bot<TOKEN>/setWebhook -d url=https://www.dockwalker.io/api/webhooks/telegram -d secret_token=<SECRET>`.
- WhatsApp notifications (`/api/notifications/whatsapp/*`): `POST register` (E.164 phone, encrypts with AES-256-GCM, sends OTP via Twilio), `POST verify` (6-digit code, marks verified, enables whatsapp_enabled preference), `DELETE` (hard-deletes channel, disables preference), `GET status` (connected flag + masked phone). Env vars: `NOTIFICATION_ENCRYPTION_KEY` (32 bytes hex), `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_WHATSAPP_FROM`.
- Permanent applications (`GET /api/permanent/applications`): crew-only. Returns crew's permanent applications (applied, shortlisted, selected, not_selected, rejected, placement_confirmed) with hydrated posting details, vessel data, poster names, cert names. Each application has `type: 'permanent'` for unified Applied tab rendering.
- Permanent withdraw (`POST /api/permanent/:id/withdraw`): crew-only. Withdraws applied/shortlisted/selected applications via `PERMANENT.WITHDRAWN` event. Selected withdrawal cascades to engagement closure and posting revert (handled by apply_projection).
- Permanent review (`GET /api/permanent/:id/review`): employer-only, ownership-gated. Returns applicants with profile joins (availability, nationality, certs), shortlist_cap, shortlist_count, posting_status, selected_crew_id. Each applicant carries `cert_match: { ok, matched, total, missing_count } | null` (bundle-aware via `meetsRequirements`) and `cert_extras_ids: string[]` for the over-qualification pill on the review card.
- Permanent shortlist (`POST /api/permanent/:id/applicants/:crewId/shortlist`): employer-only. Shortlists an `applied` candidate. Cap enforced at API layer. Emits `PERMANENT.SHORTLISTED`.
- Permanent reject (`POST /api/permanent/:id/applicants/:crewId/reject`): employer-only. Rejects `applied` or `shortlisted` candidates. Emits `PERMANENT.REJECTED`.
- Permanent select (`POST /api/permanent/:id/applicants/:crewId/select`): employer-only. Selects a `shortlisted` candidate. Creates engagement, moves posting to `in_negotiation`. Returns `{ success, engagementId }`. Emits `PERMANENT.SELECTED`.
- Permanent confirm (`POST /api/permanent/:id/confirm`): employer-only. Confirms placement. Posting → `filled`. Remaining shortlisted/applied → `not_selected`. Emits `PERMANENT.PLACEMENT_CONFIRMED`.
- Permanent revert (`POST /api/permanent/:id/revert`): employer-only. Reverts selection. Engagement closed. Posting → `active`. Emits `PERMANENT.SELECTION_REVERTED`.
- Permanent cancel (`POST /api/permanent/:id/cancel`): employer-only. Cancels posting (`active` or `in_negotiation`). Projection handles engagement close cascade. Body: `{ reason?: string }`. Emits `PERMANENT.CANCELLED_BY_EMPLOYER`.
- Permanent engagement close (`POST /api/permanent/engagements/:id/close`): either party. Closes permanent engagement conversation. Body: `{ outcome: 'successful_placement' | 'not_successful' | 'withdrew' }`. Crew withdrew reverts posting to active. Emits `PERMANENT.ENGAGEMENT_CLOSED`.
- Permanent mine (`GET /api/permanent/mine`): employer-only. Returns all employer's permanent postings with application counts (applicant_count, shortlist_count, total_applications) and selected_crew_name for in_negotiation postings. Single batch query for counts, no N+1.
- Messages API (`GET /api/messages`): now includes `permanent_posting_id` and `permanent_postings(...)` LEFT JOIN on engagement queries. Response includes `type: 'daywork' | 'permanent'`. Permanent engagements have no overdue/rating computations.
- Messages context (`GET /api/messages/:id/context`): now includes `permanent_posting_id`, `outcome`, and `permanent_postings(...)` LEFT JOIN with full posting details (salary, vessel, role, port). Response includes `type` field.
- Permanent notifications: 8 event types generate push + in-app notifications via `notifyOnEvent`. Email templates for SHORTLISTED, SELECTED, PLACEMENT_CONFIRMED. `APPLICATION_BLOCKED` and `WITHDRAWN` are silent (no notification). Engagement-starts cron resolves role names for permanent engagements. GDPR export includes `permanent_posting_id`, `outcome`, `rejection_reason` on applications/engagements, plus `permanent_postings` table for employer data.
- Message read cursor (`POST /api/messages/:engagementId/read`): upserts read cursor for current user on engagement. Called on chat mount and visibility change.
- Notifications (`GET /api/notifications`): lists notifications for current user, supports `?unread_only=true` and `?grouped=true`. Default path returns `{ notifications, unread_count }` with raw rows. Grouped path calls the `grouped_notifications()` RPC (migration 00110) and returns `{ groups, unread_count }` where each group collapses notifications sharing `(type, deep_link)` — fields include `group_key`, `total_count`, `unread_count`, `latest_id`, and the most-recent notification's title/body/deep_link/created_at.
- Notifications read (`POST /api/notifications/read`): marks notifications as read. Body: `{ notificationIds: string[] }` or `{ all: true }`.
- Notifications read-group (`POST /api/notifications/read-group`): marks every unread notification in a `(type, deep_link)` group as read in one UPDATE. Body: `{ type: string, deep_link: string | null }`. `deep_link` field is required — omitting it returns 400 to prevent accidentally clearing every notification of a type across all resources.
- Notifications count (`GET /api/notifications/count`): hat-scoped unread counts for badge polling. Returns `{ notification_count, message_count, alt_notification_count, alt_message_count }`. Notifications are scoped by `role_context` column (crew/employer). Messages are scoped by the user's role in each engagement (crew_person_id vs employer_person_id). `message_count` is the number of **threads with unread messages** (not total unread messages). `NotificationBell` uses `notification_count`, `BottomNav` messages badge uses `message_count`, `HatSwitcher` shows `alt_notification_count + alt_message_count` as a combined badge for the other hat.
- Profile avatar (`POST /api/profile/avatar`): multipart form upload, validates JPEG/PNG/WebP and <=2MB, uploads to Supabase Storage `avatars/{userId}/avatar.{ext}`, appends `PROFILE.UPDATED` event. (`DELETE /api/profile/avatar`): removes file from storage, sets avatar_url to null.
- Daywork update positions (`POST /api/daywork/:id/update-positions`): employer-only. Body: `{ positionsAvailable: number }`. Validates >= 1, >= current filled count, <= 20, daywork must be active. Appends `DAYWORK.POSITIONS_UPDATED` event. Projection handles fill-triggered transition to `in_progress`.
- View profile (`GET /api/profile/[personId]`): context-gated read-only view of another user's profile. Requires engagement, application, invitation relationship, or target having an active/in-negotiation posting. Active poster checks use service client to bypass RLS. Crew profiles include experiences (no salary), certifications, vessel size exposure. Employer profiles include non-NDA vessels, role specializations, active posting count. Returns 403 if no context. Each crew experience and agent maritime-background entry includes a `historical_vessel_name` field (Vessels V2 Wave F — Fix 255), set only when a vessel rename post-dated the experience window so the consumer can render the period-correct name. **References extension (Phase 5):** crew experiences include a `references` array with each accepted reference's display data (referee name + role + comment + consented_at). Visibility is subscription-tier-aware: the EXPERIENCE OWNER's plan drives the cap (Free=1, Crew Pro=3). Self-view bypasses the cap so the owner always sees all accepted references; view-only callers see only the most recent N. Pure read-side filter — the references table is unchanged on tier transitions, so a Pro→Free downgrade hides surplus references without losing them.
- Account deactivation (`POST /api/account/deactivate`): appends `PERSON.DEACTIVATED` event. Sets `deactivated_at` on `persons` via projection. Bans auth user (`ban_duration: '876000h'`) to prevent re-login — ban failure returns 500 (hard error). Profile hidden immediately via RLS filter. Data scrub is a deferred admin process.
- Account data export (`GET /api/account/export`): GDPR data portability. Returns JSON with profile, events, messages, engagements, availability, vessels, preferences, and `whatsapp_phone` (decrypted, only if verified WhatsApp channel exists).
- Deep links: `public/.well-known/apple-app-site-association` (iOS) and `public/.well-known/assetlinks.json` (Android). Placeholder values (`TEAM_ID`, `PRODUCTION_DOMAIN`, `PLACEHOLDER_SHA256_FINGERPRINT`) must be replaced for production.
- Analytics: `@vercel/analytics` + `@vercel/speed-insights` in root layout. Auto-configured on Vercel, no env vars needed.
- Realtime messages: Chat page uses Supabase Realtime (postgres_changes INSERT on `messages`) for instant message delivery. Falls back to 5s polling if Realtime fails to connect. Context polling (engagement state) retained at 5s.
- Admin API: `is_admin` flag on persons, `requireAdmin` guard (DB-checks `is_admin` directly — does not trust header fast path). Proxy guard in middleware redirects non-admins from `/admin/*`. Routes: `GET /api/admin/users` (search+paginate), `GET /api/admin/users/:id` (detail), `DELETE /api/admin/users/:id` (scrub user — emits `PERSON.DATA_SCRUBBED` + `PERSON.DEACTIVATED`, bans auth row, preserves event history), `POST /api/admin/users/:id/block` (block user with cascade — cancels engagements/postings/applications/availability via `append_events_batch`), `POST /api/admin/users/:id/unblock` (unblock user — no state restoration), `GET /api/admin/engagements` (stuck engagement finder), `POST /api/admin/engagements/:id/complete` (force-complete with reason), `POST /api/admin/engagements/:id/cancel` (force-cancel active engagement — emits `ADMIN.ENGAGEMENT_CANCELLED`, routes daywork vs permanent cascade), `POST /api/admin/postings/:id/hide` (hide daywork or permanent posting — emits `ADMIN.POSTING_HIDDEN`), `GET/POST/PATCH /api/admin/canonical/:table` (canonical data CRUD; GET accepts optional `?page=&pageSize=&q=` for paginated + searchable responses on large tables like ports/cities — returns `{ rows, total, page, pageSize, totalPages }` when paginated, legacy `{ rows }` otherwise; `pageSize` capped at 500; sort column is `label` for `experience_brackets` + `vessel_size_bands`, `name` elsewhere). All admin actions append `ADMIN.*` audit events. Blocked users redirected to `/blocked` page. `POST /api/admin/users/:id/restore` (unblock + reactivate + unban). `GET /api/admin/users/:id/notes` (list notes with author display names), `POST /api/admin/users/:id/notes` (add note — author = current admin), `PATCH /api/admin/users/:id/notes/:noteId` (edit — authoring admin only). `PERSON.DATA_SCRUBBED` rewrites notes content about the subject to `'[content scrubbed]'`. `requireDomainUser` enforces `blocked_at` across all three auth paths (header, JWT, DB); support routes opt out via `{ allowBlocked: true }`. User-facing reporting: `POST /api/reports` (body `{ reported_person_id, engagement_id?, reason_category, reason_text }`, rejects self-report, caps at 5 open reports per reporter), `GET /api/reports` (list own). Admin moderation: `GET /api/admin/reports` (query params `status`, `reason`, `filed_against`, `filed_by`, `page`; safety_concern sorted first), `GET /api/admin/reports/:id` (detail with reporter/reported/admin names), `PATCH /api/admin/reports/:id` (validates status/resolution enums; stamps `admin_person_id` + `resolved_at` when terminal). Support channel: `GET/POST /api/support` (list/create threads), `GET/POST /api/support/:threadId` (messages). Admin support: `GET /api/admin/support` (inbox), `GET/POST /api/admin/support/:threadId` (messages + reply as DockWalker), `POST /api/admin/support/:threadId/close`, `POST /api/admin/support/initiate` (open thread for user). `GET /api/admin/vessels` (vessel list with search). Blocked users can access `/support` (exempted from middleware redirect).
- Shore-based experience: `GET/POST /api/shore-experiences`, `PATCH/DELETE /api/shore-experiences/:id`, `GET /api/shore-experience-categories`. Structured non-maritime work history with 30 industry categories. Informational only — not counted in experience brackets. Category pills shown on applicant cards and profile overlay. View-only profile API (`GET /api/profile/:personId`) includes `shore_experiences` in crew profile response.
- Cron jobs: Vercel Cron runs `GET /api/cron/availability-expiry` daily at 08:00 UTC. Notifies crew whose availability expires within 24h (in-app + push). Auth via `CRON_SECRET` bearer token. Duplicate prevention (won't re-notify within 24h). Vercel Cron also runs `GET /api/cron/reference-expiry` daily at 05:00 UTC — fires `REFERENCE.EXPIRED` for accepted references where `expires_at < now()` (24-month default) AND for pending invitations where `pending_expires_at < now()` (30-day default). Per-row event firing keeps the audit trail granular; idempotent via WHERE-clause status guards. **Data-scrub cron (S-2):** `GET /api/cron/data-scrub` runs daily at 04:00 UTC. Finds persons whose `deactivated_at` is older than 30 days AND who do not yet have a `PERSON.DATA_SCRUBBED` event in the ledger; appends the event for each via `appendEvent` with deterministic idempotency key `PERSON.DATA_SCRUBBED:auto:<personId>` (D-1 dedup if Vercel retries within 5 minutes). The 00108 projection wipes the row server-side. Per-person try/catch so one failure doesn't abort the run; failures returned in the response body. Closes the GDPR Article 17 manual-process gap (deactivation flow at `/api/account/deactivate` only fires `PERSON.DEACTIVATED` and bans auth — the trailing scrub used to be a manual admin action; this cron makes it automatic).
- Consent-based references: `POST /api/references` (crew creates invitation, returns share-link), `GET /api/references/by-token/:token` (public — referee preview before signing in, masks email), `POST /api/references/:id/accept` (referee consent + optional comment in atomic batch), `POST /api/references/:id/decline` (silent, no notification), `POST /api/references/:id/comment` (referee edits accepted comment), `POST /api/references/:id/revoke` (routes to REVOKED_BY_REQUESTER on pending+accepted or REVOKED_BY_REFEREE on accepted), `POST /api/references/:id/resend` (atomic revoke-old + fresh-request with B-3 salted idempotency key), `GET /api/references/mine` (three buckets: outbound, inbound_accepted, inbound_pending), `POST /api/references/:id/contact` (employer asks referee for chat — Free tier capped at 10 pending + 5 accepted/30d), `POST /api/reference-contacts/:id/{accept,decline}` (referee responds to contact request — accept inserts `active_engagements` row with `reference_contact_id` and pre-populates the chat with the employer's question if provided), `POST /api/engagements/:id/close-reference-contact` (either party closes a reference-contact chat — fires REFERENCE.CONTACT_THREAD_CLOSED, projection sets active_engagement status='closed' + outcome='reference_complete' per B-8). Existing route modifications: `PATCH /api/experiences/:id` adds the P0-A snapshot-field edit-lock (returns 409 with `{ locked_fields, active_references }` when locked fields change while pending+accepted references exist; role/start_date are unconditionally locked, end_date/is_current are locked except for the one-time closing transition on a currently-onboard experience per 00129); `DELETE /api/experiences/:id` adds the Fix A in-app fan-out (captures affected accepted-referee set BEFORE firing EXPERIENCE.REMOVED then direct-inserts notifications post-commit, bypassing notifyOnEvent because that helper has no channel override); `GET /api/account/export` extended (P0-C) to include `references.{outbound,inbound}` and `reference_contacts.{as_employer,as_referee}` arrays; `GET /api/messages` extended (Phase 4) to include a `consent_prompts[]` array surfacing pending reference invitations + pending reference-contact requests where the caller is the referee; `GET /api/messages/:id/context` extended (Phase 4) to hydrate `reference_context` (status, revoke_reason, snapshot fields, comment) when the engagement is a reference-contact chat — drives the `<ReferenceContactHeader>` chat-layout switch and the Fix A revoked banner. RPCs added: `find_person_id_by_email(p_email text) returns uuid` (service-role only, used by the B-6 conditional notification path on POST /api/references — fires an in-app notification when claimed_referee_email matches an existing DockWalker person; email courier is never used).
- Transactional email: Resend SDK — sends branded emails for DAYWORK.ACCEPTED (to crew), DAYWORK.APPLIED (to employer), MESSAGE.SENT (to recipient, rate-limited 1 per conversation per 5min). Set `RESEND_API_KEY` to enable. No-ops without it.
- Error tracking: `@sentry/nextjs` — optional, set `NEXT_PUBLIC_SENTRY_DSN` to enable. Server, client, and edge configs auto-initialise. Error boundaries capture to Sentry. No-ops without DSN.
- Google Sign-In: `GoogleAuthButton` component (`src/components/google-auth-button.tsx`) calls `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: '${origin}/auth/callback?next=...' } })`. Wired into `/auth/login`, `/auth/signup`, and `/auth/forgot-password` (with guidance for OAuth-only users who don't have a password). The `/auth/callback` route already handles the PKCE `code` exchange — no route changes were needed. Onboarding prefills `displayName` from `user.user_metadata.full_name` / `name` on first entry; other profile fields (role, certs, deck_name) are never prefilled from OAuth metadata. Settings → Account hides the change-password form for users whose only identity is `google` (detected via `hasPasswordIdentity()` in `src/lib/auth/has-password-identity.ts`) and links them to Google Account Security instead. Google is a third-party data processor listed in `/privacy`. Enabling requires: (1) Google Cloud Console OAuth 2.0 Client ID (Web app) with redirect URI `https://<project>.supabase.co/auth/v1/callback`; (2) Supabase Dashboard → Auth → Providers → Google → enable + paste credentials; (3) Supabase Dashboard → Auth → "Link identities" enabled so email+password users who later use Google (or vice versa) merge into one `auth.users` row.
- Auth flow: Login (`/auth/login`) submits to `POST /api/auth/login` which calls `signInWithPassword` server-side and attaches session cookies to the response (client-side cookie writes are unreliable with Next.js 16 middleware). On a banned/deactivated user, the route redirects back with a guidance message ("You can restore it within 30 days by resetting your password") and a defensive `persons.deactivated_at` check catches the zombie state where the auth ban was lifted but the persons row flag remained. Forgot password (`/auth/forgot-password`): email input → `supabase.auth.resetPasswordForEmail()` → success message. Auth callback (`/auth/callback`): handles both PKCE `code` exchange and `token_hash`+`type` fallback (for cross-browser/device email opens), attaches session cookies directly to the redirect response. Reset password (`/auth/reset-password`): reached via callback redirect → `onAuthStateChange` detects `PASSWORD_RECOVERY` event + `getSession()` fallback → new password form → `supabase.auth.updateUser()` → calls `POST /api/auth/reactivate` which appends `PERSON.REACTIVATED` (clears `persons.deactivated_at` via projection) and lifts the auth ban via service client if the user was deactivated. The reset-password success screen shows "Welcome back" copy when reactivation occurred. This is the reset-to-reactivate flow: any user can self-recover within the 30-day retention window before `PERSON.DATA_SCRUBBED`. All auth pages are public routes (no auth required). Production SMTP is configured in Supabase Dashboard, not in code — see `.env.example` for setup notes.
- Location selection uses the `LocationPicker` component (`src/components/location-picker.tsx`) — a searchable hierarchical Region → City → Port/Marina popover. Two modes: `port-required` (post-daywork, profile, onboarding, discover filters) requires a specific port selection; `port-optional` (availability overlay) allows city-only selection with optional port drill-down. The component self-fetches location data from Supabase.
- CV Builder (Stage 1, Phase 2): `POST /api/cv/generate` is a Stage-1 stub that returns 503 with a "DockWalker CV — Coming Soon" payload. Crew-hat gated at the route layer so the contract for Stage 2 (real PDF return) is locked in before the renderer ships. `POST /api/admin/cv/mint-handle/[personId]` is an admin-only operational route that mints an 8-char alphanumeric `cv_handle` for a target crew person if their handle is null and fires `CV.HANDLE_REGENERATED` with `old_handle=null` for ledger audit; returns 201 with the new handle, 409 with the existing handle if already minted, 404 if no profile row exists. Internal QA seeds handles via this route during Stage 1 (natural user flows don't mint handles until Stage 2's PDF generator runs). Shared mint helper at `src/lib/cv/mint-handle.ts` retries up to 5× on Postgres unique_violation (23505), throws `MintHandleError` on exhaustion. `qrcode` npm dep installed early to stabilise the lockfile (used by Phase 8 PDF QR rendering).
- CV Builder (full user-level lockdown, 2026-04-29): single source of truth `lib/cv/feature-flag.ts` exports `CV_BUILDER_ENABLED = false` + `CV_BUILDER_LOCKED_PAYLOAD`. While locked, every user-facing CV surface returns/renders Coming-Soon — `PATCH /api/cv/settings`, `GET /api/cv/[handle]`, `POST /api/permanent/[id]/invite` all 503; `POST /api/daywork` rejects only the QR-hire branch (regular POST works); `POST /api/permanent/[id]/apply` silently strips `fromInvitationId` (regular apply works). `/settings/cv` toggles fire a Coming-Soon toast and don't PATCH; `/cv/[handle]` renders a Coming-Soon screen unconditionally with no fetch; `/daywork/post?invite=…` early-returns to Coming-Soon. Admin mint-handle, schema, projection, types, and the invitation-expiry cron stay intact for Stage 2 unlock — flipping the flag to `true` un-gates the entire surface in one commit.
- CV Builder (Stage 1, Phase 6 — auth polish + invitation expiry): `?next=` param now flows end-to-end through sign-up → email confirm → middleware bounce → onboarding → final destination. New `safeRedirectPath` helper in `lib/auth/safe-redirect-path.ts` allowlists relative paths only (rejects `//`, `..`, `:`, > 256 chars). Onboarding page reads `?next=` and routes there on completion. Middleware preserves the user's intended target as `?next=<path>` when redirecting auth-but-not-onboarded users to `/onboarding` (skipped for `/`, `/dashboard`, and `/onboarding` itself). Sign-up polling threads `next` to `/onboarding?next=...` for non-referee users. The `/cv/[handle]` sign-up CTAs use the canonical `?next=` param (replaced the ad-hoc `?redirect=`). Hat-switch CTA on the crew-hat banner of `/cv/[handle]` lets dual-hat crew flip to their employer hat in one click via `POST /api/hat`. New `GET /api/cron/invitation-expiry` (Vercel cron, daily 03:00 UTC, CRON_SECRET-gated) flips `permanent_invitations` rows from `pending` to `expired` after 30 days via direct UPDATE (no event — nothing in the domain reacts to expiry; the apply-after-invite validator already drops non-pending invitations silently). Powered by the partial index from migration 00131; idempotent.
- CV Builder (Stage 1, Phase 5b — hire-from-QR UI): `/cv/[handle]` action-bar buttons wire to the existing `/daywork/post` page with `?invite=<personId>` (daywork) or `?invite=<personId>&type=permanent` (permanent). The post page reads `?type=` to skip the type selector. Both forms thread the crew person id through to the API on submit — daywork calls `POST /api/daywork` with `inviteCrewPersonId` (atomic POST+INVITE), permanent calls `POST /api/permanent` then `POST /api/permanent/[id]/invite` (PERMANENT.INVITED). A QR-hire banner renders at the top of each form when `?invite=` is present. New `/permanent/[id]/apply` page renders the apply-after-invite UX: posting summary, optional invitation banner ("Captain James invited you to apply for {role} on {vessel}.") + the captain's optional message, 250-char textarea (deliberately no pre-fill per spec v2.1). Applying passes `fromInvitationId` to `POST /api/permanent/[id]/apply` (validated server-side in Phase 5a). New `GET /api/permanent/[id]` returns posting details + optional invitation context. `resolveDeepLinkUrl` in `lib/push-notifications.ts` extended with `permanent-apply` screen mapping powering the PERMANENT.INVITED notification deep link. ✉ Invited badge on permanent review-queue applicant cards.
- CV Builder (Stage 1, Phase 5a — backend wiring): `POST /api/daywork` extended with optional `inviteCrewPersonId` body field — when present the route atomically fires `DAYWORK.POSTED + DAYWORK.INVITED` (single transaction via `appendEvents`), validates the target crew, and applies the 5/hr per-employer rate limit (`getQrHireLimit` in `lib/rate-limit.ts`). Postgres `23505` unique_violation on `daywork_invitations(daywork_id, crew_person_id)` → 409 with "You've already invited this crew to this posting." (spec v2.1 idempotency). New `POST /api/permanent/[id]/invite` invites a specific crew to an existing permanent posting — employer/agent hat, posting must be `status='active'` and owned by the caller, fires `PERMANENT.INVITED`, same 5/hr limit + 23505→409 mapping. `POST /api/permanent/[id]/apply` extended with optional `fromInvitationId` body field; route service-fetches the invitation, verifies (id, posting, crew, pending), threads through as `invited_from_id` in the PERMANENT.APPLIED payload. Forgiving design — failed validation drops the link silently and the apply still proceeds (handles expired/revoked invitations between deep-link click and submit). The 00131 projection flips the linked invitation row to 'applied' inside the same transaction with `AND status='pending'` race guard. New `handlePermanentInvited` push-trigger fans out notification body "Captain X invited you to apply for {role} on {vessel}" with deep-link data `{ screen: 'permanent-apply', permanentPostingId, fromInvitation }`.
- CV Builder (Stage 1, Phase 4): `GET /api/cv/[handle]` — public-ish QR-landing endpoint. Validates handle format (8-char alphanumeric), rate-limits via two new Upstash buckets (`getCvHandleAnonLimit` 20/hour per IP, `getCvHandleAuthLimit` 100/hour per auth user), returns full CV-shaped payload (profile + period-correct experiences + opted-in accepted references + sea-time totals when opted-in + canonical lookups + stale flag). Tombstone state (`{ tombstone: true }`) for deactivated/blocked/scrubbed crew. NDA mask respects per-experience `cv_show_full_vessel` toggle; references inherit the mask from their bound experience and default-mask when the FK was auto-nulled. Service-role DB reads — no RLS gating; the route's columns are CV-publishable by design. Caller identity (for rate-limit segmentation only) is read off the cookie session via `createClient().auth.getUser()` — no per-row authorisation. Public `/cv/[handle]` page registered in `middleware.ts` publicRoutes alongside `/ref/`; renders three states (signed-out teaser, signed-in employer/agent full profile with inert hire-action bar pending Phase 5, signed-in crew hat-switch hint + full data fallback) plus tombstone, NotFound, stale-CV amber banner, and a permanent-availability badge per spec §5.
- CV Builder (Stage 1, Phase 3): `/settings/cv` page renders a Stage-1 Coming-Soon banner, a locked "Generate CV" button (aria-disabled + Coming-Soon toast — deliberately does NOT call `/api/cv/generate`), and three working toggle sections — sea-time on/off (profile-level), per-reference Include-on-CV for accepted references, and NDA-vessel disclosure for experiences with `vessels.nda_flag = true`. Hat gate redirects agent users back to `/settings`. Profile page hot button locked the same way, crew-hat + crew-identity scoped. Toggles persist via `PATCH /api/cv/settings` — single endpoint, polymorphic body (`{ cvIncludeSeaTime }` / `{ referenceId, includeOnCv }` / `{ experienceId, cvShowFullVessel }`), crew-hat only at the route layer, owner-scoped at the DB layer (UPDATE WHERE person_id = caller for profiles/crew_experiences; requester_person_id + status='accepted' for references). Direct UPDATE (not event-sourced) because these are per-row UI display preferences with no projection cascade or temporal semantics — they live on event-sourced projection tables but the writes are isolated to the new columns added in 00131. GET endpoints extended to surface the new state: `/api/profile` returns the four CV columns, `/api/references/mine` returns `include_on_cv` per row, `/api/experiences` returns `cv_show_full_vessel` per row.
