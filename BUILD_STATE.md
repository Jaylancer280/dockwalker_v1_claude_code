# BUILD_STATE.md — DockWalker Build State

> This file is updated by Claude Code at the end of every session that changes code.
> See `CLAUDE.md` Documentation Governance for update rules.

## Completed Stages

### Web app foundation (Stages 1-162) — ARCHIVED

> Full descriptions in git history. Summary of what was built:

**Core (1-17):** Event ledger, projections, RLS, RPCs, all canonical lookups, daywork CRUD, templates, auth, onboarding, discovery, currency, job numbers.

**Daywork lifecycle (18-40):** Shortlist, in-progress status, completion confirmation, ratings, structured cancellation (employer + crew), postponement, work-started mutual confirmation, checklists, availability overhaul (3-state with city), correctness fixes.

**Crew experience (41-62):** Applied tab, hierarchical location picker, settings/account/GDPR, unit conversions, vessel LOA, crew experiences schema, experience CRUD + auto-derivation, NDA reveal post-acceptance, 60+ integration tests.

**Push + infrastructure (63-68):** Device tokens, targeted push (13 event types), broadcast (daywork posted), in-app toast, deep links, UX fixes.

**Data model evolution (69-80):** Vessel soft data separation, experience edit, contract types, job detail polish, working day dates, job expiry/extend, post-onboarding routing, view-only profile API + overlay.

**Security + hardening (81-100):** Aggregate type CHECK fixes, projection state guards, multi-crew positions, avatars, unread counts, cursor pagination, permanent opportunity signal, UX hardening, security hardening (20 routes), rate limiting, Stripe scaffolding, landing page, email templates, forgot password, Sentry error tracking.

**Docky AI (92-94):** pgvector + MCA chunks, RAG pipeline, Anthropic LLM, crew context + cert gap analysis, personalised prompts, free tier gating.

**Email + cron (101-102, 124):** Transactional emails (accept/apply/message), availability expiry reminder, engagement-starts reminder.

**Admin + realtime (103-106):** Admin tooling, Supabase Realtime messages, deep link config, Vercel analytics.

**Permanent jobs (127-139):** Full permanent posting lifecycle — schema, post, discover, apply (cert hard-gate), review (shortlist/select), negotiation resolution (confirm/revert/cancel/close), mine page, chat integration, push triggers, cron, GDPR, NDA reveal, 9 integration tests.

**Polish (140-162):** safeFetch migration, client resilience, user testing fixes (batches 1+2), profile restructure + deck name, avatar crop, notification preferences, profile completeness, skip onboarding, location city split, languages, agent identity + market feed, bug fixes, component extraction (UI-0), shared primitives, pre-TestFlight fixes, seed overhaul, hierarchical pills, page transitions, UX hardening, permanent post fields.

### Stages 163+ — Recent

> **Mobile app (Stages 163–173) archived 2026-04-29.** The native Expo app
> (`apps/mobile/`) was deleted along with the Capacitor legacy. See
> `docs/archive/mobile-web-split-spec.md` for the full record. Stage entries
> 170–173 below remain only as historical markers; the code they describe
> no longer exists in the repo.

- [Stage 72] Job detail vessel visibility — M/Y or S/Y prefix on vessel names across discover cards, application cards, invitation cards, and chat daywork summary; summary card extended with size band and LOA; context API returns vessel_type, loa_meters, vessel_size_bands

- [Stage 73] Working days calendar selection — `working_day_dates date[]` nullable column on dayworks and templates (migration 00035); post-projection trigger writes dates; API accepts optional `workingDayDates` array with range/duplicate validation; backward compatible; 488 tests pass

- [Stage 74] Job expiry + stale engagement cleanup — `POST /api/daywork/:id/extend` route with DAYWORK.EXTENDED event (migration 00036); mine API adds computed `is_overdue` flag; conversations API adds `rating_expires_at`, `rating_expired`, `is_overdue` (3-day grace); all query-time, no background jobs; 493 tests pass

- [Stage 75] Post-implementation regression fixes — (1) rollback 00034 now includes full `apply_projection` body (was missing, would cause NULL constraint violation on vessel_operation during replay); (2) rollback 00035 drops orphaned trigger + function before column drops; (3) IMO reveal wired through chat context API and daywork summary card (`imo_number` shown for crew with active engagements on NDA vessels, completing Stage 61's data layer)

- [Stage 76] Post-onboarding landing route — onboarding already redirects to `/profile` (confirmed); dashboard page now redirects to `/profile` for all hats (not hat-routed); profile page adds "Find daywork" (crew) or "My jobs" (employer) CTA button

- [Stage 77] View-only profile API — new `GET /api/profile/[personId]` with context validation (engagement, application, or invitation relationship required); crew profiles include bio, role, certs, experience bracket, vessel size exposure, experiences (no salary), past daywork count; employer profiles include agency, role specializations, non-NDA vessels, active posting count; 7 new tests (500 total)

- [Stage 78] Profile overlay component — `ProfileOverlay` slide-up card with crew view (expandable experience cards, cert/size-band pills, past daywork count) and employer view (vessels, role specializations, active postings); fetches from view-only API; loading spinner and error state

- [Stage 79] Wire profile drill-down into existing pages — discover page job cards show "Posted by {name}" tappable to overlay; application cards show poster name; invitation cards make employer name tappable; review page applicant cards get User icon button; chat kebab menu gets "View profile" as first item; discover API resolves poster display names; applications API returns `poster_person_id` and `poster_name`; invitations API returns `employer_person_id`; 500 tests pass

- [Stage 81] Fix missing aggregate_type CHECK values — migration 00037 adds `invitation` and `experience` to `events_aggregate_type_check` (both were missing since Stages 46/53, causing real DB failures); 4 new integration tests (INVITED, INVITATION_ACCEPTED, EXPERIENCE.ADDED, EXPERIENCE.UPDATED); aggregate_type consistency check script in pre-commit; all client-side `res.json()` error branches guarded with `.catch(() => ({}))`

- [Stage 82] Redirect after accepting applicant — dismissing the "Go to messages" dialog now redirects to `/daywork/mine` In Progress tab instead of leaving employer on empty review page

- [Stage 83] Post-migration smoke test procedure documented in `tasks/lessons.md` — flag for human edit to add to CLAUDE.md Session Protocol
- [Stage 85c] Multi-crew listings — `positions_available`/`positions_filled` on dayworks, `DAYWORK.POSITIONS_UPDATED` event, multi-crew acceptance (conditional fill), no auto-revert on cancellation, "Find replacement" CTA, dynamic invitation limits, multi-crew chat links, edit crew count dialog

- [Stage 85a] Profile photos — `avatar_url` on profiles, Supabase Storage `avatars` bucket, avatar upload/delete API, Avatar + AvatarUpload components, integrated across profile, onboarding, messages, review, profile-overlay
- [Stage 85b] Unread counts + notification centre — `message_read_cursors` + `notifications` tables, read cursor API, notification CRUD API, combined unread count endpoint, push-triggers write to notifications, bottom-nav badge, notification bell, notifications page, read cursor on chat mount
- [Stage 86] Cursor-based discovery pagination — `cursor` query param on discover API, `has_more`/`next_cursor` in response, auto-load when card stack <= 5, retry on empty post-filter batches
- [Stage 87] Permanent opportunity signal — `permanent_opportunity` boolean on dayworks/templates, crew rating `permanent_opportunity_accuracy` on engagement_ratings, badge on discover/mine/applications/review pages, toggle on post form, conditional rating question in crew completed-context form
- [Stage 88] UX hardening — 6 missing update-positions tests, error states on messages/notifications pages, toast notification system replacing all `alert()` in chat, confirmation dialogs on experience delete and daywork post
- [Stage 88b] Replace `confirm()` with Dialog in chat "Mark complete" action
- [Stage 89] Security hardening — vessel lookup IMO removal, messages person_id stripping, try/catch on 20 routes (25 handlers), safe request.json() on 17 routes, health check endpoint, body size limit config, avatar magic byte validation
- [Stage 90] Rate limiting — Upstash Redis via middleware, 100/60s global + 30/60s write limits, exempt health + webhooks
- [Stage 91] Stripe subscription scaffolding — subscriptions table, checkout/portal/status/webhook routes, subscription check helper
- [Stage 92] Docky Phase 1 — pgvector + MCA chunks, advisor conversations/messages tables, RAG pipeline, Anthropic LLM, Docky tab + chat UI
- [Stage 92b] Hat-scoped notification counts — separate notification/message counts per hat, alt-hat badge on hat switcher, role_context on notifications table
- [Stage 93] Docky Phase 2 — crew context builder, cert gap analysis, personalised LLM prompts, dynamic suggestion chips
- [Stage 93b] Docky Phase 2 cleanup — extract shared chip hook, cert-analysis unit tests
- [Stage 94] Docky monetisation gating — advisor_usage table, free tier 3/month limit, paywall card, billing page, subscription settings row
- [Stage 94b] advisor_usage RLS hardening — add INSERT + UPDATE policies
- [Stage 95] Codebase hardening — projection state guards on ACCEPTED/REJECTED, DAYWORK.EXTENDED moved into apply_projection, hat validation on update-positions, daywork_templates UPDATE policy, webhook env guard
- [Stage 96] Security headers (vercel.json) + onboarding event batching for atomicity
- [Stage 97] Offline resilience — network status hook, offline banner, safeFetch wrapper with timeout, success toast variant, unified error feedback on all 9 silent-failure fetch calls across discover/review/messages/profile
- [Stage 98] Landing page — product hero, value props, how-it-works, and CTAs replacing design system preview; middleware updated to allow unauthenticated access to root path
- [Stage 99] Email templates + forgot password flow — branded confirmation/recovery/email-change templates, forgot-password and reset-password pages, email confirmations enabled, middleware updated for auth flow routes
- [Stage 100] Error tracking — @sentry/nextjs integration, server + client + edge init, error boundary capture, global error boundary, DSN-gated (no-ops without env var)
- [Stage 101] Email notification fallback — Resend integration, transactional email templates for acceptance/apply/message, fire-and-forget delivery alongside push + in-app, message email rate limiting (5min cooldown per conversation)
- [Stage 102] Availability expiry reminder — Vercel Cron daily at 08:00 UTC, in-app + push notification 24h before availability expires, duplicate prevention, CRON_SECRET auth
- [Stage 103] Admin tooling — is_admin flag, admin guard, user lookup/search, stuck engagement detection + force-complete, canonical data CRUD, ADMIN.\* audit events
- [Stage 104] Realtime messages — Supabase Realtime subscription on messages table, replaces 5s polling on chat page, fallback to polling on connection failure, context polling retained
- [Stage 105] Deep links — apple-app-site-association + assetlinks.json, iOS entitlements, Android intent filters (domain placeholders for production)
- [Stage 106] Analytics — @vercel/analytics + @vercel/speed-insights, page views and web vitals tracking
- [Stage 107] Avatar storage verification — confirmed migration 00039 creates bucket + RLS + policies in all environments (no code change)
- [Stage 108] Bug fixes — DAYWORK.APPLIED email payload (person_id → crew_person_id), MESSAGE.SENT explicit is_system flag, INVITATION_ACCEPTED notification + deep link, WORK_STARTED_CONFIRMED deep link, invitation tab deep link routing, chat page polling cleanup leak (timeout + pollRef cleared on unmount, realtimeConnected moved to ref)
- [Stage 109] Hat validation hardening — hat check + dynamic roleContext on accept, reject, shortlist, view, cancel-employer, cancel daywork, complete, checklist/toggle, avatar upload routes; update-positions test coverage (10 tests)
- [Stage 110] API validation hardening — accept race guard in apply_projection (positions-full pre-check), extend route full validation (backward extension, workingDays bounds, workingDayDates range/duplicates/length), relist status check (in_progress only), template currency validation, onboarding avatar URL validation (HTTPS only)
- [Stage 111] GDPR export completeness (experiences, applications, invitations, ratings, device tokens, advisor conversations), EventType union fix (DAYWORK.EXTENDED, ADMIN.\*), admin canonical projection no-op handlers, clearAll availability via ledger (not direct DELETE), experience open-ended overlap fix (future-dated alongside current allowed)
- [Stage 113] Epaulette badges — auto-derived rank insignia (department symbol + seniority stripes) on profile experience cards, discover job cards, review applicant cards, profile overlay; 3 hybrid roles (Deck/Engineer, Deck/Stew, Cook/Stew) with split-symbol badges; migration 00055 for hybrid role data + department CHECK; RolePicker lists hybrids under both parent departments; gold (deck/bridge/engineering) and silver (interior/galley) color coding

- [Stage 112] UI state freshness — post form double-submit guard (ref-based), checklist toggle optimistic update with rollback on error, availability re-check before apply (5-min cache), mine page + review page counts already correct (array-derived)
- [Stage 114] Epaulette polish — experience card flex row layout (profile + overlay, chevron overlap fix), all 4 SVG icons replaced with filled silhouettes, view-profile icon on AvailableCrewCard, ApplicantCard tap target fix, supabase/README.md migration entries 00053-00055, My Jobs page epaulettes, post form live epaulette preview
- [Stage 115] Experience add/edit feedback — error toasts on all failure paths (vessel creation, experience creation, overlap, network), success toasts on add/edit/delete, stale data fix on profile redirect (router.refresh), try/catch submit hardening
- [Stage 116] Badge polish — bg-destructive to bg-primary on 3 notification badges, message count changed from total-unread to threads-with-unread, OfflineBanner hydration fix (deferred navigator.onLine to useEffect)
- [Stage 117] Discover card scroll containment — header z-index bump (z-10 to z-30), card stack overflow clip
- [Stage 118] Invitation accept improvements — auto-shortlist invited crew (source column on applications, conditional status in apply_projection), "Invited" badge on review page, stale invitation filtering (non-active dayworks excluded from crew view), source field on applicants API
- [Stage 119] Chat header cleanup (stripped to name + actions, job details in summary card only), vessel edit page (/vessels/[id]/edit with PATCH API), "My Vessels" Ship icon button on employer profile header
- [Stage 120] Toast consistency — success/error toasts on all mutations across discover (apply/withdraw/invite), mine (cancel/positions/templates), post (submit/save template), review (accept/reject/shortlist/invite), chat (completion/rating/work-started/checklist/postponement/cancel/relist), vessels (add)
- [Stage 121] Test gap coverage — propose-postponement (8 tests), respond-postponement (5 tests), respond-crew-cancel (5 tests), notifications/count (5 tests); invite/extend/relist already covered; 701 tests total
- [Stage 122] Test coverage completion — messages-read (4 tests), notifications-read (4 tests); checklist set/toggle, push-tokens, profile-view, experiences already covered by existing tests; 709 tests total
- [Stage 123] Crew nationality + visas — `nationalities` (40 entries) and `visa_types` (10 entries) canonical lookups, `nationality_id` and `visa_ids` on profiles, updated `apply_projection`, onboarding + profile edit + profile PATCH accept nationality/visas, flag emoji display on profile page + overlay + review cards (ApplicantCard + AvailableCrewCard), view-only profile API returns nationality + visas
- [Fix 118a] Invitations GET filters out fully-filled daywork positions (positions_filled >= positions_available); 2 new tests
- [Fix 123a] Rollback 00057 completed — full apply_projection body from 00056 restored (self-contained per CLAUDE.md rule 4)
- [Fix 123b] Onboarding UI — nationality dropdown (required for crew) and visa checkboxes added to profile step; wired into submit payload with validation
- [Stage 124] Engagement starts tomorrow reminder — daily cron (07:00 UTC), in-app + push + email notification for both parties, duplicate prevention, engagement-starting email template, exported getRecipientEmail; 5 new tests (716 total)
- [Stage 125] Notification count N+1 fix — `get_unread_counts` Postgres function (migration 00058) replaces per-engagement COUNT loops in `/api/notifications/count` and `/api/messages`; fixed query count regardless of engagement volume; tests updated to mock RPC

- [Stage 126] Pre-launch lint cleanup — ESLint warnings fixed (unused vars, unused imports, next/image), cron error handling
- [Stage 126b] Daywork engagement route hardening — daywork_id IS NOT NULL guard on 11 engagement routes, prevents permanent engagement IDs from triggering daywork events
- [Stage 127] Permanent jobs schema + types + events — permanent_postings table, permanent_templates table, profile availability columns, applications + engagements XOR extensions, 12 PERMANENT.\* event handlers in apply_projection, aggregate_type + application_status CHECK updates
- [Fix 127a] Integration test seed data alignment — vessel name/type/working_days expectations, self-contained application + lifecycle tests, experience size band fix, rating CHECK values, invitation cleanup
- [Stage 128] Permanent post API + form — POST /api/permanent, permanent template CRUD routes, PostingTypeSelector, PermanentPostForm, type selector wired into post page; 14 new tests (730 total)
- [Stage 129] Permanent discover API + scrollable UI — GET /api/permanent/discover with filters/pagination, PermanentJobCard/Detail/Feed components, Daywork/Permanent toggle on discover page; 10 new tests (740 total)
- [Stage 130] Permanent apply + cert hard-gate + career status — POST /api/permanent/:id/apply with cert superset check, PERMANENT.APPLICATION_BLOCKED intelligence event, profile PATCH permanent availability fields, career status section on profile page, availability route LEFT JOIN, Apply button enabled on cards; 10 new tests (750 total)
- [Stage 131] Crew permanent applications + unified Applied tab + withdraw — GET /api/permanent/applications, POST /api/permanent/:id/withdraw (PERMANENT.WITHDRAWN event), unified Applied tab with Promise.all fetch, PermanentApplicationCard with status badges, type-based rendering; 12 new tests (762 total)
- [Stage 132] Permanent review + shortlist + select — GET /api/permanent/:id/review, POST shortlist/reject/select routes, PermanentReviewPage with Applicants + Shortlisted tabs, shortlist cap enforcement, select confirmation dialog, negotiation banner, profile view permanent context; 16 new tests (778 total)
- [Stage 133] Negotiation resolution — POST confirm/revert/cancel/engagement-close routes, ConfirmPlacementDialog + RevertSelectionDialog + CloseConversationDialog components; 19 new tests (797 total)
- [Stage 134] Permanent mine page — GET /api/permanent/mine with application counts, PermanentMineSection with 5 tabs (Active/Negotiating/Filled/Cancelled/Templates), Daywork/Permanent toggle on mine page; 5 new tests (802 total)
- [Stage 135] Chat integration — messages API + context API dual LEFT JOIN for permanent_postings, EngagementContext type extended, PermanentSummaryCard, chat page type detection + conditional kebab menu (confirm/revert/close/cancel/withdraw), daywork actions gated behind !isPermanent
- [Stage 136a] Permanent push triggers + email templates — 8 notification types in push-triggers, 3 email templates (shortlisted/selected/placement confirmed), deep link routing for permanent events; 10 new tests (812 total)
- [Stage 136b] Cron + GDPR export + admin — engagement-starts cron resolves permanent role names, GDPR export includes permanent_posting_id + outcome + permanent_postings table, admin engagements show permanent_posting_id
- [Stage 136c] Final contamination checklist passed — all 812 tests green, zero daywork route contamination, single migration (00059), integration tests pass, documentation complete
- [Stage 137] Permanent integration tests — 9 new integration tests verifying all PERMANENT.\* event roundtrips against real DB (45 integration tests total)
- [Stage 138] Push-triggers decomposition — 1033-line monolith split into 9 focused modules (types, loaders, notification-mapper, daywork-handlers, permanent-handlers, event-router, email-dispatcher, broadcast, index); zero behavioral change
- [Stage 139] NDA reveal for permanent engagements — migration 00060 extends `get_vessel_public` with permanent_postings OR branch for IMO reveal
- [Fix 139a-f] Hardening batch — network failure try/finally on 8 pages, onboarding re-entrant retry, Stripe origin header hardening, Docky atomic usage RPC (migration 00061), integration test password fix, UUID validation on profile view param, ESLint cleanup
- [Stage 141a] safeFetch migration — replaced all 82+ bare fetch() calls across 20 client-side page/component files with safeFetch (15s timeout, typed discriminated union, zero unhandled rejections); updated 2 test mocks for res.text() compatibility; 812 tests pass
- [Stage 142] Client resilience fixes — (a) avatar upload reordered: validate magic bytes → upload new → delete old, preventing broken avatar_url on bad files; (b) chat realtime: reactive polling fallback that starts/stops based on connection state instead of one-shot 5s decision; (c) availability overlay: all 4 fetch calls → safeFetch with try/finally + deselected-date DELETE error handling; (d) completed safeFetch migration on remaining 13 client files (components, hooks, lib, onboarding); zero bare fetch() in client code; 814 tests pass
- [Stage 143] User testing fixes batch 1 — (a) working days input dynamically clamped to calendar span with helper text; (b) replacement button already correct — skipped; (c) removed 3 unused STCW certs via migration 00062; (d) back button on notifications page; (e) 3 extra daywork + 2 permanent seed postings for discover feed density; (f) fallback card in chat when job details unavailable
- [Stage 144a-i,k] User testing fixes batch 2 — (a) career status collapsed to compact inline display; (b) "Browse jobs" button copy + w-fit; (c) sign-out redirects to splash page; (d) splash page copy updated for daywork+permanent; (e) IMO partial prefix search at 4+ digits with debounced auto-trigger + dropdown; (f) crew hat vessels button ungated; (g) LOA display on discover daywork/permanent cards + application cards; (h) sea time migration 00063 + salary/sea time private intelligence UI on experience forms; (i) experience pill calendar-month calculation fix; (k) NDA vessel info callout in VesselSelector + daywork post confirmation dialog

- [Stage 144j] Auto-derived primary role + desired role — migration 00064 adds `desired_role_id` to profiles, updates `derive_experience_profile()` to auto-derive `primary_role_id` from latest experience; profile page shows "Current Role" (non-editable) + "Desired Role" (editable via RolePicker); experience bracket selector removed from edit mode (auto-derived); profile overlay shows "Seeking: {role}"; view-only profile API includes desired_role; 819 tests pass
- [Fix 144-batch] User testing fixes — (1) vessel form motor/sail; (2) salary per day; (3) size band removed from exp header; (4) visibilitychange profile re-fetch; (5) cert checker error handling + re-fetch; (6) vessel size exposure read-only; (7) comprehensive reseed (4 experiences, 10 daywork, 7 permanent, templates, availability); (8) cert match/mismatch coloring on all job cards
- [Stage 145] Profile restructure + deck name — migration 00065 adds `deck_name` to profiles with supplementary trigger; profile header shows flag + epaulette + deck name inline; crew view mode reorganised into 4 semantic sections (Summary, Looking for, About, Experience); availability collapsed from hero card to compact row; deck name on profile overlay, chat header, review cards; "Browse jobs" button removed; 819 tests pass

- [Stage 146] Avatar crop + cache bust + permanent post wiring + scroll containment + notification strategy + profile clarity — (A) `react-easy-crop` + `ImageCropper` component for circular crop before upload; (B) `?t=Date.now()` cache-bust on avatar URL in API + event payload; (C) `permanentTemplateId` URL param auto-selects permanent posting type + pre-loads template; (D) `useBodyScrollLock` hook on 6 overlays + profile overlay max-height fix; (E) email reduced to 2 critical events (DAYWORK.ACCEPTED, PERMANENT.SELECTED) + engagement-starts cron, gated behind `hasPushTokens` + `email_enabled` preference; push gated behind per-category preferences; notification preferences migration 00066, GET/PATCH `/api/preferences`, settings page notifications section; `PushPrompt` after onboarding; email footer unsubscribe link; (F) collapsible profile sections with summaries, value-driven empty-field prompts, "How employers see you" preview button, `computeSeaTime` utility; (G) `useDockyReadiness` hook, Docky profile completeness nudge card, inline thinking indicator context; 819 tests pass

- [Fix 147a] Self-view profile fix — `GET /api/profile/[personId]` no longer rejects self-viewing; "How employers see you" preview now works by skipping the relationship context check for own person_id

- [Stage 148] Profile polish — (A) `computeSeaTime` → `computeTotalExperience` rename (file, function, tests); (B) collapsible section state persisted to localStorage; (C) employer hat sees career status in Looking For section, agent profile fields wrapped in collapsible "Agency Info" section with empty-state prompts; (D) empty Summary section shows "Add your first experience" prompt

- [Stage 149] Skip onboarding + profile nudge + onboarding fields — (A) "Skip for now" link on profile step (crew only), auto-derives display name from email prefix, navigates to hat selection; (B) profile completion nudge card on discover page for incomplete profiles (email-prefix name, no nationality, no role/certs); (C) deck name, desired role, career status fields added to onboarding profile step (crew only, all optional); onboarding API passes through to PROFILE.CREATED payload; migration 00067 adds supplementary trigger for career status fields on PROFILE.CREATED events; 844 tests pass

- [Stage 150-pre] Split location city from daywork port — migration 00068 adds `location_city_id` FK to profiles with supplementary trigger; profile GET/PATCH/view APIs return city-level location with joined region; profile Summary shows city+region, Looking For relabelled to "Daywork port"; edit mode has separate city picker ("Where are you based?") and port picker; onboarding uses city picker for crew, port picker for agents; profile overlay shows city-level location; `EventPayloadMap` extended with `location_city_id`; 844 tests pass

- [Stage 150] Languages — full stack — shared `LANGUAGES` constant (`apps/web/src/lib/languages.ts`); profile API GET/PATCH/view returns and accepts languages with validation; profile page About section shows language pills (view + edit), collapsed preview includes language count; profile overlay shows language pills; migration 00069 adds `required_languages text[]` to dayworks, permanent_postings, daywork_templates, permanent_templates with supplementary trigger; daywork + permanent POST APIs accept `requiredLanguages`; template save/load includes languages; discover APIs return `required_languages`; daywork + permanent cards show green/amber language pills matching crew's declared languages; permanent detail view shows language pills; employer review APIs include crew `languages` in profile select; applicant cards show language count badge; 844 tests pass

- [Stage 151] Agent identity — (A) agent onboarding: identity step subtext, agency name required, nickname/nationality/role specializations, bio hidden, hat hardcoded to `agent`; (B) agent profile: "How candidates see you" preview, Maritime Background section, My Vessels link, `buildAgentProfile` API with experiences; (C) experience constraints: `isCurrent` blocked, `endDate` required, UI checkbox hidden, page titles renamed; (D) market feed: `/discover/market` read-only mixed feed with filters + pagination, poster identity stripped server-side (Option A), "View job market" button on My Jobs, agent redirect from `/discover`; (E) activity log: migration 00070 `agent_activity_log` table with RLS, `POST /api/agent/activity` route, `logAgentActivity` client helper, debounced filter telemetry; 856 tests pass
- [Stage 152] Bug fixes + architectural cleanup — (Fix 151a-d) RLS, agent notifications, middleware redirect, permanent review languages; (152e) migration 00072 consolidates 6 triggers into apply_projection; (152f) profile page decomposed 1632→~400 lines + 6 components; (152g) discover page decomposed 1793→718 lines + 4 components; (152h) deferred — agent market kept as separate page, middleware redirect sufficient; (152i) avatar try/catch; (152j) skipped — language list stable at 20 hardcoded entries; 856 tests pass
- [Stage UI-0] Decomposition + component extraction — 5 shared UI primitives (EmptyState, LoadingSpinner, SegmentedToggle, BottomSheet, UnderlineTabs); onboarding 1440→452 + 6 steps; review 1216→472 + 7 components; permanent post form 529→358 + 4 sections; experience forms 731+500→264+244 + 4 shared sections; settings 667→136 + 4 sections; mine 744→589 + 6 components; market 612→302 + 3 components; 856 tests pass
- [Fix UI-0] Wire shared primitives — EmptyState adopted in 12 files (25 refs), LoadingSpinner in 15 files (41 refs), SegmentedToggle in 2 files, UnderlineTabs in 3 files, BottomSheet in 5 chat overlays; 91 total adoptions replacing inline patterns; 856 tests pass
- [Pre-TestFlight fix batch] 9 fixes for shipping — (1) vessels RLS: 3 new SELECT policies for non-owner reads (migration 00074), 6 integration tests; (2) employer /discover redirect (middleware + client); (3) crew /review redirect (middleware regex); (4) profile banner narrowed to email-prefix only; (5) discover feed error state; (6) applied tab error handling + placement_confirmed terminal status (migration 00075); (7) daywork mine cancel confirmation dialog; (8) cert/language pills unified across daywork/permanent forms; (9) availability timezone date-only comparison; 860 unit + 53 integration tests pass

- [Fix batch 153] UX fixes — (1) signup page polls for confirmed session every 3s, auto-redirects to /onboarding when email confirmed in other tab; (2) login page shows info message on `?error=auth_failed` query param; (3) availability overlay + API allow "not available" without city selection (cityId optional when notAvailable: true); (4) removed duplicate "Display name" input from profile page avatar section; (5) experience section empty-state CTA visible even when section is collapsed (Button with Plus icon); 870 tests pass
- [Fix 154] SUG fixes — SUG-001: added client-side hat guard on review page (crew redirected to /discover on client-side navigation); SUG-004/011/016 verified already fixed in pre-TestFlight batch (migration 00074, cancel dialog, middleware+client redirect); updated review-available-tab test mock for new auth.getUser() call; 870 tests pass
- [Fix 155] Quick wins batch — (A) PF-001: discover daywork feed crash — null `daywork_id` from permanent applications polluted `excludedIds`, breaking the `.not('id','in',...)` filter and returning zero jobs; added `.filter(Boolean)`; (B) experience brackets: migration 00076 seeds 5 brackets into production (seed file only runs locally); fixed seed `ON CONFLICT`; (C) app icon: copied new full-bleed DockWalker icon; (D) overscroll: `overscroll-behavior: none` on html+body + `scrollEnabled: false` in Capacitor iOS config; 906 tests pass
- [Fix 156] Role gate fixes — added client-side hat guard to permanent review page (crew redirected to /discover); extended middleware crew-review regex to cover `/permanent/[id]/review` alongside daywork; SUG-016 employer discover redirect already working from pre-TestFlight batch; 906 tests pass
- [Stage 157] Seed data overhaul — realistic names (Hein van der Merwe, James Thornton, Sophie Laurent, Victoria Chase); DockWalker avatar on all profiles; nationality + visa IDs via subselect; crew-owned vessel copies for experiences (new UUIDs 33a/33b/33c); employer keeps original vessels for postings; Sophie gets Palma availability + permanent immediate + applies to DW-03; agent posts 2 daywork + 3 permanent positions; discover feed shows jobs from multiple posters; duplicate direct-INSERT experiences removed from 002 (003 handles via events); 906 tests pass
- [Stage 158] Hierarchical pill picker — new `HierarchicalPills` component with `rolesToGroups`/`certsToGroups` helpers; single-select (roles) and multi-select (certs) with category→item drill-down; deployed to all role pickers (daywork/permanent/profile/onboarding/discover), all cert pickers (grouped by category), visa+language pickers (flat pills replacing checkboxes); location pickers kept as popover; old `DepartmentRolePills`/`RolePicker` no longer imported; 906 tests pass
- [Fix 158b] Profile edit cleanup — agent Role Specializations converted from checkboxes to HierarchicalPills (multi-select by department); removed unused Checkbox import; resolved merge conflict markers from stash; duplicate Display name already fixed in Fix batch 153; 906 tests pass
- [Stage 159] Page transition speed — bottom nav `prefetch={true}`; skeleton `loading.tsx` for discover/messages/profile/daywork-mine; SWR installed + `useSafeFetch` hook; messages page converted to SWR (cached data on re-nav); discover/profile/mine SWR deferred; 906 tests pass
- [Fix 160] UX hardening A-D — "Pull down to refresh" → "Tap Retry" with Retry button; reject applicant confirmation dialog; template deletion confirmations (3 locations); error toasts on data export + account deletion failure; 906 tests pass
- [Stage 161] UX hardening E-G — (E) profile section completeness hints: "X fields not set" in collapsed Summary/Looking For/About headers; removed "Complete your profile" banner from discover; (F) onboarding progress dots: animated dot indicator with step count, skips welcome, conditional step sequence per identity/experience; (G) unread message indicators: blue dot on avatar + bold name for conversations with unread messages, wired `get_unread_counts` RPC; 906 tests pass
- [Stage 162] Permanent post form fields A-D — migration 00077 adds `contract_type`, `contract_details`, `description`, `meals`, `positions_available`, `positions_filled` to permanent_postings + templates; updated `apply_projection` PERMANENT.POSTED handler + all PERMANENT handlers matched to 00075 version; TypeScript types extended; POST route accepts new fields with validation; discover + mine APIs return new fields; template CRUD saves/loads new fields; form wired with contract type selector, description textarea, meals checkboxes, positions input; 906 tests pass
- [Fix 162b] Permanent post display + tests — job card shows contract type, positions (total only, no filled count for crew), meals badges; chat PermanentSummaryCard shows contract type; discover API strips `positions_filled` from crew response; context API returns `contract_type`; 2 new tests (invalid contract_type + positions out of range); 908 tests pass
- [Fix 162c] Post-review cleanup — resolved merge conflict in permanent-summary-card.tsx; rollback 00077 made self-contained with full apply_projection body; removed unused `optional` prop from HierarchicalPills; added pre-commit hook to block merge conflict markers; Capacitor static export dead-end removed (CAPACITOR_BUILD conditional, capacitor-build.sh, 6 \_client.tsx wrapper splits); 908 tests pass

- [Stage 163] Mobile Phase 1 — Turborepo, Expo SDK 53 shell, `packages/shared/` extraction (4 modules, 43 web imports), `createMobileClient`, auth context, 5-tab nav, onboarding shell
- [Fix 163] Auth context schema bugs + template asset cleanup
- [Stage 164-165] Mobile Phase 2 — Discovery + Swipe: daywork swipe stack (gesture handler + reanimated + haptics), permanent infinite scroll, job detail sheets, filter panel, availability overlay with city picker, apply flow, canonical data hooks
- [Fix 165/165b] Permanent detail cert gate, MMKV toggle, pre-Phase 3 audit (3 critical: onboarding identityType, availability cityId, app.json bundle ID + 3 high)
- [Stage 166] Mobile Phase 3a — Employer foundations: vessel selector, form pickers (role/location/cert/language), hat-conditional tabs, My Jobs screen, post type selector, apiGet, shared grouping extraction
- [Fix 166] Vessel API response shape, required IMO+LOA validation, hook extraction
- [Stage 167] Mobile Phase 3b — Post daywork + permanent forms with all fields, validation, hat guards
- [Stage 168] Mobile Phase 3c — Daywork review (swipe stack, auto-view, accept/reject/shortlist), permanent review (shortlist/select with cap), applicant card, template hooks + selector
- [Stage 169] Mobile Phase 4 — Conversation list, chat thread with realtime, summary cards, engagement action overlays (cancel/postpone/rate/checklist/work-started/complete), permanent actions. Fix: meal casing, selected_crew_name resolution

- [Fix 258e] NDA references — drop write-time gate, mask at display layer. The original NDA gate forbade references on NDA vessels, but the rule was internally incoherent: the referee was the captain/HOD on that vessel and obviously already knew the IMO; hiding it from them made verification impossible. NDA's actual purpose is hiding identity from OUTSIDE parties during the hiring cycle, not between insiders. Migration 00130 re-creates `apply_projection` from the 00129 body with the 3-line NDA block dropped from REFERENCE.REQUESTED (existence + hidden_at checks remain; handler count unchanged at 82). API helper `checkVesselReferenceGate` drops the NDA branch in lockstep. Profile-experience-section drops `ndaBlocked` from the gate so users can add references on their NDA experiences. **Display-layer masking** lands in `/api/messages/[engagementId]/context`: when the chat is a reference-contact engagement AND the caller is the employer side AND the underlying vessel is currently NDA AND the caller has no separate active daywork/permanent engagement on that vessel with the crew (existing 00083 trust-boundary precedent), the route replaces `snapshot_vessel_name` with `'NDA Vessel'` and nulls `snapshot_vessel_imo`. The referee always sees full snapshot (they were aboard). Other surfaces audited and confirmed safe: `/api/profile/[personId]` already strips snapshot fields from references[]; `/ref/[token]`, `/api/references/mine`, `/api/account/export`, and `/messages` consent_prompts are all party-only views; ContactReferenceDialog only takes refereeDisplayName. Stress test extended (`scripts/stress-test-references-currently-onboard.ts`) with section D — 4 new live-DB checks (NDA REFERENCE.REQUESTED succeeds, snapshot captures full name+IMO regardless, referee reads full snapshot via RLS, vessel `nda_flag` observable for display-mask inputs); 21/21 pass against the remote project. 1347 unit tests pass (+3 net: existing NDA-rejection test flipped to NDA-acceptance happy-path, +3 new messages-context masking tests covering referee-full / employer-masked / employer-engaged-unmasked).

- [Fix 258d] References on currently-onboard experiences — drop the B-2 `is_current=true` gate so first-vessel crew can gather references from the captain they're working under right now (the most valuable referee they'll ever have, blocked from speaking by the original retrospective-only rule). Migration 00129 re-creates `apply_projection` with the 00128 body modified in two handlers: REFERENCE.REQUESTED loses the B-2 block (existence check stays); EXPERIENCE.UPDATED's edit-lock is loosened so currently-onboard experiences with active references can transition to completed exactly once (`end_date` null→date, `is_current` true→false) — `vessel_id`/`role_id`/`start_date` remain unconditionally locked; the projection then auto-propagates the new end_date to references whose `snapshot_end_date` was captured null, so referee-verified records show "Jan 2024 — Mar 2025" instead of "Jan 2024 — Present" forever after the user leaves. API mirrors: `/api/references` POST drops the is_current rejection, `/api/experiences/[id]` PATCH loosens the lock with explicit `wasOnboard` detection. UI: `profile-experience-section.tsx` drops `currentBlocked` from the gate; `experience-details-section.tsx` gains an `allowClosing` prop that keeps end_date + is_current editable when refs are active and the experience was loaded as currently-onboard; the edit-experience page tracks `initialIsCurrent`/`initialEndDate` and gates the closing transition behind a one-time confirmation dialog ("This locks the end date permanently. Once saved, references on this vessel can't be edited.") before saving. 1344 tests pass (+3 net: existing B-2 rejection test flipped to a happy-path currently-onboard test, +3 new edit-lock tests covering closing transition allowed / completed-experience date change rejected / role-change always rejected).

- [Fix 258c] References pending-vessel relaxation — drop the `source = 'curated'` gate from REFERENCE.REQUESTED so users can add references to their own pending vessels while admin curation catches up. Migration 00128 re-creates `apply_projection` with the 00126 body minus the 3-line source check (handler count unchanged at 82). API-side gate in `lib/references/helpers.ts#checkVesselReferenceGate` drops the same check (NDA + hidden_at gates remain). Profile experience cards drop `sourceBlocked` from the Add/badge gating in `profile-experience-section.tsx`. Without the relaxation, the new pending-curation flow (00119 + `/api/vessels` POST setting `source: 'pending'`) created a dead-end UX where every freshly-added vessel locked references behind admin approval. 1341 tests pass.

- [Fix 222e] Imagery rollout (empty-state photos + desktop hero strips + auth ambient) — (1) Empty-state photos on Applied tab, Permanent job feed, Invitations tab (previously lucide icons); (2) new shared `HeroStrip` component wired above Messages list, Permanent feed, Invitations tab, Vessels page, Notifications page — 60px photo band hidden on mobile to keep viewport dense; (3) new `AuthAmbientBackground` component (hero-bow.jpg + bg-background/80 scrim + backdrop-blur) on all 4 auth pages + `/not-found`, mobile unchanged. Deliberately skipped: landing hero side illustration (hero already anchored by 208px logo + "how it works" image grid), billing page ambient (would fight with app shell sidebar bg). `profile.jpg` still unplaced — pending user decision on whether to use as (a) deactivated-user view on view-only profile, (b) own-profile empty state, or (c) new "profile not found" page. 1164 tests still passing, no behavioural change.

- [Fix 222g] Notifications grouping — server-side collapse of noisy types. Migration 00110 adds `grouped_notifications()` RPC (SECURITY INVOKER, respects RLS) that returns the current user's notifications partitioned by `(type, coalesce(deep_link, ''))` — one group row per partition with the most-recent title/body + `total_count` + `unread_count` + `latest_id`. `GET /api/notifications?grouped=true` calls the RPC; `POST /api/notifications/read-group` marks every unread notification in a `(type, deep_link)` group as read in a single UPDATE (with the `deep_link` field required — omitting it returns 400 so we never accidentally clear an entire type across all resources). Notifications page now defaults to grouped mode, shows a count badge on collapsed groups ("New applicant · 3 · 2 earlier"), and marks-read at the group level on tap. Raw-list path (`?grouped` absent) preserved for any external consumers. 5 new tests (grouped RPC handler, read-group happy path, null-deep-link path, missing-type rejection, omitted-deep_link safety rejection); 1174 total passing. Migration applied to live via `supabase db push` before commit per lessons.md.

- [Fix 222f] Regions country_code format CHECK — migration 00109 adds `CHECK (country_code ~ '^[A-Z]{2}$')` on `regions`. NULL permitted (three-valued logic), populated rows must match ISO-3166 alpha-2. Stops admin canonical CRUD typos ("gb", "England") from breaking flag-emoji / country-filter downstream. Schema version v108 → v109.

- [Fix 222d] Voice calling deferred post-launch — WebRTC calls over Supabase-Realtime-signaling + Twilio-TURN produced too narrow a reliability window for a cold-start platform (symmetric NAT on yacht-crew networks, MiFi hotspots, satellite links routinely break P2P even with TURN). Phone button in `chat-header.tsx` now always renders (more coverage — both daywork + permanent chats) but `aria-disabled` + `opacity-60`, click shows a "Voice calls — coming soon" toast instead of opening a call. `IncomingCallListener` unmounted from `(app)/layout.tsx` (with its `DeferredMount` wrapper + the now-unused `personId` local). All the hand-rolled WebRTC infra kept as dead code for a future restore: `use-voice-call.ts` (hook still mounted on chat page for inert state), `voice-call-context.tsx`, `VoiceCallProvider` in layout, `call-bar.tsx`, `incoming-call-listener.tsx`, `/api/calls/turn-credentials`, `/api/messages/[id]/call-ended`. When voice ships, the plan is to swap the hand-rolled stack for a managed RTC provider (LiveKit Cloud preferred) rather than fix Twilio — SFU routing solves the yacht-network problem, brings call history for free, and removes ~300 lines of ICE-restart + timeout bookkeeping. Voice-calling browser QA matrix removed from BLOCKED (no longer a launch gate); full restoration checklist recorded in `tasks/todo.md` Backlog → Deferred post-launch. 1164 tests still passing.

- [Fix 222c] Email templates Level 1 + 2 polish — content enrichment + branding. Shared `wrap()` now embeds the DockWalker white logo against the web-app's dark bg (`#111a24`), switches the accent/CTA to the app's light-theme primary (`#2d7de0`), and prepends a hidden preheader span so inbox clients show useful preview text alongside the subject. New helpers `formatVesselName()` + `formatEmailDate()` centralise M/Y-vs-S/Y prefixing and en-GB long-form date rendering. Subjects now surface role + vessel + key date (e.g. "You've been accepted — Deckhand aboard M/Y Serenity"); `applicationReceivedEmail` adds the applicant's experience bracket + home city as differentiator bullets; `newMessageEmail` truncates long previews at 100 chars with an ellipsis and names the engagement role in the subject; `engagementStartingEmail` includes vessel when known; `permanentShortlistedEmail` deliberately omits vessel name (NDA-safe); `permanentPlacementConfirmedEmail` CTA now deep-links to the specific engagement. New loaders `getDayworkContext`, `getPermanentPostingContext`, `getApplicantProfileSummary`, `getEngagementRoleName`, `getActiveEngagementIdByPermanentPosting` batch the richer data server-side. Engagement-starts cron extended to batch-fetch vessel names for dayworks + permanent postings. 17 new tests (template helpers + per-template coverage + dispatcher context-passing assertions); 1164 total passing.

- [Fix 222b] Email notifications production-hardened for the current fallback-only window (mobile app blocked, web never registers push tokens, WhatsApp pending). New `src/lib/email/cooldown.ts` layers three Upstash-backed caps so email stays useful not spammy: 15-min per-conversation on `MESSAGE.SENT`, 60-min per-(poster × daywork) on `DAYWORK.APPLIED`, 20/24h global safety cap per recipient. `sendEmail()` now wraps the Resend call in try/catch with `Sentry.captureException` (tagged `source: 'email-dispatch'`, subject in extras, recipient email scrubbed for PII) and emits a one-shot Sentry warning when `RESEND_API_KEY` is missing in production. Default fallbacks for `NEXT_PUBLIC_SITE_URL` (5 sites) and `RESEND_FROM_EMAIL` corrected from `dockwalker.com` → `www.dockwalker.io` / `dockwalker.io`. 13 new tests (7 cooldown + 2 send hardening + 4 dispatcher cooldown integration), 1147 total passing.

- [Fix 222a] WhatsApp connect flow gated behind a disabled "Coming soon" button pending Meta Cloud API integration. Connected users (from earlier test connections) retain the disconnect affordance and send-via-WhatsApp toggle; the phone/OTP step components are unreachable but left in place so the feature can be re-enabled with a single-line diff when delivery lands.

- [Stage 222] Admin moderation — full-profile wipe on Delete + explicit confirmation dialogs. Migration 00108 extends `PERSON.DATA_SCRUBBED` to null every profile PII field (22 columns) plus `persons.current_hat`, and to delete `crew_experiences` + `shore_experiences` rows. `custom_access_token_hook` now emits `onboarded=false` when `current_hat is null`. Middleware DB fallback and `requireDomainUser` DB fallback both updated to treat null `current_hat` as not-onboarded. Onboarding route branches on re-onboarding — when a restored user's `persons` row exists with null hat, it fires `PERSON.HAT_CHANGED` + `PROFILE.UPDATED` via `appendEvents` instead of the `onboard_person` RPC (which would PK-conflict). Admin user detail page swaps the inline Block/Delete modals and the bare Unblock/Restore buttons for a shared `AdminActionDialog` component; all four actions (Block / Unblock / Restore / Delete) now render an explicit confirmation dialog describing exactly what the action does — Restore specifically calls out "previously Deleted → profile wiped → user re-onboards on next sign-in", and Delete spells out the email-remains-reserved caveat. `router.refresh()` fires after each action so the admin page reflects the new state. 1134 tests pass (+3 new).

- [Stage 221] Admin Phase 2 — user reporting system. Migration 00107 creates `reports` CRUD table with enum CHECK constraints, self-report prevention, RLS (authenticated INSERT + SELECT own, admin via service client), admin_delete_person extended for cleanup. User-facing: `POST /api/reports` (rejects self-report, caps at 5 open), `GET /api/reports` (list own), `ReportDialog` wired into chat kebab + sidebar (engagement pre-filled) and profile overlay. Admin: `GET /api/admin/reports` with safety_concern priority sort + status/reason/filed_against/filed_by filters + pagination, `GET /api/admin/reports/:id` with reporter/reported/admin display names, `PATCH /api/admin/reports/:id` (validates enums, stamps admin_person_id + resolved_at on terminal status). Admin reports page `/admin/reports` with filter + inline detail panel + update form. Admin user detail page gains "Filed against" + "Filed by" sub-tables. Sidebar link added. 1131 tests pass (+20 new).

- [Stage 234] Daywork swipe gesture removed from the discover deck. The framer-motion-driven `SwipeableCard` wrapper around `JobCard` (drag, rotate, APPLY/PASS overlays, haptics, exit animations, imperative `triggerApplySwipe` for compose-message submit) was the core mechanic for the (now-deleted) native mobile target — on desktop it didn't fit the input model and the drag handler was intercepting taps on the new "+X more certifications" expand pill. Top card now renders as a plain `<JobCard>`; the existing red "Show next" + green "Apply" buttons in `DayworkBrowse`'s action row become the only entry points (they were already the visible primary CTAs — the swipe was a parallel path). Dead-prop sweep through the chain: `swipeRef`, `SwipeableCardHandle`, `triggerApplySwipe`, `onAvailabilityGate` (was the swipe-time gate trigger; `requireAvailability()` already opens the dialog itself before tap-driven applies), and `hasAvailability` (was the `canApply` gate inside SwipeableCard) all dropped from `daywork-card.tsx`, `daywork-browse.tsx`, `discover-browse.tsx`, and `discover/page.tsx`. `handleMessageSubmit` no longer fires the imperative swipe-then-apply animation — just calls `handleApply` directly. `framer-motion` + `haptics` imports gone from `daywork-card.tsx`. Employer-side swipe (applicants-tab + available-crew-tab in the review flow) deliberately untouched — out of scope. CLAUDE.md still says daywork is "via swipe mechanic" and dockwalker_mission.md reads similarly; both are read-only and need a human edit to align with this product decision. 1513 tests still pass; type-check + lint clean.

- [Stage 233] Capacitor legacy fully removed. Deleted `apps/web/capacitor.config.ts`, the entire `apps/web/ios/` and `apps/web/android/` native project trees, and `apps/web/scripts/cap-build.mjs`. Dropped 8 `@capacitor/*` packages from `apps/web/package.json` (`@capacitor/core/cli/ios/android/haptics/push-notifications/splash-screen/status-bar`) plus the 4 `cap:*` npm scripts. Two web-side components retired (only purpose was native gating): `apps/web/src/components/native-init.tsx` (called `registerPushNotifications()` from `Capacitor.isNativePlatform()`) and `apps/web/src/components/push-prompt.tsx` (rendered an "Enable notifications" card on native only). Removed their import + render from `apps/web/src/app/layout.tsx` and `apps/web/src/app/(app)/discover/page.tsx`. `apps/web/src/lib/haptics.ts` reduced to 5 no-op async stubs (zero deps); the existing 4 swipe callsites in `discover/_components/daywork-card.tsx`, `daywork/[id]/review/_components/applicants-tab.tsx` + `available-crew-tab.tsx` keep calling them and silently no-op. `apps/web/src/lib/push-notifications.ts` reduced to a single export — `resolveDeepLinkUrl(data)` — preserved because the server-side push-trigger handlers + `__tests__/lib/push-deep-link.test.ts` import it; the Capacitor-bound `registerPushNotifications` and `deregisterPushToken` are gone. `safe-fetch.ts` and `globals.css` Capacitor comments rewritten to platform-neutral language. `apps/web/README.md` Capacitor helpers section deleted; push-tokens documentation rewritten to clarify the table is dormant infrastructure (no native shell registers tokens; the API + dispatcher + trigger handlers stay live for any future web-push or worker client). Mobile-web split spec + TestFlight guide archive headers (drafted in Stage 232's working tree but not landed) included in this commit. 1456 tests pass; type-check clean; 0 lint errors.

- [Stage 232] Mobile app + spec archived — `apps/mobile/` (Expo/React Native) deleted entirely from the repo. Mobile-web split spec moved to `docs/archive/mobile-web-split-spec.md` with an archive header documenting why (53 React Hook violations between `post-daywork.tsx` and `post-permanent.tsx`, missing References surface, unverified "Mac+Xcode blocked" narrative with no Sentry/crash-logs to back it up, partial-feature audit conclusion that the mobile branch was net-negative for codebase coherence). `tasks/testflight-tester-guide.md` archived alongside. New `docs/archive/README.md` documents the archive pattern. `CLAUDE.md` scrubbed of all mobile/Capacitor invariants: dropped apps/mobile + EAS Build rows from the stack table, removed the "Two Frontends, One Backend" architectural invariant (replaced with a "Shared Packages Stay Pure-TS" cross-platform-discipline note), removed the Capacitor status section, removed mobile-specific correctness criteria (Cold start, Swipe gesture, Realtime, Push deep-link, Offline reads), removed mobile mentions from Pre-Commit Hook Requirements, Test Requirements, Documentation Governance table, Session Protocol Orient list, "API-first rule (mobile)", Human Review Checklist item 6. Root `README.md` monorepo-structure block updated to remove `apps/mobile/`, plus a paragraph pointing readers at the archive. `BUILD_STATE.md` "Mobile app (Stages 163+) — CURRENT" header renamed to "Stages 163+ — Recent" with an archive note prepended; "In Progress: Mobile Phase 7" replaced with "(none)". `tasks/todo.md` P1-M3 mobile-blockage item removed; Sentry P1-I-1 mobile mention dropped; voice-calling backlog browser-QA matrix narrowed to desktop only. Memory store cleaned: 4 mobile/Capacitor memory files deleted (`feedback_capacitor_refresh.md`, `feedback_no_mobile.md`, `project_mobile_architecture_split.md`, `project_mobile_blocked.md`); MEMORY.md sections renamed (Architecture Decisions/Blockers → single "Platform Scope" entry pointing at the archive). 1456 tests still pass; type-check clean; 0 lint errors. Capacitor src callers + config deletion follows in Stage 233.

- [Stage 231] CV Builder v1 — full user-level lockdown (no migration). Per the 2026-04-29 product call, every user-facing CV-related surface is hard-locked behind a "Coming Soon" gate, including paths Phase 5 had wired up (QR-landing, hire-from-QR, apply-after-invite, settings toggles). Single source of truth: new `apps/web/src/lib/cv/feature-flag.ts` exporting `CV_BUILDER_ENABLED = false` + `CV_BUILDER_LOCKED_PAYLOAD` constant. **Locked routes** return 503 with the locked payload BEFORE any auth or DB work: `PATCH /api/cv/settings`, `GET /api/cv/[handle]`, `POST /api/permanent/[id]/invite`. **Conditional locks** (the route still works for non-CV paths): `POST /api/daywork` rejects only the QR-hire branch (when `inviteCrewPersonId` is supplied — regular daywork POST untouched), `POST /api/permanent/[id]/apply` silently drops `fromInvitationId` (regular `/discover` apply untouched, just no invitation attribution recorded). **Locked UI**: `/settings/cv` toggles fire a Coming-Soon toast on tap and don't call PATCH (banner copy updated to clarify they don't save anything yet); `/cv/[handle]` page early-returns to a dedicated `<ComingSoon />` screen with no fetch; `/daywork/post?invite=…` early-returns to a Coming-Soon screen with a "Go to My Jobs" link; `/permanent/[id]/apply` strips the invitation context query param so the regular apply UX still works for any direct-navigation case. **Untouched** (Stage 2 unlock-ready): admin mint-handle route, schema, projection, types, daily invitation-expiry cron. Stage 2 unlock is a single-flag flip from `false` → `true` with no other code changes. Test coverage preserved by `vi.mock`'ing the flag to `true` in the existing 5 underlying-behaviour test files (cv-settings, cv-handle, permanent-invite, daywork-qr-hire, permanent-apply-from-invitation) plus 8 new locked-state tests in 3 new files (cv-routes-locked, daywork-qr-hire-locked, permanent-apply-from-invitation-locked) proving the 503/silent-drop paths. Page-level test (cv-settings-page) updated to assert the locked toggle behaviour. 1456 tests pass (+8 new). 0 lint errors. Type-check clean.

- [Stage 230] CV Builder v1 Phase 7 — stress test + device-testing additions (Stage 1 scope, no migration). New `scripts/stress-test-cv-builder.ts` covers the cumulative Stage 1 surface with 30 live-remote checks: schema sanity (13), CV.GENERATED back-fill + coalesce-on-OLD preservation + CV.HANDLE_REGENERATED rotation (6), PERMANENT.INVITED projection insert with status='pending' + captain-binding + message persistence (3), apply-after-invite chain (PERMANENT.APPLIED with `invited_from_id` flips invitation to 'applied', stamps responded_at, sets applications.invited_from_id) plus race-guard (a second PERMANENT.APPLIED pointing at the already-applied invitation is a no-op, proving the `AND status='pending'` clause is wired) (5), invitation-expiry cron path (31-day-old pending flips to expired; <30-day pending unchanged) (2), daywork-invitations UNIQUE constraint catches duplicate firing with 23505 (1). All 30/30 pass against live remote 2026-04-29. Random handles per run (avoids cross-run UNIQUE collisions on cv_handle). Superseded `scripts/stress-test-cv-builder-phase1.ts` deleted (its 19 checks are subsumed by the cumulative file). New §7b in `tasks/device-testing.md` covers Stage 1 device QA: settings toggles + locked Generate, profile hot-button locked toast, /cv/[handle] all three states + tombstone + stale banner + NDA mask + availability badge, signed-in employer hire-daywork + daywork-idempotency + hire-permanent flows, signed-in crew hat-switch CTA, apply-after-invite notification + deep link + Invited badge, manual cron smoke. 1448 unit tests still pass; no code changes outside scripts + docs.

- [Stage 229] CV Builder v1 Phase 6 — auth flow polish + invitation expiry cron (no migration). New `safeRedirectPath` helper in `lib/auth/safe-redirect-path.ts` validates the `?next=` param against an allowlist (must start with `/`, reject `//` protocol-relative, reject `..` traversal, reject `:` for protocol injection, ≤256 chars). Sign-up → email-confirm → /onboarding → final destination chain now preserves `?next=` end-to-end so a captain who scanned a CV signed-out and then signed up lands back on `/cv/{handle}` after onboarding. Onboarding page reads `?next=` from `window.location.search`, validates via the helper, routes there on completion (default `/profile`). Middleware extended to preserve the original path + query string as `?next=<path>` when bouncing auth-but-not-onboarded users to `/onboarding` (skipped for `/`, `/dashboard`, and the onboarding routes themselves). Signup page polling path threads `next` to `/onboarding?next=<encoded>` for non-referee users. `/cv/[handle]` sign-up CTAs migrated from the ad-hoc `?redirect=` to the canonical `?next=`. Hat-switch CTA on the `/cv/[handle]` crew-hat banner: dual-hat crew (the most common QR scanner who already has a DockWalker login) can flip to their employer hat in one click — calls existing `POST /api/hat` with `{ hat: 'employer' }`, reloads the page so the JWT claim re-reads. Hidden for agents (they can't switch hats). New `GET /api/cron/invitation-expiry` (Vercel cron, daily 03:00 UTC) flips `permanent_invitations` rows from `pending` to `expired` once they hit the 30-day threshold (spec v2.1). Direct UPDATE rather than an event because nothing in the domain reacts to expiry — the apply-after-invite server-side validation already drops mismatched/non-pending invitations silently. Idempotent (WHERE-clause filter on status). Powered by `idx_permanent_invitations_pending_expiry` partial index from migration 00131. CRON_SECRET bearer auth matches the rest of the cron suite. 1448 tests pass (+15 new: 9 safe-redirect-path, 6 invitation-expiry cron).

- [Stage 228] CV Builder v1 Phase 5b — hire-from-QR UI (no migration). Wires the Phase 5a backend to user-facing surfaces. `/cv/[handle]` action-bar buttons un-disabled — Hire daywork → `/daywork/post?invite=<personId>`, Hire permanent → `/daywork/post?invite=<personId>&type=permanent` (Contact-reference button stays inert pending the existing reference-contact API wiring). The `/daywork/post` page extended to recognise `?type=` (skip the type selector when the URL specifies daywork or permanent). Both posting forms thread `inviteCrewPersonId` to the API on submit (daywork: passes through to `POST /api/daywork`; permanent: after the posting is created, fires `POST /api/permanent/[id]/invite`). Each form renders a QR-hire banner at the top when `?invite=` is present so the captain knows they're hiring a specific scanned crew member, not posting a public job. New `/permanent/[id]/apply` page renders the apply-after-invite UX: posting summary, optional invitation banner ("Captain James invited you to apply for {role} on {vessel}." plus the captain's optional message), 250-char message textarea (no pre-fill — spec v2.1 explicitly avoids putting words in the crew's mouth). Applying threads `fromInvitationId` to `POST /api/permanent/[id]/apply` which the route validated server-side in Phase 5a. New `GET /api/permanent/[id]` returns posting details + optional invitation context (validated: caller is invited crew, status='pending', posting matches). `resolveDeepLinkUrl` extended in `lib/push-notifications.ts` with the `permanent-apply` screen mapping that powers the PERMANENT.INVITED notification deep link. ✉ Invited badge added to permanent review-queue applicant cards (`Applicant.invited` boolean derived from `applications.invited_from_id IS NOT NULL` in `/api/permanent/[id]/review`). 1433 tests pass (+9 new: 6 GET /api/permanent/[id], 3 push deep-link).

- [Stage 227] CV Builder v1 Phase 5a — hire-from-QR backend (no migration). New `getQrHireLimit()` Upstash bucket: 5 hire actions/hour per employer person_id (NOT per IP — shared bucket between daywork and permanent invitations so a single captain can't fan-spam from both surfaces). `POST /api/daywork` extended with optional `inviteCrewPersonId`: when present the route fires DAYWORK.POSTED + DAYWORK.INVITED atomically via `appendEvents`, applies the 5/hr per-employer rate limit, validates the target crew is alive (identity_type='crew', not deactivated, not blocked) — and catches Postgres `23505` unique_violation on `daywork_invitations(daywork_id, crew_person_id)` to return 409 with the spec'd "You've already invited this crew to this posting." copy (v2.1 idempotency). Standard daywork POST path unchanged when `inviteCrewPersonId` is absent. New `POST /api/permanent/[id]/invite` for inviting a specific crew to an existing permanent posting: employer/agent hat, posting must be `status='active'` and owned by the caller, target crew validated, 5/hr limit shared with daywork QR-hire, fires PERMANENT.INVITED with `id, permanent_posting_id, crew_person_id, message?`, catches 23505 on `permanent_invitations(posting, crew)` → 409. Spec v2.1 deferred "select existing posting" wizard variant to v2 — Phase 5b's wizard always creates a fresh posting first then calls this route. `POST /api/permanent/[id]/apply` extended with optional `fromInvitationId` body field: route service-fetches the invitation row, verifies it exists, is `pending`, and is addressed to the caller on this posting; passes through as `invited_from_id` in the PERMANENT.APPLIED payload. Forgiving design — failed validation drops the link silently and the apply still proceeds (handles expired/revoked invitations gracefully between deep-link click and submit). Projection (00131) flips the linked invitation row to 'applied' inside the same transaction with race-guarded `AND status='pending'`. New `handlePermanentInvited` push-trigger handler in `lib/push-triggers/permanent-handlers.ts` fans out the notification with body "Captain X invited you to apply for {role} on {vessel}" (spec v2.1 content-pass) and deep-link data `{ screen: 'permanent-apply', permanentPostingId, fromInvitation }`. Wired into `event-router.ts`. 1424 tests pass (+25 new: 7 daywork QR-hire, 12 permanent invite, 6 apply-after-invite).

- [Stage 226] CV Builder v1 Phase 4 — QR-landing route + page (no migration). New `GET /api/cv/[handle]` returns the full CV-shaped payload keyed by `profiles.cv_handle` (8-char alphanumeric, validated): profile + canonical lookups (certs, nationalities, entry rights), experiences with period-correct vessel names via `resolveHistoricalVesselNames`, references filtered to `status='accepted' AND include_on_cv=true`, sea-time totals (only when `cv_include_sea_time=true`), and a `stale` flag (true when `profiles.updated_at > cv_generated_at + 30d`). NDA mask respects per-experience `cv_show_full_vessel`; references inherit the mask from their bound experience and default-mask when the FK was auto-nulled by EXPERIENCE.REMOVED. Tombstone state (200 with `{ tombstone: true }`) for deactivated_at OR blocked_at OR current_hat=null (post-PERSON.DATA_SCRUBBED). Two new Upstash limiters in `lib/rate-limit.ts`: `getCvHandleAnonLimit` (20/hour per IP) and `getCvHandleAuthLimit` (100/hour per auth user) — segmented by caller identity discovered via `createClient().auth.getUser()`. New public `/cv/[handle]` page renders three render states (signed-out teaser + sign-up CTA preserving `?redirect`; signed-in employer/agent full profile + inert hire-action bar — wizards land in Phase 5; signed-in crew hat-switch hint + full data fallback) plus tombstone, NotFound, stale-CV banner, and a permanent-availability badge per spec §5 (`immediate` → "Available now", `after_notice` → "Available in {N} days", `not_looking` → "Not currently looking — open to discuss"). Page registered as `/cv/` in `lib/supabase/middleware.ts` publicRoutes alongside `/ref/`. 1399 tests pass (+13 new route tests covering handle validation, rate-limit segmentation, tombstone for both deactivated and scrubbed, NDA mask on/off, references filter, sea-time totals, stale flag).

- [Stage 225] CV Builder v1 Phase 3 — UI (locked-entry MVP). New `/settings/cv` page (`apps/web/src/app/(app)/settings/cv/page.tsx`): Stage-1 Coming-Soon banner card per spec §12.1, locked Generate CV button (aria-disabled + Coming-Soon toast on click, deliberately does NOT call `/api/cv/generate`), three working toggle sections — sea time (profile-level), per-reference Include-on-CV (filtered to `status='accepted'` outbound references), NDA-vessel disclosure (filtered to experiences where `vessels.nda_flag = true`). Hat gate (v2.1): agent hat → `router.replace('/settings')` and render null; agent CVs are out of scope per spec §12. Profile page hot button card just below the avatar block — locked with Coming-Soon toast, gated to crew hat AND crew identity_type so agents who switch hats don't see it. New "CV Builder" entry in `account-section.tsx` so users can navigate to the page from the main `/settings` index. New PATCH `/api/cv/settings` route with polymorphic body (cvIncludeSeaTime / referenceId+includeOnCv / experienceId+cvShowFullVessel) — justified as direct UPDATE (not event-sourced) because these are per-row UI display preferences with no projection cascade or temporal semantics; route is owner-scoped (UPDATE WHERE person_id = caller for profiles + crew_experiences, requester_person_id + status='accepted' for references) and crew-hat-only. GET endpoints extended: `/api/profile` adds `cv_handle, cv_handle_updated_at, cv_include_sea_time, cv_generated_at`; `/api/references/mine` REF_COLUMNS adds `include_on_cv`; `/api/experiences` SELECT adds `cv_show_full_vessel`. 1386 tests pass (+15 new: 9 PATCH endpoint, 6 page).

- [Stage 224] CV Builder v1 Phase 2 — Stage-1 plumbing (no migration). Two new routes that lock the URL surface for the Stage 1 → Stage 2 transition: `POST /api/cv/generate` (crew-hat gated, returns 503 with a "DockWalker CV — Coming Soon" payload pointing users at Settings → CV Builder; Phase 8 swaps the body for the real PDF return without changing the contract) and `POST /api/admin/cv/mint-handle/[personId]` (requireAdmin, 404 if profile missing, 409 if `cv_handle` already exists, otherwise mints via the shared helper and fires `CV.HANDLE_REGENERATED` with `old_handle=null` for ledger audit; returns 201 with the new handle). The admin route is Stage-1 QA infrastructure — natural user flows don't mint handles until Stage 2's PDF generator runs, so internal QA + the Phase 7 stress test seed handles via this route; stays as an operational rescue tool post-Stage-2. `qrcode` npm package installed (used by the Phase 8 PDF QR section, but staged in early to keep the lock file stable). `@dockwalker/types` extended with `PERMANENT.INVITED`, `CV.GENERATED`, `CV.HANDLE_REGENERATED` event payloads + `permanent_invitation` aggregate type + optional `invited_from_id` on `PERMANENT.APPLIED`. 1371 tests pass (+9 new: 3 cv-generate stub, 6 admin mint-handle).

- [Stage 223] CV Builder v1 Phase 1 — schema + projection extensions for the CV Builder feature (Stage 1 of the staged rollout; Phase 8 ships the @react-pdf renderer). Migration 00131 adds CV-specific columns to `profiles` (`cv_handle text unique`, `cv_handle_updated_at`, `cv_include_sea_time`, `cv_generated_at`) with a partial index on cv_handle, `cv_show_full_vessel boolean default true` to `crew_experiences` (per-experience NDA disclosure toggle), `include_on_cv boolean default false` to `references` (per-reference inclusion default-OFF), and `applications.invited_from_id uuid references public.permanent_invitations(id) on delete set null` plus a partial index. Creates `permanent_invitations` table mirroring `daywork_invitations` (status CHECK with `pending|applied|declined|revoked|expired`, UNIQUE(posting, crew), indexes incl. partial `where status='pending'` for the daily expiry cron, RLS policy: invited_by OR crew OR posting employer can SELECT). Extends `events_aggregate_type_check` allow-list with `permanent_invitation`. `apply_projection` re-created from the 00130 body with 4 changes: (1) PERMANENT.APPLIED extended with optional `invited_from_id` branch — when payload has it, persist on the new applications column AND flip the linked permanent_invitations row to `applied` with `status='pending'` race guard, no-op when null; (2) PERMANENT.INVITED handler appended (INSERT into permanent_invitations); (3) CV.GENERATED handler — stamps cv_generated_at and lazy-mints cv_handle from payload via `coalesce(cv_handle, payload.handle)`, deliberately does NOT bump profiles.updated_at so the staleness signal is preserved; (4) CV.HANDLE_REGENERATED handler — rotates cv_handle and bumps cv_handle_updated_at. Handler count 82→85. New shared mint helper `apps/web/src/lib/cv/mint-handle.ts` (8-char alphanumeric, retry up to 5× on Postgres unique_violation 23505, throws `MintHandleError` on exhaustion) for use by Phase 2 admin route + Phase 8 generate/regenerate routes. Self-contained rollback drops columns + table CASCADE + restores prior aggregate_type CHECK; NOTICE points operators at re-applying 00130 to revert apply_projection. Stress test `scripts/stress-test-cv-builder-phase1.ts` 19/19 pass against live remote (schema columns exist, CV.GENERATED back-fills handle on first call + preserves on second, CV.HANDLE_REGENERATED rotates correctly). 1362 tests pass (+8 new mint-handle unit tests). Schema v130 → v131.

- [Stage 220] Admin Phase 1 (chunks 3-4) — Chunk 3 messages RLS tightening skipped because no permissive messages INSERT policy exists in the codebase (all writes flow through `apply_projection` as SECURITY DEFINER) and `POST /api/messages/:id` already rejects on non-active engagements at the API layer. Chunk 4 ships two new admin routes: `POST /api/admin/engagements/:id/cancel` (emits `ADMIN.ENGAGEMENT_CANCELLED` with posting_type routing) and `POST /api/admin/postings/:id/hide` (emits `ADMIN.POSTING_HIDDEN` for daywork or permanent). Both handlers already exist in `apply_projection` from migration 00097. Phase 1 complete. 1111 tests pass (+14 new).

- [Stage 219] Admin Phase 1 (chunks 1-2) — API-layer blocked enforcement + `user_notes`. Added `blocked_at` check to `requireDomainUser` across all 3 paths (header, JWT, DB), new `{ allowBlocked: true }` option for support routes, middleware propagates `x-blocked` header from the JWT claim. Migration 00106 creates `user_notes` (service-role only), extends `PERSON.DATA_SCRUBBED` to rewrite note content about the scrubbed subject, extends `admin_delete_person` RPC. New routes `/api/admin/users/:id/notes` (GET, POST) + `/notes/:noteId` (PATCH, author-only). Admin user detail page wires Restore action and a Notes section (list + add + edit-own). 1097 tests pass (+15 new).

- [Stage 218] Admin Phase 0 — JWT hook extended with `is_admin` and `blocked` claims (migration 00105). Middleware now reads both claims on the fast path for the `/admin/*` guard and the blocked-user redirect, with DB fallback for pre-hook sessions (~1 hour propagation window). `requireAdmin` (§0.1) and middleware admin/blocked redirects (§0.3) were already in place; §0.2 was the only remaining gap. API-layer gates unchanged — middleware is UX only. 1082 tests still pass.

- [Stage 217b] CI fix for migration 00104 — initial CI run failed on `supabase db reset` with `ports_city_id_fkey` FK violation because seed files run AFTER all migrations, so the curated city UUIDs that 00104's port INSERTs referenced didn't exist yet on a fresh DB (prod was fine because `supabase db push` applies migrations to an already-seeded DB). Fixed by (1) adding idempotent UPSERT preamble to 00104 for the 7 legacy regions + 30 curated cities + 55 curated ports, (2) removing the regions/cities/ports block from `supabase/seed/001_canonical_data.sql` — migration 00104 is now the single source of truth for location canonical data, (3) patching Stage 4 generator so re-runs emit the preamble automatically. Rollback unchanged. 1082 tests still pass.

- [Stage 217] Locations V1 marina import (Phase C + D complete) — OSM Stage 1 extracted 31,233 raw marinas via 30 regional Overpass queries. Stage 2 enriched via Nominatim reverse-geocoding (10+ hours wallclock, 1 req/sec rate limit) yielding 18,772 records. Stage 3 deduped to 18,163. Stage 3b filter (new) dropped inland-heavy countries (Germany, Netherlands interior, Scandinavia, landlocked nations) and applied a name-stem heuristic → 5,957 superyacht-relevant marinas. Stage 3c normalize (new) collapsed OSM district names onto DockWalker hub cities (Deira/Al Seef → Dubai, Al Bateen/Yas Island → Abu Dhabi, Philipsburg → St. Maarten, Charlotte Amalie → St. Thomas USVI, Juan-les-Pins → Antibes, Palma de Mallorca → Palma, Turgutreis/Yalıkavak → Bodrum, İstanbul → Istanbul) and fixed NL|Sint Maarten → SX|Sint Maarten country-code. Stage 4 generated migration 00104 applied to live Supabase: 7 launch-region UUIDs deleted (renamed to `_legacy_*` first to avoid unique-name collision), 63 new regions via UUIDv5 from country_code, 29 curated cities preserved with region_id re-parented, 67 curated ports preserved + 4 enriched with OSM lat/lon, 3,336 new cities inserted, 5,933 new ports inserted. Live totals: 63 regions / 3,365 cities / 6,004 ports. Dubai city now holds 11 marinas (5 curated + 6 OSM). 1082 tests still pass.

- [Stage 216] Locations V1 Phase B/E/F + Entry Rights V1 — three migrations (00101/00102/00103) applied to live Supabase. Phase B prep adds `country_code` to regions + `latitude/longitude/osm_type/osm_id/website/phone/capacity/vhf` to ports with supporting indexes. Phase E (fuzzy-search infra): `pg_trgm` + `unaccent` extensions, `immutable_unaccent` wrapper, trigram GIN indexes on regions/cities/ports, `search_locations` / `get_locations_by_ids` / `top_locations` RPCs, three new API routes (`/api/locations/search`, `/top`, `/by-ids`), `LocationPicker` rewritten to lazy-load via the endpoints, new `CitiesPicker` component replacing `citiesToGroups → HierarchicalPills` for agent placement cities, `useLookups` drops `ports` + `cities`. Phase F: admin canonical CRUD gains `country_code` on regions + optional OSM/contact fields on ports. `scripts/marina-extraction/` pipeline (4 Python stages + README + requirements) staged; Stage 1 produced 31,233 raw marinas. Entry Rights V1 migration 00103 renames `visa_types` → `entry_rights`, adds `category`, preserves 5 UUIDs (renamed to V1 names), deletes 5 work-permit/Other rows, inserts 19 new canonical rows, renames `profiles.visa_ids` → `entry_right_ids`, `apply_projection` PROFILE.CREATED/UPDATED coalesce entry_right_ids/visa_ids with live-entry-rights filter, new `EntryRightPicker` component. `useLookups` swaps `visaTypes` → `entryRights` (cache key bumped to `dw-lookups-v3`). 1082 tests pass (+10 new location API tests). Marina-data import migration (00104) follows once Python Stages 2/3/4 finish externally.

- [Stage 215] Qualifications V1 — canonical certification list expanded from 17 → 265 (migration 00100 adds `subcategory text null` + index, renames/remaps all 17 preserved UUIDs to the new taxonomy, inserts 248 new rows across 8 categories: basic/deck_bridge/engineering/interior/galley/watersports/helideck/other). Shared `groupCertsByCategoryAndSubcategory` + `cert-labels.ts` added to `@dockwalker/shared`. New `CertificationPicker` component (fuzzy search on top, Basic expanded by default, subcategory drill-down for deck_bridge/engineering/interior, selected pills above with × remove) replaces the old `HierarchicalPills + certsToGroups` pattern on 4 surfaces: onboarding profile step, profile edit form, daywork post page, permanent post form. Admin canonical page gains category select + subcategory input for cert CRUD; POST/PATCH payload now sends `{ name, category, subcategory }` for certifications. `CertLookup` type extended in `use-lookups.tsx` with `subcategory` + `sort_order`; all three cert queries (`use-lookups` load + revalidate, onboarding page) updated to select the new columns. Seed `001_canonical_data.sql` rewritten to match the V1 list so reseeds stay in sync. `certsToGroups` kept for backwards compatibility but no longer used. Discover filter (single-select cert dropdown) left as-is — multi-select picker semantics diverge from the single-select filter design; deferred to V1.1. 10 new grouping/label unit tests + 10 new CertificationPicker component tests; existing form-dropdowns tests updated to new mock schema. Rollback self-contained (deletes 248 new UUIDs, restores 17 preserved rows to pre-migration names/categories/sort_orders, drops `subcategory` + index). 1072 tests pass.

- [Stage 214] Legal pages + permanent rating path — (A) `/privacy` and `/terms` static pages built from `tasks/privacy-policy-spec.md` and `tasks/founder-drafts.md` §1; `apps/web/src/lib/legal-placeholders.ts` centralises company/address/jurisdiction/email/region placeholders pending lawyer review and founder fill-in; both paths added to `publicRoutes` in the auth middleware; landing footer already links them; settings page gains a Legal section; signup form gains a "By creating an account you agree to..." consent line; login page gains a small Terms · Privacy footer. (B) Permanent withdrawal rating path — `PERMANENT.WITHDRAWN` and in-negotiation `PERMANENT.CANCELLED_BY_EMPLOYER` close the engagement with status `closed` + outcome `withdrew` / `not_successful`; `canRate` in messages page extended to treat those closures as ratable, `RatingFormOverlay` invocation treats them as `isCancelled` (light cancellation form), new `ClosedBanner` in `banners.tsx` renders outcome-aware copy plus the rating CTA via `onOpenRating`, rate API route removes its daywork-only filter and accepts `closed` with those two outcomes routing to `CANCELLATION_RATED_*` events (`successful_placement` closures remain non-ratable); 4 new tests in `engagement-rate.test.ts` cover the new branch. DateInput "may block interaction" bug retired — already fixed by Stage 201 commit e0a7557 (calendar icon button with `showPicker()` + `focus().click()` fallback). 1052 tests pass.
- [Stage 213] Landing + login polish — login page rebuilt around overlap-logo card pattern (112px logo breaks the top edge of the card with shadow + ring), "Welcome back" title + supporting copy, forgot-password link inlined beside password label, larger CTA, lifted card shadow. Homepage hero enlarged: logo 112→144px mobile + 160→208px desktop with shadow + ring, H1 28→36/52px with tighter tracking, tagline bumped to 20/24px medium, body copy rewritten with verb-led "Swipe / Apply / Connect" structure and fairness reinforcement, primary CTA renamed "Sign up" → "Get started", subtle radial accent glow behind hero on desktop. 1048 tests pass.
- [Stage 212] API query parallelisation — daywork discover enrichment (vessel + poster + certs → Promise.all), permanent discover enrichment (same), daywork applicants enrichment (availability + engagements + shore → Promise.all), permanent discover size-band filter moved from post-fetch JS to DB-level WHERE clause (matching daywork pattern). Estimated 150-300ms savings per request on discover and employer review pages. 1048 tests pass.
- [Stage 211] Shore-based experience coverage completion — category pills added to the two employer surfaces Stage 210 missed: `GET /api/daywork/[id]/available-crew` (new shore subquery + `shore_experience_categories` on `AvailableCrew` type + pill row in `AvailableCrewCard`) and `GET /api/permanent/[id]/review` (shore subquery + inline `Applicant` type extended + pill row in permanent review card). 9 new component tests for `ProfileShoreExperienceSection` (empty state, entry rendering, auto-expand first entry, onEdit/setConfirmDeleteId callbacks, current-role "Present" label, entry count in collapsed header, toggleSection firing, uncategorized fallback). `available-crew.test.ts` + `permanent-review.test.ts` mocks extended for new query order. 1048 tests pass (+9 new).
- [Stage 210] Shore-based experience — 30 industry categories (Hospitality, Military, Fitness, etc.), `shore_experience_categories` + `shore_experiences` tables (migration 00099), event-sourced CRUD (SHORE_EXPERIENCE.ADDED/UPDATED/REMOVED), GDPR cleanup, categories API, view-only profile API returns shore experiences, `ProfileShoreExperienceSection` component, category pills in profile summary + applicant cards + profile overlay, add/edit forms with searchable category picker, onboarding shore_experience textarea replaced with hint. 1039 tests pass (+10 new).
- [Stage 209] Docky accuracy fixes — MCA corpus expanded from 16 → 150 PDFs (756 → 6,445 chunks via `text-embedding-3-small`); manual dedup pass removed 6 files (4 exact filename dupes for MSN 1863/1904/1846 + MIN 511, MSN 1886 A2 superseded by A3, MSN 1877 base superseded by Amendment 2); 130 files renamed to a clean `TYPE-ID[-Version][-Topic].pdf` convention across MSN/MIN/MGN/EXAM/RO/GOV prefixes; `deriveDocumentName` in `scripts/ingest-mca-docs.ts` fixed to replace underscores as well as hyphens and drop the buggy `^GOV ` re-prefix step that broke `source-urls.json` matching; new `scripts/wipe-mca-chunks.ts` utility for full-table re-ingest. `BASE_SYSTEM_PROMPT` in `apps/web/src/lib/advisor/llm.ts` hardened: explicit "<mca_documentation> is your only source of regulatory facts" framing, 4 numbered grounding rules (direct-quote requirement, no invented section numbers, exact source citation, no from-memory claims), regulatory vs general-career-advice split with an exact refusal phrase when excerpts are silent, cross-turn consistency rule, anti-apology rule, tone subordinated to factual accuracy. `buildSystemBlock` now wraps chunks in `<mca_documentation>` tags with an explicit empty-case notice so the model always sees the boundary. Production `mca_document_chunks` wiped + re-ingested (0 failures, smoke test passed at 0.703 similarity, 6,445 rows). 1019 tests pass.
- [Stage 208] Password visibility toggle — shared `PasswordInput` primitive in `components/ui/password-input.tsx` wrapping `Input` with Eye/EyeOff lucide icons, absolute-positioned toggle button, `aria-pressed` + `aria-label` for a11y. Adopted in login, signup, reset-password (both new + confirm), and settings change-password form (current + new + confirm) — 8 password fields total. 1019 tests pass.
- [Stage 207] Reset-to-reactivate flow — `PERSON.REACTIVATED` event + apply_projection handler (migration 00096), new `POST /api/auth/reactivate` route called by reset-password page after `updateUser` succeeds, clears `persons.deactivated_at` and lifts the auth ban via service client. Login route improved: "banned" message now guides users to reset their password to restore the account; defensive `deactivated_at` check after sign-in catches zombie state. Reset-password page shows "Welcome back" copy when reactivation occurs. 1019 tests pass.
- [Stage 206] Critical auth fixes — password reset (PKCE + token_hash fallback, onAuthStateChange), account deletion hardening (ban failure = hard error, middleware onboarding deactivation check, banned user login message), admin_delete_person RPC for FK-safe user cleanup
- [Stage 205] Permanent engagement fixes + UX batch — (1) Migration 00093: permanent_postings RLS allows engaged crew/employer to read posting data regardless of status; (2) Messages page: permanent conversation support (interface, job context rendering with epaulettes, 'closed' status in history tab, date fix, replacement link guard, Closed badge); (3) Chat footer: 'closed' status disables input + shows banner; (4) Auth 401 redirect: safeFetch redirects to /auth/login on 401 instead of showing empty pages, status code exposed on errors, SWR no-retry on session expired; (5) Onboarding copy updated for permanent positions; (6) EpauletteBadge added to permanent mine cards + permanent chat summary card; (7) Role picker: per-department "select the role" prompt; (8) Nav: "Discover" renamed to "Opportunities"; (9) Working day calendar picker replaces number input on daywork post form (sends workingDayDates array, backend already supports it); 991 tests pass

## Current Schema Version

v136 — Engagement phase projection (B-011 commit 2). 00136 re-creates `apply_projection` with the phase-aware logic that 00135's schema column needs. One new handler — `PERMANENT.SHORTLIST_CHAT_OPENED` — inserts `active_engagements` rows with `phase='shortlist'` after race-guarding posting status='active' AND application status='shortlisted', idempotent via `ON CONFLICT (application_id) DO NOTHING`. Modified `PERMANENT.SELECTED` cascade: promotes existing shortlist engagement to `phase='active'` if found OR inserts new active engagement, then loops over OTHER `phase='shortlist'` engagements on the same posting, inserts a warm system message ("This role has now been filled. Thanks for your interest — wishing you the right vessel soon."), and bulk-closes them to `phase='closed' outcome='role_filled'`. `PERMANENT.WITHDRAWN`/`REJECTED`/`CANCELLED_BY_EMPLOYER` extended to also close the relevant shortlist chat with role-appropriate system messages. `PERMANENT.SELECTION_REVERTED` now sets reverted application back to `'rejected'` (was `'not_selected'` — wrong because the employer made an explicit choice). Phase guards (`AND phase='active'`) added to 13 engagement events (POSTPONEMENT_PROPOSED/ACCEPTED/REJECTED, WORK_STARTED/WORK_STARTED_CONFIRMED, COMPLETION_CONFIRMED/DISPUTED, RATED_BY_CREW/EMPLOYER, CANCELLATION_RATED_BY_CREW/EMPLOYER, CHECKLIST.SET/ITEM_TOGGLED) so shortlist chats can't accidentally trigger work-started flows. Handler count 85 → 86. Self-contained rollback restores 00132's exact apply_projection body verbatim.

v135 — Engagement phase column (B-011 schema prep). 00135 adds `phase text not null default 'active'` to `active_engagements` with a CHECK enum (`'shortlist' | 'active' | 'completed' | 'closed'`) and a partial index on `(employer_person_id, phase) where phase='shortlist'` for the inbox-grouping query. Default `'active'` preserves all 19000+ existing rows without a data rewrite (Postgres 11+ stores constant defaults in column metadata, no scan). Schema-only — projection updates land in 00136. Reversible rollback drops the column + index. `'shortlist'` phase is for the new permanent shortlist-chat feature: employer-initiated, opt-in conversations that live alongside accepted/selected engagements but with most state-machine actions (work-started, postponement, completion, ratings, checklist) blocked. Once a candidate is selected, sibling shortlist chats auto-close to `phase='closed'`. Daywork is unchanged — too low-stakes for parallel chats.

v134 — Templates partial-save (B-005). 00134 drops NOT NULL on 11 partial-fields of `permanent_templates` (`vessel_id`, `role_id`, `port_id`, `start_date`, `salary_min`, `salary_max`, `salary_currency`, `salary_period`, `live_aboard`, `required_certification_ids`, `shortlist_cap`) so employers can save a minimal "Deckhand €2500" template without DB constraint violations; `template_name` and `employer_person_id` stay NOT NULL as irreducible identity. CHECK constraints on `salary_currency`/`salary_period` survive (Postgres CHECK treats NULL as unknown). Adds the missing owner UPDATE RLS policy on `daywork_templates` so a future PATCH route can edit rows — `daywork_templates` had SELECT/INSERT/DELETE since 00006 but no UPDATE, blocking edit support entirely. Brings daywork to parity with `permanent_templates` which has had UPDATE policy since 00059. Rollback DELETEs any partial-saved rows then re-asserts NOT NULL (inherent cost of reverting the feature, documented in the down file). No projection changes; handler count unchanged at 85. Lands ahead of B-005's API + UI consumers (`tasks/todo.md`) so the schema change can soak in CI on its own.

v133 — Admin action log. 00133 adds `admin_action_log` CRUD utility table (8-value action CHECK, FKs to `persons`, jsonb metadata, 3 indexes by admin/target/action+time). Service-role-only at RLS layer (no authenticated policies); admin reads via `/admin/*` routes using service client. Documented exception to event-sourcing per CLAUDE.md "Documented exceptions" pattern (matches `daywork_templates` + `permanent_templates`). Populated by the four admin routes (engagement cancel, posting hide, user block/unblock/delete/restore, report resolve, thread close) immediately after `appendEvent` succeeds — gives ops a queryable, indexed surface for "who did what when" without JSON-extraction scans on the events table.

v132 — Pre-launch audit P1 fixes. 00132 combines five P1 audit findings: PERMANENT.INVITED state guard (P1-D1), PERMANENT.SHORTLISTED projection tightened to status='applied' only (P1-D3), permanent_invitations FK changed cascade → restrict (P1-D4), NDA reveal RPCs widened to include employer-side IMO + ('active','completed','closed') status filter (P1-M1, P1-M2), index on persons(deactivated_at) for the S-2 data-scrub cron (P1-P4). Self-contained rollback restores 00131's apply_projection body + 00122's RPC bodies + cascade FK + drops the index. Handler count unchanged at 85.

v131 — CV Builder v1 Phase 1. 00131 adds CV-specific schema (profiles cv_handle/cv_handle_updated_at/cv_include_sea_time/cv_generated_at, crew_experiences.cv_show_full_vessel, references.include_on_cv, applications.invited_from_id), creates `permanent_invitations` table (status CHECK pending/applied/declined/revoked/expired, UNIQUE(posting, crew), RLS), extends events.aggregate_type with `permanent_invitation`, and re-creates `apply_projection` with 3 new handlers (PERMANENT.INVITED, CV.GENERATED, CV.HANDLE_REGENERATED) plus PERMANENT.APPLIED extended with the optional invited_from_id branch (race-guarded `AND status='pending'` flip on the linked invitation row). Handler count 82 → 85. (131 migrations applied. Stage 1 of the staged rollout — entry points stay greyed out until Phase 8 ships the @react-pdf renderer.)

## Migrations Applied

| Migration                                    | Description                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `00001_events_table.sql`                     | Append-only event ledger                                                                                                                                                                                                                                                                                                                                                                                    |
| `00002_canonical_lookups.sql`                | Roles, certs, experience brackets, locations                                                                                                                                                                                                                                                                                                                                                                |
| `00003_projection_tables.sql`                | Materialized projections (persons, profiles, vessels, daywork, applications, availability, messages)                                                                                                                                                                                                                                                                                                        |
| `00004_rls_policies.sql`                     | Row Level Security on all tables                                                                                                                                                                                                                                                                                                                                                                            |
| `00005_append_event.sql`                     | `append_event` RPC + projection triggers                                                                                                                                                                                                                                                                                                                                                                    |
| `00006_daywork_templates.sql`                | CRUD templates table (intentional non-ledger exception)                                                                                                                                                                                                                                                                                                                                                     |
| `00007_projection_consistency.sql`           | `onboard_person`, `check_no_overlap`, `clear_availability_dates`, `get_vessel_public` RPCs                                                                                                                                                                                                                                                                                                                  |
| `00008_application_profile_fk.sql`           | FKs from `applications.crew_person_id` and `active_engagements.{crew,employer}_person_id` to `profiles.person_id` for PostgREST joins                                                                                                                                                                                                                                                                       |
| `00009_currency_and_required_day_rate.sql`   | Add `currency` to dayworks/templates, make `day_rate` NOT NULL, update `apply_projection`                                                                                                                                                                                                                                                                                                                   |
| `00010_shortlist_status.sql`                 | Add `DAYWORK.SHORTLISTED` event handling to `apply_projection`, include `shortlisted` in supersede and withdraw logic                                                                                                                                                                                                                                                                                       |
| `00011_in_progress_status.sql`               | Add `in_progress` to dayworks status CHECK, set daywork to `in_progress` on acceptance, auto-reject remaining applicants                                                                                                                                                                                                                                                                                    |
| `00012_crew_completion_confirmation.sql`     | Add `crew_completion_status` to `active_engagements`, handle `ENGAGEMENT.COMPLETION_CONFIRMED` and `ENGAGEMENT.COMPLETION_DISPUTED` events                                                                                                                                                                                                                                                                  |
| `00013_engagement_ratings.sql`               | `engagement_ratings` table with crew/employer-specific fields, `ENGAGEMENT.RATED_BY_CREW` and `ENGAGEMENT.RATED_BY_EMPLOYER` event handlers                                                                                                                                                                                                                                                                 |
| `00014_fix_apply_projection_signature.sql`   | Drops orphaned 0-arg `apply_projection()`, updates the 6-arg version (called by `append_event`) with all logic from migrations 00010-00013                                                                                                                                                                                                                                                                  |
| `00015_fix_hat_and_profile_projection.sql`   | Fixes `PERSON.HAT_CHANGED` (`new_hat` → `current_hat`) and restores `agency_name` + `role_specialization_ids` in `PROFILE.UPDATED`                                                                                                                                                                                                                                                                          |
| `00016_remove_message_hiding.sql`            | Drops `hidden_by` column from `messages`, removes `MESSAGE.HIDDEN` from `apply_projection`, recreates RLS policy without hidden_by filter                                                                                                                                                                                                                                                                   |
| `00017_job_number.sql`                       | Adds `job_number SERIAL UNIQUE` to `dayworks` for human-readable job references (DW-00001), backfills existing rows by created_at order                                                                                                                                                                                                                                                                     |
| `00018_structured_employer_cancellation.sql` | Structured cancellation reasons, postponement flow, system messages, cancellation ratings, `check_no_overlap_excluding` RPC, `DAYWORK.RELISTED` event                                                                                                                                                                                                                                                       |
| `00019_postponement_relist_dates.sql`        | `DAYWORK.RELISTED` handler now updates dates/working_days when present in payload (enables relist-with-dates after crew rejection)                                                                                                                                                                                                                                                                          |
| `00020_work_started_confirmation.sql`        | Adds `work_started_status` and `work_started_at` to `active_engagements`, handles `ENGAGEMENT.WORK_STARTED` and `ENGAGEMENT.WORK_STARTED_CONFIRMED` events                                                                                                                                                                                                                                                  |
| `00021_append_events_batch.sql`              | `append_events_batch(jsonb)` RPC for atomic multi-event writes — processes array of events in single transaction                                                                                                                                                                                                                                                                                            |
| `00022_structured_crew_cancellation.sql`     | Structured crew cancellation: `cancelled_by` column, crew reason categories, updated `apply_projection` to write reason fields on crew cancel, expanded CHECK constraint                                                                                                                                                                                                                                    |
| `00023_pre_arrival_checklist.sql`            | Pre-arrival checklist: `engagement_checklists` table (JSONB items, text[] acknowledged_item_ids), `checklist` aggregate type, `CHECKLIST.SET` and `CHECKLIST.ITEM_TOGGLED` handlers in `apply_projection`                                                                                                                                                                                                   |
| `00024_availability_city.sql`                | Adds `city_id` (FK to cities) and `not_available` (boolean) to `availability_windows`; updates `apply_projection` AVAILABILITY.SET handler with city_id, not-available marker row logic, and mutual exclusion between available/not-available states                                                                                                                                                        |
| `00025_availability_port_id.sql`             | Adds `port_id` (FK to ports) to `availability_windows`; updates `apply_projection` AVAILABILITY.SET handler to write port_id on both normal and not-available paths                                                                                                                                                                                                                                         |
| `00026_user_preferences.sql`                 | `user_preferences` table (person_id PK, profile_visible boolean) with owner-only RLS. CRUD utility data, not event-sourced.                                                                                                                                                                                                                                                                                 |
| `00027_vessel_loa.sql`                       | Adds `loa_meters NUMERIC` to `vessels`; updates `get_vessel_public` to return `loa_meters`; updates `apply_projection` to write `loa_meters` on `VESSEL.CREATED` and `VESSEL.UPDATED`                                                                                                                                                                                                                       |
| `00028_crew_experiences.sql`                 | `crew_experiences` table, `flag_states` lookup (39 entries), green crew profile columns, per-registrant IMO uniqueness, `apply_projection` handlers for `EXPERIENCE.ADDED/UPDATED/REMOVED`                                                                                                                                                                                                                  |
| `00029_experience_enhancements.sql`          | Renames `vessel_type` → `vessel_operation` + new `vessel_type` (motor\|sail) on vessels; renames `charter_or_private` → `vessel_operation`, `rotation_type` → `contract_type`, `rotation_details` → `contract_details` on crew_experiences; updates `get_vessel_public` and `apply_projection`                                                                                                              |
| `00030_daywork_invitations.sql`              | `daywork_invitations` table with RLS + indexes; `apply_projection` handlers for `DAYWORK.INVITED`, `DAYWORK.INVITATION_ACCEPTED`, `DAYWORK.INVITATION_DECLINED`; revocation logic on `DAYWORK.ACCEPTED`, `DAYWORK.CANCELLED_BY_EMPLOYER`, `DAYWORK.RELISTED`; auto-accept on `DAYWORK.APPLIED`                                                                                                              |
| `00031_experience_auto_derivation.sql`       | `derive_experience_profile(person_id)` function auto-computes `experience_bracket_id` + `vessel_size_exposure_ids` on profile from crew_experiences; called after `EXPERIENCE.ADDED/UPDATED/REMOVED` in `apply_projection`                                                                                                                                                                                  |
| `00032_nda_reveal_after_acceptance.sql`      | Updates `get_vessel_public` to reveal IMO when caller has an active engagement on a daywork linked to the vessel; NDA immutability enforced at API layer                                                                                                                                                                                                                                                    |
| `00033_device_tokens.sql`                    | `device_tokens` table with RLS, unique `(person_id, token)`, platform enum (apns/fcm/web). CRUD utility data, not event-sourced.                                                                                                                                                                                                                                                                            |
| `00034_vessel_soft_data_separation.sql`      | Drops `vessel_operation` from vessels, drops `vessel_id` from daywork_templates, updates `apply_projection` and `get_vessel_public` to remove vessel_operation                                                                                                                                                                                                                                              |
| `00035_working_day_dates.sql`                | Adds `working_day_dates date[]` nullable column to dayworks and templates; post-projection trigger `apply_working_day_dates` writes dates from DAYWORK.POSTED payload                                                                                                                                                                                                                                       |
| `00036_daywork_extended.sql`                 | `DAYWORK.EXTENDED` event handler via trigger: updates end_date, working_days, working_day_dates on dayworks row                                                                                                                                                                                                                                                                                             |
| `00037_missing_aggregate_types.sql`          | Adds `invitation` and `experience` to `events_aggregate_type_check` CHECK constraint (both were missing since Stages 46/53)                                                                                                                                                                                                                                                                                 |
| `00038_multi_crew_positions.sql`             | Multi-crew positions: `positions_available`/`positions_filled` columns, full `apply_projection` rewrite with atomic fill logic, `DAYWORK.POSITIONS_UPDATED` handler, cascade cancel of active engagements, no auto-revert on cancellation                                                                                                                                                                   |
| `00039_profile_avatar.sql`                   | Profile avatar: `avatar_url` column on profiles, `apply_projection` updated for PROFILE.CREATED/UPDATED with avatar_url, Supabase Storage `avatars` bucket with RLS (public read, owner write)                                                                                                                                                                                                              |
| `00040_notifications_and_read_cursors.sql`   | `message_read_cursors` table (person_id + engagement_id PK), `notifications` table with type/title/body/deep_link/read, RLS on both, index for unread queries                                                                                                                                                                                                                                               |
| `00041_permanent_opportunity.sql`            | `permanent_opportunity` boolean on dayworks/templates, `permanent_opportunity_accuracy` on engagement_ratings, updated `apply_projection` DAYWORK.POSTED and ENGAGEMENT.RATED_BY_CREW handlers                                                                                                                                                                                                              |
| `00042_subscriptions.sql`                    | `subscriptions` table (person_id UNIQUE, stripe_customer_id UNIQUE, plan CHECK, status CHECK), RLS owner read-only. Not event-sourced — Stripe-owned state.                                                                                                                                                                                                                                                 |
| `00043_pgvector_and_mca_chunks.sql`          | pgvector extension, `mca_document_chunks` table with HNSW index, `match_mca_documents` RPC for cosine similarity search, RLS read-only for authenticated users                                                                                                                                                                                                                                              |
| `00044_advisor_conversations.sql`            | `advisor_conversations` + `advisor_messages` tables, cascade delete, indexes on person+updated_at and conversation+created_at, owner-only RLS. Not event-sourced — AI chat utility data.                                                                                                                                                                                                                    |
| `00045_notification_role_context.sql`        | Adds `role_context` (crew/employer/agent) to `notifications` with CHECK constraint. Backfills existing rows by type. Replaces unread index to include role_context for filtered count queries.                                                                                                                                                                                                              |
| `00046_advisor_usage.sql`                    | `advisor_usage` table (person_id + month UNIQUE, question_count int). Owner read-only RLS. Not event-sourced — usage counter utility data for free tier Docky limits.                                                                                                                                                                                                                                       |
| `00047_advisor_usage_write_policies.sql`     | Adds INSERT and UPDATE RLS policies to `advisor_usage` so owner can write own usage rows. Defensive hardening — current code uses serviceClient but policies prevent silent failures if regular client used accidentally.                                                                                                                                                                                   |
| `00048_projection_state_guards.sql`          | Adds `AND status IN ('applied','viewed','shortlisted')` WHERE guards to DAYWORK.ACCEPTED and DAYWORK.REJECTED in `apply_projection`; moves DAYWORK.EXTENDED handler into `apply_projection` and drops standalone trigger/function                                                                                                                                                                           |
| `00049_templates_update_policy.sql`          | Adds UPDATE RLS policy to `daywork_templates` (owner can update own templates)                                                                                                                                                                                                                                                                                                                              |
| `00050_admin_role.sql`                       | Adds `is_admin` boolean to `persons` (default false), adds `'admin'` to `events_aggregate_type_check` CHECK constraint                                                                                                                                                                                                                                                                                      |
| `00051_admin_projection.sql`                 | Adds `ADMIN.ENGAGEMENT_COMPLETED` handler to `apply_projection` — same completion logic as `DAYWORK.COMPLETED` but reads daywork_id from payload                                                                                                                                                                                                                                                            |
| `00052_messages_realtime.sql`                | Adds `messages` table to `supabase_realtime` publication for Supabase Realtime subscriptions                                                                                                                                                                                                                                                                                                                |
| `00053_accept_race_guard.sql`                | Adds positions-full pre-check to DAYWORK.ACCEPTED handler in `apply_projection` — concurrent accepts on a fully-filled daywork no-op instead of creating phantom fills                                                                                                                                                                                                                                      |
| `00054_admin_canonical_projection.sql`       | Adds audit-only no-op handlers for `ADMIN.CANONICAL_ADDED` and `ADMIN.CANONICAL_UPDATED` in `apply_projection` (RAISE NOTICE instead of falling through to unknown event warning)                                                                                                                                                                                                                           |
| `00055_hybrid_roles.sql`                     | Expands department CHECK constraint to include `deck_engineering`, `deck_interior`, `galley_interior`; inserts 3 hybrid roles (Deck/Engineer, Deck/Stew, Cook/Stew)                                                                                                                                                                                                                                         |
| `00056_invitation_source.sql`                | Adds `source` column (`direct`/`invitation`) to `applications`; updates `apply_projection` DAYWORK.APPLIED handler to auto-shortlist when source is `invitation`                                                                                                                                                                                                                                            |
| `00057_nationality_and_visas.sql`            | `nationalities` (40 entries) and `visa_types` (10 entries) canonical lookups with RLS; `nationality_id` and `visa_ids` columns on `profiles`; updated `apply_projection` PROFILE.CREATED/UPDATED handlers                                                                                                                                                                                                   |
| `00058_unread_counts_function.sql`           | `get_unread_counts(uuid)` Postgres function — returns per-engagement unread message counts in a single query, replacing N+1 COUNT loops in badge polling endpoints                                                                                                                                                                                                                                          |
| `00059_permanent_jobs.sql`                   | `permanent_postings` + `permanent_templates` tables, profile permanent availability columns (`permanent_availability`, `notice_period_days`, `currently_employed`), applications + engagements XOR extensions (`permanent_posting_id`, `outcome`, `daywork_id` nullable), `PERMANENT.*` handlers in `apply_projection`, `aggregate_type` + `application_status` + `active_engagements_status` CHECK updates |
| `00060_nda_reveal_permanent.sql`             | Extends `get_vessel_public` with permanent engagement OR branch — crew with active permanent engagement on NDA vessel can see IMO (alongside existing daywork reveal)                                                                                                                                                                                                                                       |
| `00061_atomic_advisor_usage.sql`             | `increment_advisor_usage(uuid, text, integer)` RPC — atomic check-and-increment for free-tier Docky usage; INSERT ON CONFLICT with WHERE guard prevents concurrent requests bypassing limit                                                                                                                                                                                                                 |
| `00062_remove_unused_stcw_certs.sql`         | Removes 3 unused STCW certifications (Survival Craft, Advanced Fire Fighting, Medical First Aid), keeping only STCW Basic Safety Training                                                                                                                                                                                                                                                                   |
| `00063_sea_time_columns.sql`                 | Adds `sea_time_days` and `sea_time_nautical_miles` integer columns to `crew_experiences`; supplementary trigger `apply_sea_time_from_event` patches values from EXPERIENCE.ADDED/UPDATED payloads                                                                                                                                                                                                           |
| `00064_desired_role_and_auto_primary.sql`    | Adds `desired_role_id` FK to profiles; updates `derive_experience_profile()` to auto-derive `primary_role_id` from most recent experience; supplementary trigger `apply_desired_role_from_event` writes desired_role_id from PROFILE events                                                                                                                                                                 |
| `00065_deck_name.sql`                        | Adds `deck_name VARCHAR(50)` to profiles; supplementary trigger `apply_deck_name_from_event` writes deck_name from PROFILE.CREATED/UPDATED payloads                                                                                                                                                                                                                                                         |
| `00066_notification_preferences.sql`         | Adds `email_enabled`, `push_jobs`, `push_applications`, `push_messages`, `push_reminders` boolean columns to `user_preferences` (all default true)                                                                                                                                                                                                                                                          |
| `00067_career_status_from_event.sql`         | Supplementary trigger `apply_career_status_from_event` writes `permanent_availability`, `notice_period_days`, `currently_employed` from PROFILE.CREATED/UPDATED payloads (columns already existed from 00059)                                                                                                                                                                                               |
| `00068_location_city.sql`                    | Adds `location_city_id UUID REFERENCES cities(id)` to profiles; supplementary trigger `apply_location_city_from_event` writes location_city_id from PROFILE.CREATED/UPDATED payloads                                                                                                                                                                                                                        |

| `00069_required_languages.sql` | Adds `required_languages text[]` to dayworks, permanent*postings, daywork_templates, permanent_templates; supplementary trigger `apply_required_languages_from_event` writes from DAYWORK.POSTED/PERMANENT.POSTED payloads |
| `00070_agent_activity_log.sql` | Creates `agent_activity_log` table (UUID PK, person_id FK, action, metadata JSONB, created_at) with RLS (agent insert own, admin select all) |
| `00071_fix_activity_log_rls.sql` | Fixes agent_activity_log RLS: INSERT restricted to agents via identity_type check, SELECT uses `is_admin` boolean instead of nonexistent `identity_type='admin'` |
| `00072_consolidate_triggers.sql` | Consolidates 6 supplementary triggers into `apply_projection` — adds `desired_role_id`, `deck_name`, `location_city_id`, career status to PROFILE handlers; `sea_time_days/miles` to EXPERIENCE handlers; `required_languages` to DAYWORK/PERMANENT.POSTED; drops all 6 trigger functions |
| `00073_availability_date_expiry.sql` | Availability date-based expiry: each date row expires at `date + interval '1 day'` instead of 7-day fixed window; updated `apply_projection` AVAILABILITY.SET handler |
| `00074_vessels_rls_read_access.sql` | Vessels RLS fix: 3 new SELECT policies — authenticated non-NDA read, engaged user NDA read, crew experience NDA read; unblocks PostgREST embedded joins for non-owners |
| `00075_placement_confirmed_status.sql` | Adds `placement_confirmed` to application status CHECK; updates PERMANENT.PLACEMENT_CONFIRMED handler to mark selected application as terminal |
| `00076_seed_experience_brackets.sql` | Seeds 5 experience bracket rows via migration for production (seed files only run locally); idempotent with `ON CONFLICT DO NOTHING` |
| `00077_permanent_post_fields.sql` | Adds `contract_type`, `contract_details`, `description`, `meals`, `positions_available`, `positions_filled` to `permanent_postings` (and same minus `positions_filled` to `permanent_templates`). Updates `PERMANENT.POSTED` handler in `apply_projection` to write the new columns. |
| `00078_custom_access_token_hook.sql` | Custom access token hook: injects `person_id`, `current_hat`, `identity_type`, `onboarded`, `deactivated` into JWT app_metadata. Requires dashboard enablement. Grants to `supabase_auth_admin`. |
| `00079_batch_vessel_lookup.sql` | `get_vessels_public_batch(uuid[])` RPC — same NDA logic as `get_vessel_public` but operates on array of vessel IDs via `WHERE id = ANY(p_vessel_ids)` |
| `00080_smoker_visible_tattoos.sql` | Add `smoker` and `visible_tattoos` nullable boolean columns to profiles; updated apply_projection PROFILE.CREATED and PROFILE.UPDATED handlers |
| `00081_docky_interactions.sql` | `docky_interactions` table (service-role only analytics); GDPR DATA_SCRUBBED handler restored + extended (deletes advisor_conversations, scrubs interactions) |
| `00082_fix_availability_expires_at.sql` | Fixes AVAILABILITY.SET normal path regression — restores `d::date + interval '1 day'` for per-date expiry (regressed by 00075/00077/00080/00081 back to client-sent expires_at which is NULL) |
| `00083_fix_nda_vessel_name.sql` | Adds name masking to `get_vessel_public` + `get_vessels_public_batch` — NDA vessel names return 'NDA Vessel' unless caller is owner or has active engagement. Bug introduced in 00027 when `loa_meters` was added |
| `00084_lower_mca_match_threshold.sql` | Lowers `match_mca_documents` default threshold from 0.7 to 0.6 — small curated corpus (756 chunks) had best matches at 0.678 |
| `00085_invitation_direct_hire.sql` | Makes `application_id` nullable on `active_engagements`; updates `DAYWORK.INVITATION_ACCEPTED` handler to create engagement directly (fill position, supersede overlapping apps, transition to in_progress) — invitation acceptance is now a direct hire, not an application |
| `00086_agent_placement_cities.sql` | Agent placement cities table (person_id FK, city_id FK, unique constraint, RLS owner CRUD + authenticated read) |
| `00087_whatsapp_notification_channels.sql` | `notification_channels` table (person_id, channel_type, encrypted phone, verified, OTP) with owner RLS; `whatsapp_enabled` on user_preferences |
| `00088_gdpr_notification_channels.sql` | DATA_SCRUBBED handler extended: deletes `notification_channels` + `agent_placement_cities`, nulls `deck_name` |
| `00089_subscription_plan_update.sql` | Subscriptions CHECK: remove `crew_unlimited`, add `employer_pro`. Data migration: existing `crew_unlimited` rows → `crew_pro` |
| `00090_engagement_documents.sql` | `engagement_documents` table (engagement FK, message FK, uploader FK, storage path, 48h expiry, soft-delete), `engagement-documents` private storage bucket, participant-gated RLS + storage policies |
| `00091_gdpr_engagement_documents.sql` | DATA_SCRUBBED handler: soft-delete + expire `engagement_documents` for scrubbed users |
| `00092_fix_availability_full_replace.sql` | AVAILABILITY.SET else branch: expire ALL windows (not just not_available) before inserting new range — fixes stale dates persisting |
| `00093_permanent_postings_rls_engaged.sql` | Permanent postings SELECT RLS: allow crew/employer with active_engagement referencing the posting to read it regardless of posting status (fixes null data when posting is filled/cancelled) |
| `00094_fix_deactivation_hat_constraint.sql` | PERSON.DEACTIVATED handler: set `deactivated_at=now()` instead of `current_hat='deactivated'` (violated CHECK constraint, blocked account deletion) |
| `00095_admin_delete_person.sql` | `admin_delete_person(uuid)` RPC — deletes all child rows in FK dependency order then persons row, enabling test user cleanup from Supabase dashboard |
| `00096_person_reactivated.sql` | `PERSON.REACTIVATED` handler in apply_projection — clears `deactivated_at`. Inverse of `PERSON.DEACTIVATED`. Used by reset-to-reactivate flow within retention window. |
| `00097_admin_blocking.sql` | Admin blocking infrastructure — `blocked_at` + `last_event_at` on persons, `cancelled_by` CHECK extended with `'admin'`, 4 new handlers (USER_BLOCKED, USER_UNBLOCKED, ENGAGEMENT_CANCELLED, POSTING_HIDDEN), `last_event_at` tracking on every event, backfill. |
| `00098_support_channel.sql` | Support channel — `support_threads` + `support_messages` tables with RLS, Realtime publication, `aggregate_type` CHECK extended with `'support'`, 2 audit no-op handlers (SUPPORT.THREAD_OPENED, SUPPORT.MESSAGE_SENT). |
| `00099_shore_experiences.sql` | Shore-based experience — `shore_experience_categories` (30 seeded categories) + `shore_experiences` tables with RLS, `aggregate_type` CHECK extended with `'shore_experience'`, 3 handlers (SHORE_EXPERIENCE.ADDED/UPDATED/REMOVED), GDPR cleanup in DATA_SCRUBBED + admin_delete_person. |
| `00100_certifications_v1_expansion.sql` | Qualifications V1 — `subcategory text null` on `certifications`, `idx_certifications_category_subcategory`, 17 preserved UUIDs renamed + remapped to new taxonomy (basic/deck_bridge/engineering/interior/galley/watersports/helideck/other), 248 new canonical rows inserted. |
| `00101_locations_v1_preparatory.sql` | Locations V1 prep — `country_code char(2)` on `regions`; `latitude/longitude/osm_type/osm_id/website/phone/capacity/vhf` nullable on `ports`; `idx_ports_lat_lon` + unique `idx_ports_osm`. |
| `00102_pg_trgm_location_search.sql` | Locations V1 fuzzy-search infra — `unaccent` + `pg_trgm` extensions, `immutable_unaccent` wrapper, trigram GIN indexes on regions/cities/ports, `search_locations` / `get_locations_by_ids` / `top_locations` RPCs. |
| `00103_entry_rights.sql` | Entry Rights V1 — rename `visa_types` → `entry_rights`, drop `region`, add `category` (citizenship/residence/visa), preserve 5 UUIDs with renamed/recategorised rows, delete 5 work-permit/Other, insert 19 new canonical rows, rename `profiles.visa_ids` → `entry_right_ids`, `apply_projection` coalesces entry_right_ids vs visa_ids with live-entry-rights filter. |
| `00104_marinas_v1_expansion.sql` | Locations V1 marina import — OSM-sourced global marina dataset (5,957 filtered + hub-normalized records). Renames 7 launch-region rows to `\_legacy*\*`(avoiding unique-name collision), upserts 63 country-based regions via UUIDv5, re-parents 29 curated cities, deletes legacy region rows, inserts 3,336 new cities, updates 4 curated ports with OSM lat/lon/metadata, inserts 5,933 new ports. Dubai/Abu Dhabi/Antibes/St. Maarten etc. districts normalized onto hub cities. |
|`00105_jwt_hook_admin_and_blocked.sql`| Admin Phase 0 — extend`custom_access_token_hook`to inject`is_admin`and`blocked`JWT claims. Existing claims preserved. Middleware reads the new claims on the fast path for`/admin/\*`guard and blocked-user redirect; DB fallback retained for pre-hook sessions. API-layer gates unchanged. |
|`00106_user_notes.sql`| Admin Phase 1 —`user_notes`table (service-role only, no authenticated RLS policies). FKs:`person_id`RESTRICT,`admin_person_id`ON DELETE SET NULL. Extends`PERSON.DATA_SCRUBBED`to rewrite content of notes about the scrubbed subject to`'[content scrubbed]'`. Extends `admin_delete_person`RPC to cover user_notes for both subject and author. |
|`00107_reports.sql`| Admin Phase 2 —`reports`CRUD table with CHECK constraints (reason_category enum, reason_text ≤1000, status enum, resolution enum, self-report prevention), indexes on`(status, reason_category)`and`reported_person_id`. RLS: authenticated INSERT where reporter_person_id = auth.uid(), SELECT own; admin reads via service client. Extends `admin_delete_person` to delete reports where target is reporter/reported and null out admin_person_id. |
|`00108_admin_delete_full_wipe.sql`| Expand `PERSON.DATA_SCRUBBED`projection to null all ~22 profile PII fields +`persons.current_hat`+ delete`crew_experiences`+`shore_experiences`rows (previously just 4 profile fields). Update`custom_access_token_hook`so`onboarded=true`requires both the profile row AND`persons.current_hat is not null`. Restored users land on null profile + null hat, routing them through onboarding on next sign-in. |
|`00109_regions_country_code_check.sql`| `CHECK (country_code ~ '^[A-Z]{2}$')`on`regions`. NULL values still permitted (three-valued logic); populated rows must match ISO-3166 alpha-2 format. Stops admin canonical CRUD typos from breaking flag-emoji / country-filter downstream code. |
|`00110_grouped_notifications.sql`| `grouped_notifications()`RPC partitions a user's notifications by`(type, coalesce(deep_link, ''))`, returning per-group `total_count`, `unread_count`, `latest_id`, and the most-recent title/body/deep_link/created_at. SECURITY INVOKER with explicit `person_id = auth.uid()` filter. Powers the collapsed notifications UI. |
|`00111_telegram_notification_channels.sql`| Widen `notification_channels.channel_type`CHECK from`'whatsapp'`only to`in ('whatsapp', 'telegram')`. Add `user_preferences.telegram_enabled boolean default false`. New `telegram_link_tokens` table (id, person_id, token, expires_at, consumed_at) for the bot account-linking handshake — owner can SELECT own tokens, writes via service role only. |
|`00112_perf_composite_indexes.sql`| 4 composite indexes paired with hot-path query patterns identified during the Fix 222j audit: `idx_messages_engagement_created (engagement_id, created_at desc)`— pairs with the`.limit(200)`LIMIT in`/api/messages`so the conversations-list query becomes an indexed seek + ordered scan instead of sort-in-memory.`idx_applications_daywork_status_created (daywork_id, status, created_at desc)`and`idx_applications_permanent_status_created (permanent_posting_id, status, created_at desc)`— applicant review pages filter by posting + status and sort by recency.`idx_notifications_person_type_created (person_id, type, created_at desc)`— cron dedup queries filter by`(person_id, type, created_at)`and the existing`idx_notifications_unread`leads with`(person_id, read, role_context)`, missing the type prefix. Plain `create index`(not`concurrently`) — pre-launch with no live write contention, brief table locks not user-visible. Self-contained rollback DROPs all four. |
|`00113_locations_osm_fallback_v1.sql`| Provenance + coordinate columns on `cities` (`source`'curated'|'osm',`latitude`, `longitude`, `osm_place_id`with unique partial index) and`source`on`ports`. Existing rows backfilled to `source='curated'`before NOT NULL + CHECK applied. Targeted gap-fill INSERTs for 11 yacht-hub cities reported missing or commonly typed: Valletta MT, Tortola VG (alias for Road Town), Porto Cervo / Portofino / Capri IT, Split HR, Phuket TH, Monte Carlo / La Condamine MC, Vilamoura PT. Conflict on`(region_id, name)` is no-op so migration is idempotent. |
|`00114_multi_nationality_v1.sql`| Schema prep for multi-nationality (user feedback: dual-passport holders want both flags shown). `profiles.nationality_ids uuid[] not null default '{}'`added, backfilled from existing`nationality_id` (`update profiles set nationality_ids = array[nationality_id] where nationality_id is not null`), GIN index added. Legacy `nationality_id`column kept for backward compat until the projection + read paths migrate in a follow-up — drop in a future migration once all readers use the array. Rollback drops the index + column; legacy column untouched. |
|`00115_certification_bundles_v1.sql`| Bundle/component model for cert matching. New `certification_components(bundle_cert_id, component_cert_id)`junction table, primary key (bundle, component), check (bundle ≠ component), FKs cascade on delete. RLS: read-only for authenticated; admin writes via canonical CRUD with service role. Seeded with two real-world bundles: (1)`MCA Approved Engine Course (AEC 1 & 2) Certificate`covers`AEC 1`+`AEC 2`individually; (2)`STCW 95 (STCW 2010)` covers Personal Survival Techniques (A-VI/1-1), Fire Prevention & Fire Fighting (A-VI/1-2), Elementary First Aid (A-VI/1-3), Personal Safety & Social Responsibilities (A-VI/1-4), Proficiency in Medical First Aid (A-VI/4 1-3). Application code reads this table to expand a candidate's bundles into covered components when matching against required certs — fixes the launch-blocking class of bug where a holder of a bundled cert couldn't apply to a job that listed components separately. |
|`00116_multi_nationality_projection.sql`| Multi-nationality projection wiring (phase 2 of the multi-nationality work). CREATE OR REPLACE`apply_projection`based on the latest 00108 body, with ONLY the PROFILE.CREATED + PROFILE.UPDATED handlers modified to write the`nationality_ids`array column added in 00114. Both handlers accept either the new array OR the legacy single`nationality_id`field (array wins when both present). Dual-write: legacy`nationality_id`column is denormalised as the first element of the array, so existing readers don't break during the transition. A future migration can drop the single column once every reader uses the array. Rollback emits a NOTICE pointing operators at re-applying 00108 to restore the prior body — copying 490 lines into a rollback verbatim was rejected as too brittle. |
|`00117_locations_pending_v1.sql`| Locations V2 schema prep — wave A of a three-layer fallback for "user can't find their location" (canonical → OSM Nominatim live fallback → manual request → admin curation). Extends `cities.source`and`ports.source`CHECKs to include`'pending'`(alongside the existing`'curated'`and`'osm'`) so manually-requested rows persist in the same tables but stay invisible to everyone except the submitter until an admin acts on them. Adds `hidden_at timestamptz null`to both tables so the admin "Hide" action keeps the row resolvable for the submitting user (their FK still works) while excluding it from everyone else's search. Self-contained rollback drops the`hidden_at` columns and restores the prior two-value CHECKs. |
|`00118_locations_pending_search_filter.sql`| Locations V2 wave C — replaces `search_locations`and`top_locations`RPCs from 00102 with versions that filter cities + ports to`source IN ('curated', 'osm')`AND`hidden_at IS NULL`. Pending rows inserted via `/api/locations/request`now stay invisible to other users' searches; the submitting user's own FK still resolves through`get_locations_by_ids` (intentionally NOT filtered) so they keep seeing the name they typed on their profile/posting. Region rows aren't affected — no manual-request flow at the region level. Self-contained rollback restores the unfiltered 00102 bodies. |
|`00119_locations_admin_queue_columns.sql`| Locations V2 wave D — adds `created_at timestamptz default now()`and nullable`submitted_by uuid references public.persons(id) on delete set null`to`cities`and`ports`, plus partial indexes`idx_cities_pending_created`/`idx_ports_pending_created`on`(created_at desc) where source='pending'`. Powers the`/admin/locations/pending`queue's recency ordering and submitter display. Existing canonical/OSM rows back-fill`created_at = now()`at migration time (acceptable because the queue filters by`source='pending'`). Rollback drops the indexes then the columns; FK is`ON DELETE SET NULL`so admins can delete users without breaking pending rows. |
|`00136_engagement_phase_projection.sql`| B-011 commit 2 — `apply_projection` re-creation with phase-aware handlers. New `PERMANENT.SHORTLIST_CHAT_OPENED` handler inserts `active_engagements(phase='shortlist')` rows after race-guarding posting status='active' AND application status='shortlisted', idempotent on `(application_id)`. `PERMANENT.SELECTED` cascade promotes existing shortlist engagement to `phase='active'` if found OR INSERTs new active engagement; loops over OTHER `phase='shortlist'` engagements on the same posting, inserts a warm system message ("This role has now been filled..."), bulk-closes them to `phase='closed' outcome='role_filled'`. `PERMANENT.WITHDRAWN`/`REJECTED`/`CANCELLED_BY_EMPLOYER` extended to also close the relevant shortlist chat with role-appropriate system messages. `PERMANENT.SELECTION_REVERTED` now sets reverted application back to `'rejected'` (was `'not_selected'` — wrong since the employer made an explicit decision). Phase guards (`AND phase='active'`) added to 13 engagement events (POSTPONEMENT_PROPOSED/ACCEPTED/REJECTED, WORK_STARTED/WORK_STARTED_CONFIRMED, COMPLETION_CONFIRMED/DISPUTED, RATED_BY_CREW/EMPLOYER, CANCELLATION_RATED_BY_CREW/EMPLOYER, CHECKLIST.SET/ITEM_TOGGLED) so shortlist chats can't accidentally trigger work-started flows. Handler count 85 → 86. Self-contained rollback restores 00132's exact apply_projection body verbatim. |
|`00135_engagement_phase.sql`| B-011 schema prep — adds `phase` column + CHECK enum to `active_engagements` (default `'active'`) for the permanent shortlist-chat feature. Partial index on shortlist-phase rows for inbox-grouping. Schema-only; projection updates in 00136. Reversible rollback drops the column + index. |
|`00134_templates_partial_save.sql`| B-005 — Templates partial-save schema. Drops NOT NULL on 11 partial-fields of `permanent_templates` so employers can save minimal templates without DB constraint violations. Adds the missing owner UPDATE RLS policy on `daywork_templates` so a future PATCH route can edit rows. No projection changes. Rollback DELETEs partial-saved rows then re-asserts NOT NULL. |
|`00133_admin_action_log.sql`| Audit P1-S6 — `admin_action_log` CRUD utility table (id, admin_person_id, action CHECK with 8 values, target_person_id FK ON DELETE SET NULL, target_id, reason, metadata jsonb, created_at). 3 indexes: by admin (admin_person_id + created_at desc), by target person (partial WHERE target_person_id IS NOT NULL), by action + time. Service-role only at RLS layer (RLS enabled, no authenticated policies); admin reads happen via service client in `/admin/*` routes. Documented exception to event-sourcing per CLAUDE.md (matches daywork_templates + permanent_templates pattern). Populated by the four admin routes (engagement cancel, posting hide, user block/unblock/delete/restore, report resolve, thread close) immediately after `appendEvent` succeeds — gives ops a queryable, indexed surface for "who did what when" without JSON-extraction scans on the events table. Rollback drops the table CASCADE. |
|`00132_audit_p1_combined_fixes.sql`| Pre-launch audit P1 fixes (D-1, D-3, D-4, M-1, M-2, P-4) combined into one migration. PERMANENT.INVITED projection adds a state precondition guard matching the API-layer status check at `/api/permanent/[id]/invite/route.ts:85-90` (P1-D1) — without it, ledger replay against a re-seeded DB where the posting later reached `in_negotiation`/`cancelled`/`filled` would silently create invitations against stale postings. PERMANENT.SHORTLISTED tightened from `status in ('applied', 'viewed')` to `status = 'applied'` only (P1-D3) — permanent has no PERMANENT.VIEWED event so the 'viewed' clause was dead code that diverged from the route-layer check. `permanent_invitations.permanent_posting_id` FK changed from `on delete cascade` to `on delete restrict` (P1-D4) — hard-deleting a posting was previously cascading invitations away and silently nulling `applications.invited_from_id`, losing the audit trail of how applications were sourced; forces admin scripts to soft-cancel postings instead. `get_vessel_public(uuid)` + `get_vessels_public_batch(uuid[])` re-created with `OR ae.employer_person_id = auth.uid()` added to both IMO branches (mirroring the existing name branches — P1-M1) and the status filter widened from `'active'` only to `('active', 'completed', 'closed')` across all 4 reveal branches + the row-visibility filter (P1-M2) — once an engagement completes/closes, the crew member who genuinely worked on the NDA vessel keeps access to its IMO + name. Partial index `idx_persons_deactivated_at on public.persons(deactivated_at) where deactivated_at is not null` added (P1-P4) for the S-2 data-scrub cron's nightly scan. apply_projection body otherwise an exact copy of 00131; handler count unchanged at 85. Self-contained rollback restores 00131's apply_projection body + 00122's RPC bodies + cascade FK + drops the index. |
|`00131_cv_builder_v1.sql`| CV Builder v1 Phase 1 — schema + projection extensions. Profile-level CV settings (`cv_handle text unique`, `cv_handle_updated_at`, `cv_include_sea_time`, `cv_generated_at`) with partial index on `cv_handle`. `crew_experiences.cv_show_full_vessel boolean default true` (per-experience NDA disclosure toggle, only surfaced for NDA vessels). `references.include_on_cv boolean default false` (per-reference inclusion default-OFF). New `permanent_invitations` table mirroring `daywork_invitations` (status CHECK pending/applied/declined/revoked/expired, UNIQUE(posting, crew), three indexes incl. partial `where status='pending'` powering the daily expiry cron, RLS policy: invited_by OR crew OR posting employer). `applications.invited_from_id uuid references permanent_invitations(id) on delete set null` + partial index for backlinking apply→invitation. `events_aggregate_type_check` allow-list extended with `permanent_invitation`. `apply_projection` re-created with 4 changes vs 00130: PERMANENT.APPLIED extended with optional `invited_from_id` branch (race-guarded `AND status='pending'` flip on the linked invitation row, no-op when payload field is null); PERMANENT.INVITED handler appended (INSERT into permanent_invitations); CV.GENERATED handler — stamps cv_generated_at and lazy-mints cv_handle from payload via coalesce(cv_handle, payload.handle); CV.HANDLE_REGENERATED handler — rotates handle, bumps cv_handle_updated_at. Both CV handlers deliberately do NOT bump profiles.updated_at so the spec §11 staleness signal is preserved. Handler count 82 → 85; `$$` count = 2, file ends `end case; end; $$;`. Self-contained schema rollback (drops invited_from_id + index, drops permanent_invitations CASCADE, drops references/crew_experiences/profiles columns + index, restores prior aggregate_type CHECK); apply_projection NOTICE points operators at re-applying 00130 to revert the function body. `apps/web/src/lib/cv/mint-handle.ts`ships the shared 8-char alphanumeric mint helper (5-retry on Postgres 23505, throws`MintHandleError`on exhaustion) for Phase 2 admin route + Phase 8 generate/regenerate routes. Stress test`scripts/stress-test-cv-builder-phase1.ts` 19/19 pass against live remote. |
|`00130_references_nda_referee_visibility.sql`| Drops the NDA write-time gate from REFERENCE.REQUESTED. CREATE OR REPLACE with the 00129 body line-for-line minus the 3-line NDA block (existence + hidden_at checks remain; handler count unchanged at 82). Original NDA gate was internally incoherent — the referee was the captain/HOD on that vessel and already knew the IMO. Protection moves to the display layer in `/api/messages/[engagementId]/context`: when caller is the employer side of a reference-contact chat, vessel is currently NDA, and caller has no separate active daywork/permanent engagement on that vessel with the crew (existing 00083 trust-boundary precedent), `snapshot_vessel_name`is replaced with`'NDA Vessel'`and`snapshot_vessel_imo` is nulled. Referee always sees full snapshot. Other surfaces are party-only or already strip snapshot fields. Rollback NOTICEs operators to re-apply 00129; display-layer mask lives in route code and is unaffected by the SQL rollback. |
|`00129_references_currently_onboard.sql`| Allows references on currently-onboard experiences. Re-creates `apply_projection`with two handlers modified vs 00128: REFERENCE.REQUESTED drops the B-2`is_current=true`block (existence check stays — first-vessel crew can request references from the captain they're working under right now); EXPERIENCE.UPDATED's edit-lock is loosened so currently-onboard experiences with active references can transition to completed exactly once —`end_date`null→date and`is_current`true→false are allowed;`vessel_id`/`role_id`/`start_date`stay unconditionally locked. After the closing UPDATE, the handler propagates the new end_date to any references whose`snapshot_end_date`was captured null, so referee-verified records read "Jan 2024 — Mar 2025" instead of "Jan 2024 — Present" forever. Handler count unchanged at 82. Lessons-mandated`$$` count = 2, ends `end case; end; $$;`. Rollback NOTICEs operators to re-apply 00128. |
|`00128_references_pending_vessel_relaxation.sql`| Drops the `source = 'curated'`requirement from the REFERENCE.REQUESTED handler in`apply_projection`. CREATE OR REPLACE with the 00126 body line-for-line minus the 3-line source check (handler count unchanged at 82). Backstops the API-side relaxation in `lib/references/helpers.ts`. Rationale: since 00119 + the `/api/vessels` POST flip, every newly-added vessel enters the pending-curation queue — gating references on curated-only created a dead-end UX. NDA + hidden_at gates remain. Rollback NOTICEs operators to re-apply 00126 to restore the gate. |
|`00127_find_person_by_email.sql`| `find_person_id_by_email(text)` RPC — case-insensitive email→`persons.id` lookup with `auth.users` join + onboarding-completion gate. SECURITY DEFINER; service-role callers only (`auth.users` is not readable to authenticated users). Used by `POST /api/references` (B-6 conditional notification flow) to fire an in-app `REFERENCE.REQUESTED` notification only when the claimed referee email matches an existing onboarded DockWalker account; the share-link remains the privacy-preserving fallback. Returns NULL on no match (no row, no auth user, or persons row missing). |
|`00126_references_projection.sql`| Consent-based references — projection extensions. CREATE OR REPLACE `apply_projection` with the latest 00123 body PLUS surgical extensions to four existing handlers (`PROFILE.CREATED`extended INSERT to write the new`referee_only`column from payload;`EXPERIENCE.UPDATED`adds a guard query that counts`references`where`experience_id = ... AND status IN ('pending','accepted')`and raises if the payload would change`vessel_id`/`role_id`/`start_date`/`end_date`— P0-A edit-lock;`EXPERIENCE.REMOVED`rewritten as a 3-step Fix A sequence inside the same transaction: stamp`references SET status='revoked', revoke_reason='experience_removed'`first → close pending`reference_contacts`→ DELETE the experience, with the FK ON DELETE SET NULL on`references.experience_id`auto-nulling AFTER the audit rows are stamped;`PERSON.DATA_SCRUBBED`extended with a CASE-based references UPDATE stamping`requester_deactivated`/`referee_deactivated`revoke reasons + a UPDATE on`reference_contacts`for pending employer contacts) PLUS 11 new REFERENCE.* handlers appended below SHORE_EXPERIENCE.REMOVED:`REFERENCE.REQUESTED`(vessel-state gate, B-2 is_current=true gate, per-experience cap by subscription tier),`REFERENCE.ACCEPTED`(state-guard on pending+pending_expires_at),`REFERENCE.COMMENT_UPDATED`, `REFERENCE.DECLINED`, `REFERENCE.REVOKED_BY_REQUESTER`(allows pending+accepted),`REFERENCE.REVOKED_BY_REFEREE`(accepted only),`REFERENCE.EXPIRED`(dual-path with distinct revoke_reasons),`REFERENCE.CONTACT_REQUESTED`(B-2 two-tier employer gate),`REFERENCE.CONTACT_ACCEPTED`(re-validates underlying ref still accepted, INSERTs active_engagements with reference_contact_id),`REFERENCE.CONTACT_DECLINED`, `REFERENCE.CONTACT_THREAD_CLOSED`(B-8 hardcoded`outcome='reference_complete'`). Handler count 71 → 82. Stress test `stress-test-references-schema.ts` covers 40 checks across all reference flows + Fix A 5 sub-cases + B-1 partial-unique-index status filter; all pass. Existing Wave B (37/37) + D-1 (6/6) + D-2 (3/3) regression suites re-run post-migration: still passing. Rollback NOTICEs operators to re-apply 00123. |
|`00125_references_schema.sql`| Consent-based references — schema foundation. Creates `references`table (one row per (experience, claimed_referee) pair with snapshot fields preserved through experience_id auto-null) and`reference_contacts`table (employer-asks-referee chat requests). New columns + alterations:`references`has`requester_person_id`/`experience_id`/`vessel_id`/`requester_role_at_time`/`claimed_referee_role`/`claimed_referee_name`/`claimed_referee_email`/`token (unique)`/`status` (CHECK pending/accepted/declined/revoked/expired)/`referee_person_id`/`comment` (CHECK length ≤500)/`comment_updated_at`/`consented_at`/`responded_at`/`expires_at` (default now()+24mo)/`pending_expires_at` (default now()+30d)/`revoked_at`/`revoke_reason`(CHECK with 7 enum values)/snapshot fields (vessel_imo, vessel_name, start_date, end_date). FK behaviour:`experience_id`/`vessel_id`ON DELETE SET NULL (Fix A — lets EXPERIENCE.REMOVED projection stamp revoke_reason BEFORE the FK auto-nulls so snapshot history survives),`referee_person_id`/`requester_person_id`ON DELETE NO ACTION. Partial unique index`(experience_id, referee_person_id) WHERE referee_person_id IS NOT NULL AND experience_id IS NOT NULL AND status IN ('pending','accepted')`— B-1 fix:`status IN (live)`clause excludes revoked/expired/declined audit rows so the same referee can be re-invited after revoke.`reference_contacts`has`reference_id`(CASCADE),`employer_person_id`, `engagement_id`(FK to active_engagements added later in same migration after the active_engagements broadening),`question`(CHECK ≤200),`status`(CHECK pending/accepted/declined).`active_engagements`broadened (B-4): drops NOT NULL on`application_id`/`start_date`/`end_date`, adds `reference_contact_id uuid`(FK ON DELETE SET NULL), replaces the 2-way`engagements_posting_xor`CHECK with a 3-way one-of (exactly one of daywork_id/permanent_posting_id/reference_contact_id must be set), extends`outcome`CHECK with`'reference_complete'`(B-8 — looked up via pg_constraint introspection because the original CHECK was added inline without an explicit name). New`profiles.referee_only boolean not null default false`column + partial index for lightweight referee signup users (P1-C).`events_aggregate_type_check`allow-list extended with`'reference'`and`'reference_contact'`. Indexes: `idx_references_requester(requester_person_id, status)`, `idx_references_referee(referee_person_id, status)`, partial `idx_references_experience_active(experience_id, status) WHERE experience_id IS NOT NULL`, partial `idx_references_pending_expiry/accepted_expiry`for the cron,`idx_reference_contacts_employer(created_at desc)`, `idx_reference_contacts_reference`, `idx_engagements_reference_contact`, partial `idx_profiles_referee_only`. RLS: `references`SELECT = own outbound or inbound (employer cross-party reads happen via service role);`reference_contacts`SELECT = parties only. Rollback drops both tables CASCADE, drops`profiles.referee_only`(which cascades the partial index), DELETEs orphan reference-contact engagements before re-adding the strict 2-way XOR + restoring NOT NULL on application_id/start_date/end_date, restores prior`outcome`CHECK and prior`events_aggregate_type_check` allow-list. |
|`00124_event_idempotency.sql`| Event idempotency (Risk D-1 from 2026-04-27 audit). Adds `events.idempotency_key text`column + partial unique index on`(person_id, idempotency_key) where idempotency_key is not null`. DROPs the original 6-arg `append_event`(PostgreSQL function overloading by signature would otherwise leave both versions coexisting and ambiguate every 6-arg caller) then recreates it with an optional`p_idempotency_key text default null`parameter. On`unique_violation`(retry from same person), the function catches the error, looks up the original event_id, and returns it without re-running`apply_projection`. Six high-risk routes (DAYWORK.ACCEPTED, DAYWORK.INVITATION_ACCEPTED, ENGAGEMENT.RATED_BY_CREW, ENGAGEMENT.RATED_BY_EMPLOYER, ENGAGEMENT.CANCELLATION_RATED_BY_CREW, ENGAGEMENT.CANCELLATION_RATED_BY_EMPLOYER) wired in the same Fix to pass deterministic keys. Stress test `stress-test-d1-idempotency.ts`exercises 5x concurrent DAYWORK.ACCEPTED with same key vs 3x without: 6/6 pass. Rollback removes the index + column and restores the original 6-arg`append_event` body. |
|`00123_apply_projection_null_safety.sql`| Null-safety hardening for three daywork handlers in `apply_projection`(Risk D-2 from 2026-04-27 audit).`DAYWORK.ACCEPTED`, `DAYWORK.INVITATION_ACCEPTED`, `DAYWORK.POSITIONS_UPDATED`each previously did`SELECT positions_filled, positions_available INTO v_filled, v_available`(or`UPDATE ... RETURNING ... INTO`) and compared the variables in `IF v_filled >= v_available`. If the daywork row was missing, both variables stayed NULL, `NULL >= NULL`coerced to false, and the handler proceeded to insert orphan FKs. Each handler now raises an explicit exception when`v_available IS NULL`. Function body is an exact line-for-line copy of 00121 with the three guards added; handler count 71 → 71. Rollback NOTICEs operators to re-apply 00121. |
|`00122_vessel_history_public_filter.sql`| Vessels V2 wave F follow-up — re-creates `get_vessel_public(uuid)`and`get_vessels_public_batch(uuid[])`with a WHERE-clause filter that excludes`source = 'pending' OR hidden_at IS NOT NULL`rows for non-owner non-engaged callers. Owner exception:`v.owner_person_id = auth.uid()`always returns the row. Engagement exception: an active daywork or permanent engagement (crew or employer side) on a posting referencing the vessel also returns the row — preserves the operational case where admin hides a vessel after an engagement was already booked. Existing NDA / owner / engagement reveal logic for`imo_number`+`name` masking is unchanged; the new filter is upstream of that, deciding whether the row is returned at all. Mirrors the lookup-route filter shipped in Fix 253 for the API side door. Five callsites (`/api/daywork/{discover,applications,invitations}`, `/api/permanent/{applications,discover}`) consume these RPCs; all already build a `Map<id, row>` and handle missing entries gracefully. Rollback NOTICEs operators to re-apply 00083 to restore previous definitions. 6/6 stress checks pass against live DB. |
|`00121_vessel_history_projection.sql`| Vessels V2 wave B — `apply_projection` extension. Three new event handlers (`VESSEL.RENAMED`, `VESSEL.REFLAGGED`, `VESSEL.METADATA_UPDATED`) plus an extended `VESSEL.CREATED`that also writes the new`source`+`submitted_by`columns and seeds an open-ended`vessel_names`row so the timeline always has data from day one. RENAMED / REFLAGGED handlers close the previously-open history row with`effective_to = effective_from - 1 day`and insert a new one; both use`GREATEST(effective_from, ...)`to clamp same-day renames into a zero-length interval rather than underflow the`effective_to >= effective_from`CHECK. The denormalised "current" columns on`vessels` (`name`, `flag_state_id`) only update when the new row is open-ended (skipping admin back-fills of historical aliases). METADATA_UPDATED uses `case when payload ? 'field' then ... else field end`to distinguish "explicit null clears the value" from "omitted preserves the value". Function body is otherwise an exact line-for-line copy of 00116 (handler count: 68 → 71). Lessons-mandated checks pass:`$$`count = 2, file ends with`end case; end; $$;`, all five VESSEL handlers present. Rollback emits a NOTICE pointing operators at re-applying 00116 (same pattern as 00116's own rollback). |
|`00120_vessel_history_v1.sql`| Vessels V2 wave A — schema foundation for the temporal name / flag layer. New columns on `vessels`:`gross_tonnage int`,`beam_meters numeric(6,2)`,`year_built int`(CHECK 1850–2100),`builder text`,`flag_state_id text references public.flag_states(id)`(denormalised current flag),`source text default 'curated' check ('curated'\|'user_submitted'\|'pending')`,`hidden_at timestamptz`,`submitted_by uuid references public.persons(id) on delete set null`; legacy rows back-filled to`source='curated'`. Two new history tables —`vessel_names`and`vessel_flag_states`— each with`(id, vessel_id, name/flag_state_id, effective_from date, effective_to date null, source, submitted_by, created_at)`, an interval CHECK rejecting`effective_to < effective_from`, and indexes for the`(vessel_id, effective_from desc, effective_to)`overlap query Wave F will run. Trigram GIN index on`vessel_names(name)`for fuzzy historical-name search. Partial indexes on`where source='pending'`for the admin queue (Wave E). Backfilled one open-ended`vessel_names`row per existing vessel so display logic has fallback context immediately. RLS: authenticated SELECT on both history tables; INSERTs flow through`apply_projection`as service role (Wave B). 23/23 live-DB checks pass — column existence, CHECK constraints, FK enforcement, interval CHECK, CASCADE delete sweep on the history tables. Self-contained rollback drops the history tables CASCADE then the new vessels columns. |

## Deferred Decisions

- Agent verification process and verified badge
- NDA access request features (crew requesting NDA info)
- Vessel deactivation
- Internal metrics (availability reliability, engagement frequency)
- Push notifications — token infrastructure (Stage 63), targeted delivery (Stage 64), broadcast (Stage 65), and in-app handling (Stage 66) complete on web. Mobile uses `expo-notifications` (Phase 6). Remaining: app icon badge count management via `expo-notifications` badge API
- Whether `daywork_templates` should remain CRUD forever or later move into the ledger
- Admin tooling for duplicate vessel resolution (per-registrant IMO records may diverge)
- "Verified against public records" badge on vessels
- Size band discovery filter operates post-fetch (50-row DB limit applied before filtering) — may produce sparse results. Options: increase pre-filter limit when sizeBandId set, or denormalize size_band_id onto dayworks table
- Stripe product/price creation and dashboard setup (required before subscriptions work in any environment)
- MCA document corpus ingestion — pgvector table deployed empty, need PDF download + chunking + embedding script
- Docky streaming responses (Anthropic SDK supports streaming; deferred to polish stage)
- Docky conversation export/share
- Per-user rate limiting (requires keying on user ID from auth session — adds latency since auth check must run before rate limit)
- ~~**Realtime messaging**~~ — DONE. Web: Stage 104 (Supabase Realtime subscription, fallback polling). Mobile: Stage 169 (`use-realtime-messages.ts`).
- **Admin dashboard** — deferred to closer to launch. Scope: user lookup + deactivation, dispute viewer (ENGAGEMENT.COMPLETION_DISPUTED resolution), event log browser (read-only, filterable), crew list viewer, ability to send messages as "DockWalker Admin" (new sender identity, not tied to a person record). Option 2 approach: built-in `/admin` routes in the existing Next.js app behind a role check, using service role client. Not a quick CRUD panel — intended to be domain-aware with smart tooling.
- **Docky for agents (market intelligence)** — Docky is currently crew-only (career guidance + MCA certs). For agents, Docky would surface aggregate market intelligence from ledger data: salary benchmarks per role/region, time-to-fill metrics, cert demand frequency, applicant-to-fill ratios. Requires significant posting volume to be statistically meaningful (hundreds of postings per region minimum). No agent-vs-agent competition metrics. No individual crew data. Monetisation: agent subscription tier. Prerequisites: platform data density, agent activity patterns from `agent_activity_log`. Do not build until post-traction phase.

- **Certification expiry-date tracking (V1.1)** — V1 stores crew certs as `certification_ids uuid[]` with no per-cert metadata. Adding issue/expiry dates requires either a linking table (`profile_certifications`) or a JSONB column. Punted from Stage 215; revisit when crew or employers request cert revalidation visibility.
- **Discover cert filter migration to multi-select** — V1 left `daywork-browse.tsx` and related filter panels with the existing single-select `Select` for cert filter. Upgrading to multi-select via `CertificationPicker` changes filter semantics (intersection vs exact match) and wire format; defer until employer filter UX is revisited.
- **`education_level` profile field** — separate V1.1 scope, deliberately excluded from Stage 215.
- **Specialised Culinary cert expansion** — 198 niche culinary items in the source (cuisines, techniques, dietary specialisms) deferred until chef user demand surfaces. Chefs without specialty pills fall back to free-text experience entries.

- **Legal placeholders pending lawyer review** (Stage 214) — `apps/web/src/lib/legal-placeholders.ts` ships with `registeredAddress`, `jurisdiction`, and `supabaseRegion` as `[PLACEHOLDER]` strings, and `companyName` / `supportEmail` / `dpoEmail` set to provisional values. `/privacy` and `/terms` render these directly. Must be finalised and lawyer-reviewed before public launch.

- [Stage 170] Mobile UI primitives — 12 shared components in `apps/mobile/src/components/ui/` (Button, Card, Pill, FormInput, SectionHeader, ScreenHeader, EmptyState, TabBar, StarRating, YesNoPartialPicker, BoolToggle, colors.ts), 4 inline component extractions (MessageBubble, PermanentApplicantRow, ConversationRow, ProgressDots), full adoption across 27 screens/components. `#2563eb` eliminated from all non-primitive files via `colors.primary`

- [Stage 171] Mobile Phase 5: Profile + Experience — profile view with hat switch + avatar upload/delete, profile edit with all field pickers (nationality, visa, flag state), experience add/edit/delete with IMO vessel lookup, vessel list/edit screens. New hooks: use-profile, use-experiences, useNationalities/useVisaTypes/useFlagStates in use-canonical. apiPatch + apiUpload utilities. 3 new picker components. expo-image-picker installed.

- [Stage 172] Mobile Phase 6: Docky + Polish — Docky AI advisor (conversation list, chat with sources, usage tracking, 402 upgrade prompt), push notifications (expo-notifications, token register/deregister, tap→deep link), notifications tab with badge counts on tabs, settings (notification prefs, data export, account deletion), billing (plan status, Stripe portal, external Safari checkout), More tab rebuild.

- [Stage 173] Mobile Phase 7 config: EAS Build — eas.json (development/preview/production profiles), app.json plugins (expo-notifications entitlements, expo-image-picker permissions), .env.example verified. Ready for first `eas build`.

- [Stage 174] Web performance + UI polish — (A) hat switcher: "Switch to {otherHat}" action text replaces ambiguous current hat name; (B) full-bleed department images on job cards: `getDepartmentImageSrc` helper extracted, daywork+permanent cards use absolute-fill background with dark gradient overlay, all text switched to white/alpha, `DepartmentChip` no longer used in cards; (C) daywork/permanent toggle relocated from browse content to sticky header bar, "Discover" title removed; (D) JWT custom claims: migration 00078 `custom_access_token_hook` injects person_id/current_hat/identity_type/onboarded into app_metadata, middleware+auth guard read claims first with DB fallback, hat switch forces token refresh, 4 new auth guard tests; (E) batch vessel lookup: migration 00079 `get_vessels_public_batch(uuid[])` replaces N+1 `get_vessel_public` calls across 5 routes (daywork+permanent discover, applications, invitations); 912 tests pass

- [Stage 175] Web performance fixes 3-5 — (A) LookupsProvider: React context in app layout fetches all canonical data (roles, certs, brackets, size bands, nationalities, visas, ports, cities) once on mount with 24h revalidation; 11 pages migrated from inline Supabase queries to context; (B) middleware header dedup: API routes receive x-user-id/x-person-id/x-current-hat/x-identity-type headers from middleware, auth guard skips getUser() when headers present, spoofing prevented by stripping incoming x- headers; (C) parallel initial fetches on discover page; duplicate get_unread_counts already resolved; 914 tests pass (2 new middleware header tests)

- [Stage 176] NotificationCountsProvider — single `/api/notifications/count` call shared across bottom-nav, hat-switcher, notification-bell via React context (was 3 duplicate calls per page); visibility-change revalidation; `refresh()` exposed for post-action updates; 914 tests pass

- [Stage 177] Smoker + visible tattoos profile fields — migration 00080 adds two nullable boolean columns to profiles; `apply_projection` updated (PROFILE.CREATED uses direct cast, PROFILE.UPDATED uses `CASE WHEN ? THEN` pattern to support explicit false/null); onboarding, profile GET/PATCH, view-only profile API all updated; onboarding UI, profile about section, profile edit form all wired; 1 new PATCH test; 915 tests pass

- [Stage 178] Responsive Redesign Phase 0 (Foundation) — CSS variables (`--sidebar-width`, `--content-inset-left`), 3 width utility classes (`page-width`, `page-width-narrow`, `page-width-wide`); new `SidebarNav` component (hidden mobile, visible md+); `BottomNav` hidden on desktop; app layout wires sidebar + content offset; 8 viewport-positioned components fixed for sidebar offset (dialog, toast, bottom-sheet, profile-overlay, availability-overlay, image-cropper, permanent-job-detail, push-toast); z-index normalisation across 11 components; duplicate nav elements hidden on desktop; swipe animation proportional to container width; mechanical `max-w-lg` → `page-width`/`page-width-wide` migration across 30+ files; viewport meta allows zoom; screenshot utility script; 915 tests pass

- [Stage 179] Responsive Redesign Phase 1 (Landing + Auth + Onboarding) — landing hero: 2-column flex layout on desktop with larger logo, left-aligned text, wider description; value props: 3-column row on md+; footer wrapped in page-width-wide; auth pages: subtle radial glow background on desktop; onboarding steps: wider containers on desktop (max-w-sm→max-w-lg, vessel step max-w-md→max-w-2xl); 915 tests pass

- [Stage 180] Responsive Redesign Phase 1 fix + Phase 2 (Discover) — reset-password form gradient fix; daywork card stack constrained to max-w-md on desktop with centred action buttons; permanent job feed, applied tab, and invitations tab use 2-column grid on lg+; 915 tests pass

- [Stage 181] Responsive Redesign Phase 3 (Profile) — crew profile 2-column layout on lg+ (main content + sticky QuickStats sidebar with avatar, role, location, career status, completeness bar); edit form pairs fields side-by-side on md+ (name+deck, city+port, nationality+visas); smoker/tattoos checkboxes flex-wrap; agent form pairs name+agency; 915 tests pass

- [Stage 182] Responsive Redesign Phase 4 (Messages) — chat page 2-column layout on lg+ (chat left + engagement sidebar right with summary card + action buttons); ChatSidebarActions component mirrors kebab menu logic as vertical buttons; summary card hidden in MessageList on desktop; kebab menu hidden on desktop; 915 tests pass

- [Stage 183] Responsive Redesign Phase 5 (My Jobs + Review) — daywork mine tabs use 2-column grid on lg+; permanent mine section grids for templates and postings; daywork review card stack constrained to max-w-md with proportional swipe; permanent review applicant cards in 2-column grid; permanent review min-h-svh fix; 915 tests pass

- [Stage 184] Responsive Redesign Phase 6 (Forms + Simple Pages) — FINAL PHASE. Daywork post form pairs working days + crew needed; permanent form pairs contract type + positions, live aboard + shortlist cap; experience form pairs dates and vessel operation + flag state; vessels page side-by-side form + list on lg+; billing plan cards side-by-side on md+; docky min-h-svh + pb-nav consistency; docky conversation sticky input md:bottom-0; 915 tests pass

- [Stage 185] Audit fixes — codebase quality sweep: bio validation raised to 1000 chars, try/catch on 5 route handlers (availability GET, messages GET/POST, messages/read POST, notifications/read POST), hat cast hardening on availability DELETE + avatar POST/DELETE, theme color constants extracted to shared module, aria-expanded on accordion/toggle buttons, aria-label on icon-only buttons; 915 tests pass

- [Stage 186] Docky single-thread migration — replaced multi-conversation API (`/conversations`, `/conversations/[id]`, `/conversations/[id]/messages`) with single-thread API (`/thread` GET, `/thread/messages` POST, `/thread/clear` POST); 72h auto-expiry on threads; merged conversation list + chat into single `/docky` page with expiry countdown, new-conversation dialog, suggestion chips on empty state; deleted old routes and `[conversationId]` page; loading skeleton; rewrote all advisor tests (5 thread + 2 clear + 7 messages + 3 personalised + 5 usage); 914 tests pass

- [Stage 187] Docky prompt caching + cost reduction — system block with cache_control ephemeral replaces fake user/assistant message pairs; injection defence appended; trimHistory() with 3000-token budget replaces DB LIMIT 10; DOCKY_CORPUS_READY guard in rag.ts skips OpenAI embedding call when corpus not ingested; 921 tests pass

- [Stage 188] Docky rate limits + streaming + interaction logging

- [Stage 189] MCA corpus ingestion script

- [Stage 190] Docky off-topic guard

- [Stage 191] CI/CD automated Supabase migration deploy

- [Stage 192] Production corpus ingestion support

- [Stage 193] CI pipeline fixes — exclude mobile from CI lint (`--filter='!mobile'`); rollback 00076 nullifies FK references before deleting seeded experience brackets — `--production` flag on ingestion script loads `.env.production.local`, 3-second countdown with target URL display before writing to live DB — `deploy-migrations` job in `.github/workflows/ci.yml` runs `supabase db push` on merge to main after all quality gates pass; requires `SUPABASE_ACCESS_TOKEN` and `SUPABASE_DB_PASSWORD` GitHub secrets — refusal rule in BASE_SYSTEM_PROMPT directs non-maritime questions away; refusal marker matches `was_refused` detection in interaction logging — `scripts/ingest-mca-docs.ts` reads PDFs from `corpus/mca/`, section-based chunking with overlap, batch OpenAI embeddings, idempotent Supabase insert via service role, smoke test query, source URL mapping; `pdf-parse` devDep added; 16 MCA PDFs present in corpus; execution pending Docker + API key — usage always tracked (free 15/mo, pro 500/mo); streamDocky() SSE stream with text deltas + done event; client reads stream incrementally; migration 00081 creates docky_interactions table (service-role only) + restores GDPR DATA_SCRUBBED handler (deletes advisor_conversations, scrubs interactions); interaction log inserted after stream completion with latency, token counts, refusal detection; 921 tests pass

- [Stage 194] Bug fixes + rollback hardening — rollback 00023 (delete checklist events before CHECK tighten), rollback 00028 (exception handler for duplicate IMO uniqueness), migration 00082 (fix availability expires_at regression from 00073), profile view service-client for active poster checks (fixes "Profile unavailable" on permanent job cards, also checks `in_negotiation` status), permanent job feed single-card centering, profile page error state instead of blank render; 922 tests pass

- [Stage 195] Agent profile + UX fixes — (A) onboarding agent department selection: replaced flat role checkboxes with HierarchicalPills grouped by department; (B) agent profile view: added bio, nickname, nationality (flag emoji), languages, visas, placement cities display; (C) agent edit form: added bio, nickname, nationality, languages, visas, placement cities fields; (D) agent experiences: loadExperiences() now called for agents, maritime history CTA made into visible button; (E) agent placement cities: migration 00086 creates `agent_placement_cities` table (person_id FK, city_id FK, unique constraint, RLS owner CRUD + authenticated read); GET/PATCH profile endpoints extended; onboarding saves placement cities post-submit; (F) HierarchicalPills: single-select no longer auto-collapses after selection, group pills stay highlighted when containing selected item; (G) salary UX: single input by default with "Salary range" toggle revealing min+max; max defaults to min when range off; (H) contract type drill-down: permanent post form now uses shared ContractDetailsInput with rotational sub-options, permanent leave days, seasonal period; contractDetails wired through to API; (I) meals label: shows "Meals included (optional)" when live aboard checked; (J) agent indicator: discover APIs return poster_is_agent; daywork+permanent cards show "(Agent)" suffix; poster names underlined for tappability; 932 tests pass

- [Stage 196] UX polish batch — (A) public job page: error.tsx client boundary + try-catch in generateMetadata and page component for server-side error logging; (B) DateInput component: dd/mm/yyyy display with auto-formatting + hidden native picker for mobile; replaced all 11 `<Input type="date">` across daywork post, permanent post, experience forms, onboarding, discover filters, postponement overlay; (C) permanent card height: cert pills truncated to 3 + "+X more", languages to 2 + "+X more"; grid items-start alignment; (D) citiesToGroups() shared helper: groups cities by region for HierarchicalPills; placement cities drill-down on profile edit + onboarding; (E) renamed "Role specialisations" → "Department specialisations" across agent UI; resolved IDs to department pills instead of count; (F) permanent mine tabs: replaced inline buttons with shadcn Tabs/TabsList/TabsTrigger; 932 tests pass

- [Stage 197] WhatsApp notifications Session 1 + 2 — migration 00087: `notification_channels` table (person_id UNIQUE FK, channel_type CHECK, encrypted bytea, verified, OTP code + expiry) with owner RLS; `whatsapp_enabled` boolean on user_preferences. AES-256-GCM phone encryption (`crypto.ts`). 4 API routes: `POST register` (E.164 validation, rate limit 3/hr, encrypt, OTP, Twilio send), `POST verify` (code match, expiry check, set verified + whatsapp_enabled), `DELETE disconnect` (hard-delete channel, disable pref), `GET status` (connected flag + masked phone). Preferences API whitelist + select updated for `whatsapp_enabled`. 13 new tests (register: unauth/invalid/rate-limit/success, verify: missing/wrong-length/no-channel/expired/wrong-code/success, disconnect: unauth/success). Session 2: `whatsapp.ts` Twilio send via raw fetch; `whatsapp-dispatcher.ts` template resolver (22 event types → template names + variables + button URLs); `notifyOnEvent` rewritten: in-app always → WhatsApp if connected+enabled → push if no WA → email if no push tokens; 5-minute MESSAGE.SENT cooldown; system message skip; existing push-trigger + permanent-notification tests updated for new dispatch flow; 6 new dispatch tests. Session 3: `WhatsAppSection` component with 3 states (not connected → phone input → OTP verification → connected); country code picker, OTP countdown timer, disconnect with confirmation; `NotificationPrefs` extended with `whatsapp_enabled`; settings page wires toggle + section. Session 4: migration 00088 extends `apply_projection` DATA_SCRUBBED to delete `notification_channels` + `agent_placement_cities` + null `deck_name`; GDPR export includes decrypted WhatsApp phone; broadcast handler batch-queries WhatsApp channels and sends `dw_new_job` template (push skipped for WhatsApp recipients); 951 tests pass

- [Stage 198] Subscription plan cleanup + hat-aware billing page — migration 00089: subscriptions CHECK updated (remove `crew_unlimited`, add `employer_pro`), data migration for existing rows. `SubscriptionPlan` type updated. `requireSubscription` rewritten for exact plan match (parallel tiers, no rank hierarchy). `hasAnyPro` helper added. Checkout route + webhook updated for `employer_pro`. Docky free tier limit reduced 15→10. All `crew_unlimited` references removed from codebase. Session 2: billing page rewritten — hat-aware tier display (crew: €4.99 Crew Pro with Docky/template features; employer/agent: €14.99 Employer Pro with template/shortlist features); billing status API returns `current_hat`; dual-hat edge case handled (cross-tier info banner + "Switch plan" via Stripe portal); 5 billing page tests (crew tier, employer tier, current plan badge, manage subscription, correct plan checkout); 956 tests pass. Session 3: daywork template cap (free: 3 DW, crew hat: 5 DW, employer_pro: unlimited) and permanent template cap (free: 1 PM, employer_pro: unlimited) enforced server-side in POST handlers; client-side 402 `template_limit_reached` toast in both post forms; 2 new cap tests; 958 tests pass. Session 4: shortlist cap tier-gated — free employer max 3, employer_pro max 8; effective cap = min(posting cap, tier max); posting creation clamps submitted cap; shortlist route includes upgrade_url when tier-limited; client-side shortlist input max from billing status fetch; existing tests updated for tier clamping; 958 tests pass

- [Stage 199] Ephemeral document exchange Session 1 — migration 00090: `engagement_documents` table (engagement FK, message FK, uploader FK, storage path, 48h expiry, soft-delete), `engagement-documents` private storage bucket with participant-gated INSERT + service_role DELETE policies. 5 API routes: `POST upload` (single-file, 4MB max, MIME + magic byte validation, 20/day rate limit), `POST finalize` (links documents to MESSAGE.SENT event with `message_type: 'documents'`), `GET list`, `GET download` (signed URL, 410 for expired/deleted), `DELETE` (uploader only, hard-delete storage + soft-delete metadata). `EventPayloadMap['MESSAGE.SENT']` extended with `message_type` + `document_count`. 14 new tests; 972 tests pass. Session 2: document-cleanup cron (every 6h — deletes expired from storage + soft-deletes metadata, cleans orphaned storage, monitors stragglers); chat UI: paperclip button in footer (sequential uploads + finalize), `DocumentCard` component (file icon, size, countdown, download via signed URL, uploader delete, expired/deleted states), document map on page load + refresh after upload; 972 tests pass. Session 3: migration 00091 extends DATA_SCRUBBED to soft-delete + expire engagement_documents for scrubbed users; GDPR export includes document metadata; WhatsApp dispatcher uses `doc_shared` template for `message_type: 'documents'` with uploader name + count + role + job number; 1 new template test; 973 tests pass

- [Stage 200] UX + performance polish batch — P0-A: Safari `crypto.randomUUID()` polyfill via `uuid.ts` fallback; P0-B: card gradient opacity 0.08→0.18, gradient reach 60%→80%, seed-based angle variation, hardcoded green→design tokens; P1-A: required field asterisks on daywork+permanent forms, debounced sessionStorage draft auto-save, inline blur validation with error messages, `DateInput` onBlur prop; P1-B: generic `SearchableSelect` component, agent nationality select converted; P1-C: `Skeleton`/`CardSkeleton`/`ConversationSkeleton`/`ChatSkeleton` components replacing spinners on market, messages list, and chat pages; P1-D: empty state CTAs (Browse jobs on messages, Clear filters on market feed, ShareJobButton on applicant review with expanded meta query); P1-E: `safeFetch` error differentiation (timeout/5xx/4xx/network); P2-A: card padding p-5→p-4, footer divider pt-2→pt-3, destructive button variant, summary card padding normalization; P2-B: 44px tap targets on xs/icon-xs buttons, htmlFor/id pairs on profile form, status badge icons (Send/Clock/Check); P2-C: lookups mount revalidation skip when cache fresh (saves 8 queries), discover size band filter moved to DB-level pre-query (50 rows vs 200); P3: removed availability diagnostic logs, removed unused `.page-width-narrow` CSS; 977 tests pass

- [Stage 201] Post-audit remediation — positions-filled removed from crew views (mission violation fix); discover RPC error handling; daywork post form complete validation + dead vessel_id template ref removed; unused SearchableSelect deleted; aria-required on form inputs; beforeunload draft flush; onboarding double-submit guard; combined filters test strengthened (7 filter assertions); 977 tests pass

- [Stage 202] Pre-launch remediation — rate limiting middleware wired (`middleware.ts` re-exports `proxy.ts`, cron routes exempted); `positions_filled`/`positions_remaining` stripped from crew-facing discover API + "Last position!" gamification removed; try/catch hardened on profile GET/PATCH + accept route; Escape key dismiss on BottomSheet (5 overlays), AvailabilityOverlay, ProfileOverlay; 3 new test files (call-ended, cron-document-cleanup, whatsapp-status — 13 tests); 990 tests pass

- [Stage 203] LCP performance fixes — Framer Motion entrance animation removed from card stack (saves ~500ms); loading spinner replaced with CardSkeleton placeholders; profile + availability fetches deferred via `startTransition` (non-blocking); discover API SQL audit (all joins verified necessary, cert names removable as future optimization); font preloads handled by Next.js `localFont` + `display: swap`; try/catch hardened on daywork POST (FK validation), apply POST (lookup + availability), rate POST (engagement + rating check); 990 tests pass

- [Fix 222i] Duplicate signup detection — `/auth/signup` now detects existing-user signup attempts instead of falsely showing "Check your email". Two paths: (a) Supabase obfuscation case where `signUp` returns no error but `data.user.identities` is empty (existing confirmed user, anti-enumeration default behaviour) and (b) explicit `error.message` matching `/already registered/i`. Both route to a new `alreadyRegistered` view rendering an "Account already exists" card with a primary "Sign in" CTA → `/auth/login`, secondary "Reset password" CTA → `/auth/forgot-password`, and a "Use a different email" reset that clears the form and returns to the signup card. Existing `signup-polling.test.tsx` mock updated to return populated `identities` so the success/poll flow keeps routing correctly; new `signup-already-registered.test.tsx` covers the empty-identities case, the explicit error case, the genuine-new-signup case, the reset-form action, and ensures non-duplicate errors still render inline. 1190 tests pass (+5 new).

- [Fix 222j] Disk IO emergency — first pass. Supabase project hit Disk IO budget exhaustion: low CPU + sustained IOwait spikes, slow logins (incl. one 400 traced to JWT custom_access_token_hook starvation), 406s on `engagement_ratings`/`engagement_checklists` polluting API logs. Codebase audit pinpointed the always-on culprits; this stage ships the surgical wins. (1) `/api/messages` conversations list — added `.limit(200)` to the `messages` query that previously pulled every message from every engagement the user had ever had, just to derive last-message previews. Trade-off: users with very high cumulative message volume across many engagements may see a missing preview on the oldest engagement; acceptable pre-launch, RPC-based per-engagement DISTINCT ON deferred. (2) `/api/cron/availability-expiry` — added `.limit(20000)` safety cap on the full-table scan of `availability_windows` (table now ~6k rows and growing). (3) `NotificationCountsProvider.handleVisibility` — replaced naïve "fetch on every visibility-change" with a 30s cooldown timer (debounced trailing edge), preventing API floods when users rapidly switch tabs. (4) `.single()` → `.maybeSingle()` on the four chat-hot-path callsites that hit `engagement_ratings`/`engagement_checklists` with no row present — chat context route (2 callsites, fired every 5s by the realtime fallback poll), checklist POST + rate POST. Stops the 406 stream on PostgREST and skips the error-then-recover branch. Test mocks (`messages-context`, `engagement-rate`, `engagements-checklist`, `messages`) updated to chain through `.maybeSingle` and `.limit`. 1190 tests pass. Deferred follow-ups documented for the next perf stage: Telegram broadcast batching (cities/notification_channels lookups per event), Realtime-aware chat polling (only poll while disconnected), JWT hook caching, RPC for last-message-per-engagement.

- [Fix 222k] Disk IO emergency — second pass (composite indexes). Migration 00112 applied to live Supabase: 4 composite indexes that pair with actual hot-path query patterns identified by the Stage Fix 222j codebase audit. (1) `idx_messages_engagement_created (engagement_id, created_at desc)` on `messages` — pairs with the bounded `/api/messages` query so the conversations-list lookup is an indexed seek + ordered scan instead of sort-in-memory. (2) `idx_applications_daywork_status_created` on `applications` — daywork applicant review filters by `daywork_id + status` ordered by recency; existing single-column indexes forced one-at-a-time selection. (3) `idx_applications_permanent_status_created` — same pattern for permanent posting review. (4) `idx_notifications_person_type_created` — cron dedup queries filter by `(person_id, type, created_at)` and the existing `idx_notifications_unread` leads with `(person_id, read, role_context)`, missing the type prefix. Plain `create index` (not `concurrently`) is acceptable pre-launch — zero live write contention, brief table locks not user-visible. Schema v111 → v112. 1190 tests pass. Active_engagements composites (suggested by audit) intentionally skipped: `/api/messages` doesn't filter by status or order on that query, and `/api/cron/engagement-starts` runs once daily — neither warrants the index maintenance cost.

- [Fix 264] Bug-intake Batch 3d — References Phase 2 (B-003). Wires the new `<ContactReferencesButton>` onto both shortlist tab card surfaces so employers can contact references without first opening the ProfileOverlay. `/api/daywork/[id]/applicants` and `/api/permanent/[id]/review` extended to hydrate `references[]` per applicant (only populated when status is shortlisted-or-downstream — matches the contact-API shortlist gate so pre-shortlist cards never display a button that would 403). Service client bypasses owner-scoped RLS on `references` (mig 00125); per-referee profile + role resolution batched alongside the existing applicant enrichment so no extra round-trips on the page side. UI shows "References (N)" pill on the applicant card; tap opens the picker (2+ references) or the ContactReferenceDialog directly (1 reference). Two existing tests (`applicants.test.ts`, `permanent-review.test.ts`) extended with the new references-empty mock to match the route's added query stage. 1516/1516 tests pass; type-check + lint clean. No schema changes.

- [Fix 263] Bug-intake Batch 3c — Rejection notification tone (B-002). Closes the user-screenshotted "❌ Permanent application not selected" Telegram card complaint. Three Telegram-dispatcher branches reframed away from the red ❌ + "rejected" framing to forward-looking copy that matches the celebratory positive templates' shape (emoji + bold header — role name): `DAYWORK.REJECTED` → `📈 On to the next — {role}`, `PERMANENT.REJECTED` → `🔍 Keep exploring — {role}`, `PERMANENT.PLACEMENT_CONFIRMED` Position-Filled branch (same audience — rejected applicants) → `🌟 Role filled — {role}`. In-app + push handlers softened body text to match (titles stay neutral as `Application Update` so the badge doesn't pre-load a negative emotion). WhatsApp dispatcher edits dropped from scope per project deferral notes (WhatsApp migration is a v2 feature, Telegram covers crew/employer notifications today) — inline comments added on the three `dw_rejected` / `pm_rejected` / `pm_position_filled` Meta-template branches noting that warmer copy mirroring the Telegram body should be re-submitted as `_v2` templates when WhatsApp launches. 1516/1516 tests pass; type-check + lint clean. No schema changes. No new tests — copy-only changes.

- [Fix 262] Bug-intake Batch 3b — References feature, Phase 1 (B-003). Closes the user's core complaint that consented references and the Contact-reference button were "nowhere to be seen on the employer side." Root cause was an RLS visibility leak: the profile API queried `references` with the regular `supabase` client, but the table's RLS (mig 00125) is owner-scoped (requester + referee), so any non-owner viewer (employer/agent) silently got an empty array and the entire references section rendered nothing — even when consented references existed. Fix: `buildCrewProfile` now accepts `serviceClient` as a separate param and uses it for the references query specifically; tier-cap visibility logic + upstream `checkRelationshipContext` are the access controls. ProfileOverlay's existing per-reference "Contact reference" buttons (already gated to employer/agent hat) now actually render data. Plus a new server-side shortlist gate on `/api/references/[id]/contact`: contacting a reference is only allowed when the calling employer has a shortlisted-or-downstream (`shortlisted`, `accepted`, `selected`, `placement_confirmed`) application from the reference's requester on one of their own postings — enforces the user's "shortlist→reference-contact" mechanic, prevents fishing, limits referee burden. Gate runs after the global budget gates (budget is "stop everything", shortlist is per-request). Crew self-profile gains a sibling "Manage references" button next to "Add reference (x/y)" on each experience card (renders when refs already exist), routing to `/settings/references?exp={id}` — closes the discoverability gap that buried the settings page. Settings references page reads `?exp=` and scrolls the matching row into view with an accent ring. New shared `<ContactReferencesButton>` component handles 0/1/many references with an inline picker dialog (Phase 2 will wire it into shortlist tab cards once applicant APIs return references[]). 1516/1516 tests pass; type-check + lint clean. No schema changes. Phase 2 (shortlist-card surfacing of References pills) deferred — the ProfileOverlay path already covers the user's core need.

- [Fix 261] Bug-intake Batch 3a — Templates feature reworked end-to-end (B-005). Closes the user-reported "templates are completely broken" issue from the 2026-05-02 intake. Permanent template POST rewritten to build the insert from only fields the user supplied (no `|| null` coercion that hit the NOT NULL constraints relaxed in 00134). Daywork template POST refactored to the same partial-friendly shape + the missing `name` required-field validation. New `PATCH /api/daywork/templates/[id]` route mirrors the permanent PATCH (depends on 00134's daywork UPDATE RLS policy). Both post forms reframe with a top-of-form `templateMode` toggle: when ON, required-field asterisks drop, full-form validation is bypassed in submit, header + submit button reframe to template-create / template-edit copy, legacy "save AND post" path is gone. Search-param routing: `?mode=template` (auto-toggle into create), `?mode=edit&templateId=...` (load template + PATCH on submit), plus existing `?templateId=` (Use, pre-fill only). Posting type selector gains a third "Post from a template" entry routing to `/daywork/mine?tab=templates`. Templates tab on My Jobs (both daywork + permanent sections) gets `Edit` and `Create template` buttons alongside the existing Use/Delete. Old in-form "Load template" dropdowns + the bottom save-as-template checkbox are removed; the dedicated entry point is now My Jobs. Migration 00134 (committed 64ca038) provides the schema relaxation + missing UPDATE policy. 1516 tests still pass; type-check + lint clean.

- [Fix 260] Bug-intake Batch 2 — mid-sized UI + API fixes from the 2026-05-02 intake (B-001/B-008/B-010, fix plan in `tasks/todo.md`). (B-008) Applied tab tombstone — daywork + permanent symmetric: when the joined posting is null (cancelled, closed, or RLS-hidden), the card no longer silently returns `null` and disappears from the rendered list. Renders a dashed-border card with status badge, "Posting no longer available" message, applied date, optional original message, and a Withdraw button when status is still withdrawable. Closes the count-vs-list discrepancy that surfaces after a connected user is data-scrubbed or a posting goes to a non-`active` status. (B-001) Cert extras pill expansion — permanent and daywork at parity: `/api/permanent/[id]/review` returns `cert_extras_ids` (alongside the existing count); `/api/daywork/[id]/applicants` extended with the same bundle-aware `cert_match` + `cert_extras_ids` computation (mirrors permanent's `meetsRequirements` logic, fetches `certification_components` in parallel only when the posting requires certs). UI on both review pages shows the cert-match indicator (✓ All N required / ⚠ M/N missing) plus a clickable `+ N additional` pill that expands inline to render the extra cert NAMES (resolved via `useLookups`). Daywork's bare `{count} certs` badge is gone. (B-010) Available Crew empty states + monetization invariant pinned — `available-crew-tab.tsx` empty state now explains the visibility rule transparently ("Crew Pro members with current, in-range availability in the same city as the posting"); `applicants-tab.tsx` empty state on zero applicants now leads with `<Users /> View available crew` instead of "Check back later" (closes the discoverability gap that was a quiet revenue leak). New `__tests__/api/daywork-available-crew.test.ts` ships 3 tests covering the monetization invariant — Pro crew + current availability + matching city + role mismatch + `allRoles=true` MUST surface; non-Pro crew MUST NOT; `allRoles=false` filters by role. Route logic is provably correct under the codified invariant; the user's "missing test crew" reproduction is most likely a data state (expired availability, city mismatch, or subscription not `active`/`trialing`) rather than a code bug — diagnostic instrumentation deferred (pre-commit ban on `console.log`). Schema unchanged. 1516/1516 tests pass (+3), type-check + lint clean.

- [Fix 259] Bug-intake Batch 1 — quick-win UI + middleware fixes from the 2026-05-02 user-reported bug intake (B-001 → B-010 logged in `tasks/todo.md`). Four bugs shipped: (B-006) header z-index collision on review pages — `daywork/[id]/review/page.tsx:325` and `permanent/[id]/review/page.tsx:235` headers bumped from `z-10` → `z-30` so the swipeable applicant card (also `z-10`) no longer paints over them on desktop scroll. (B-009) daywork past-date block — `todayISO` constant added to post page, `min={todayISO}` on start `<DateInput>`, `min={startDate || todayISO}` on end `<DateInput>`; `WorkingDayCalendar` extended with a `selectableDates` memo + `disabled={isPast}` on past date-cells (opacity-40 cursor-not-allowed) so users can no longer pick yesterday and have submission 4xx. Daywork-only — permanent post intentionally allows past dates (display as "ASAP"). (B-004) employer review buttons made explanatory — daywork's three icon-only circular buttons replaced with crew-style labelled `<Button>` pairs (red `<X /> Show next`, amber `<Star /> Shortlist` applicants-tab-only, green `<Check /> Hire`); permanent review's `Reject` outline buttons softened to `<X /> Show next` with destructive-coloured outline; `UnderlineTabs` extended with optional `icon?: ReactNode` prop and `<Star />` wired to the Shortlist/Shortlisted tabs on both review pages so the icon meaning anchors across page (button + tab). (B-007) middleware role-gate — two new clauses in `lib/supabase/middleware.ts`: crew → `/discover` redirect for `/daywork/post`, `/daywork/mine`, `/permanent/mine`; employer → `/daywork/mine` redirect for `/profile/add-experience`, `/profile/edit-experience/*`, `/profile/add-shore-experience`, `/profile/edit-shore-experience/*`, `/settings/cv`, `/settings/references`. Closes the CLAUDE.md role-gating invariant gap exposed by hat-switch reload landing on the same unguarded page. JWT custom claims (Stage 174) make this zero-DB-cost. 1513 tests pass, type-check + lint clean. No schema changes.

- [Fix 258b] Consent-based references — Phases 4-6: full UI surface + visibility filter + tests + docs. Phase 4 ships all 9 UI surfaces (AddReferenceDialog with W-A consent + share-link primitive, /ref/[token] SSR landing + RefereeConsentClient with W-E load-bearing consent + W-F silent decline, /settings/references with W-B/W-C/W-D/W-G/W-H modals + 3 tier-aware lists + Inbound history with human-readable revoke_reason labels, Profile experience cards "Add reference (n/cap)" gated on B-2 + vessel state, edit-experience H-7 lock banner, Add Experience required Yes/No checkbox auto-disabled when is_current=true (B-2), lightweight referee signup branch on /auth/signup?referee=1 calling onboard_person with referee_only=true, ProfileOverlay reference rows viewed-by-others with EpauletteBadge + comment quote + tap-to-open nested ProfileOverlay + Contact reference button gated to employer/agent + ContactReferenceDialog with W-J + structured 402 gate fallback, /messages consent_prompts[] aggregator with W-E + W-F + W-I prompts and silent contact decline, ReferenceContactHeader replacing the daywork/permanent ChatHeader when reference_contact_id IS NOT NULL — shows snapshot + comment + W-K close + Fix A 7-reason revoked banner). Phase 5: GET /api/profile/[personId] crew branch returns experiences[i].references[] with EXPERIENCE OWNER's plan driving the visibility cap (Free=1 / Crew Pro=3); self-view bypasses cap; pure read-side filter so downgrade hides surplus without losing data. Phase 6: end-to-end stress test stress-test-references.ts covers full happy path + 5 negative cases + B-3 idempotency-key-salting verification — 21/21 pass on live remote; 32 new vitest unit tests in references-routes.test.ts cover the 11 routes (auth/validation/gates/idempotency/state guards) — 32/32 pass. Schema unchanged from Fix 258 (still v127); 1286 → 1318 unit tests (+32). Foundation + API + UI + tests all green.

- [Fix 222l] Vercel function region pin to `fra1` (Frankfurt). `curl -I /api/health` showed `X-Vercel-Id: fra1::iad1::...` — edge in Frankfurt but the FUNCTION was running in Washington DC. Supabase is in EU (Frankfurt), so every API route's queries crossed the Atlantic twice (function → DB → function), adding ~80-100ms per query × 3-5 queries per route = ~300-500ms of pure network latency on every page load. Pinned via `"regions": ["fra1"]` in `vercel.json` — co-locates functions with the database. Post-deploy verification: `X-Vercel-Id: fra1::fra1::...`. Single biggest latency win available without code changes; dwarfs everything from Fix 222j/k combined. Safe for global users — for US users the function leg gets slightly longer, but the function-to-DB leg is now zero, net still a win.

- [Fix 258] Consent-based references — Phase 1: schema foundation + projection extensions. Migrations `00125_references_schema.sql` + `00126_references_projection.sql` ship the foundation for the consent-based referencing feature (crew add Captain/HOD references to past experiences; referees consent in-app via opaque-token share-link routed through `/messages`; employers can later request to contact a referee; full audit trail via the event ledger anchored to vessel-IMO + experience snapshots; subscription-tier gates with explicit pay-gate UX). 00125 creates `references` and `reference_contacts` tables, broadens `active_engagements` to host reference-contact chats (drops NOT NULL on `application_id`/`start_date`/`end_date`, replaces the 2-way `engagements_posting_xor` with a 3-way one-of including `reference_contact_id`, extends `outcome` CHECK with `'reference_complete'` for B-8 — the original CHECK was added inline in 00059 without an explicit name, so 00125 looks the constraint up via `pg_constraint` introspection before dropping). Adds `profiles.referee_only boolean not null default false` for lightweight referee signups (P1-C). Extends `events.aggregate_type` allow-list with `'reference'` + `'reference_contact'`. 00126 replaces `apply_projection` with the 00123 body PLUS 11 new REFERENCE.\* handlers + surgical extensions to PROFILE.CREATED (referee_only column write), EXPERIENCE.UPDATED (P0-A snapshot-field edit-lock — guard query counts `references where experience_id=... AND status IN ('pending','accepted')` and raises if vessel/role/start/end would change), EXPERIENCE.REMOVED (Fix A 3-step soft-revoke: stamp references→close pending contacts→DELETE; FK ON DELETE SET NULL on `references.experience_id` auto-nulls AFTER stamping so snapshot history survives), and PERSON.DATA_SCRUBBED (CASE-based references revoke for requester/referee paths + reference_contacts close for employer path). Handler count 71 → 82. **Eight pre-implementation fixes baked in:** B-1 — partial unique index `(experience_id, referee_person_id) WHERE status IN ('pending','accepted')` excludes revoked/expired audit rows so the same referee can be re-invited after a revoke; B-2 — `is_current=true` experiences forbidden from receiving references (route + projection); B-3 — resend's REFERENCE.REQUESTED idempotency key salted with `:resend:${oldRefId}` so D-1 dedup doesn't drop it; B-4 — active_engagements broadened to accept reference-contact-only inserts (3-way XOR); B-5 — rollback drops `profiles.referee_only` + DELETEs orphan reference-contact engagements before re-asserting NOT NULL; B-6 — REFERENCE.REQUESTED notification fires conditionally only when claimed_referee_email matches an existing person (route-side responsibility; projection unchanged); B-7 — verified `onboard_person(p_identity_type, p_current_hat, p_profile jsonb, p_person_id)` accepts flexible JSONB payload — no signature change needed, lightweight signup just passes `referee_only: true` inside `p_profile`; B-8 — CONTACT_THREAD_CLOSED handler hardcodes `outcome='reference_complete'` (W-K modal doesn't capture an outcome string). New stress test `scripts/stress-test-references-schema.ts` covers 40 checks (per-experience cap free vs pro, vessel-state gates, B-2 is_current gate, accept flow, comment 500-char CHECK, B-1 unique-after-revoke, revoke routing + revoke_reason stamping, snapshot immutability, edit-lock, Fix A 5 sub-cases, B-4 XOR, PERSON.DATA_SCRUBBED references logic, PROFILE.CREATED referee_only); all pass. Existing Wave B (37/37) + D-1 (6/6) + D-2 (3/3) regression suites re-run post-migrations: still passing. Schema v124 → v126. Phase 1 of 6 (Phase 2 = event types + helpers, Phase 3 = 11 new API routes + 1 cron + 3 modified routes, Phase 4 = 9 UI surfaces, Phase 5 = subscription degrade visibility, Phase 6 = tests + docs).

- [Fix 257] Event idempotency — Risk D-1. Migration `00124_event_idempotency.sql` adds `events.idempotency_key text` (nullable) + a partial unique index on `(person_id, idempotency_key) where idempotency_key is not null`, then DROPs the original 6-arg `append_event` and recreates it with an optional 7th argument `p_idempotency_key text default null`. On `unique_violation` from the partial index (a retry of the same event by the same person), the new function catches the error, looks up the original event*id, and returns it WITHOUT re-running `apply_projection` — the network retry resolves to the original event silently. The old signature DROP is essential because PostgreSQL overloads functions by signature: a bare `CREATE OR REPLACE` with the new arity left both versions coexisting, and any 6-arg caller hit `Could not choose the best candidate function`. Discovered when the existing Wave B + D-2 stress tests broke immediately after applying the migration; fixed by adding `DROP FUNCTION ... (text, text, text, text, jsonb, uuid)` before the CREATE OR REPLACE. `appendEvent` helper in `packages/db/src/events.ts` extended with optional `idempotencyKey?: string` parameter; passes through as `p_idempotency_key`, defaults to `null` when omitted (full backwards compat with the ~25 routes that don't yet opt in). Six high-risk routes wired in this commit: (a) `DAYWORK.ACCEPTED` in `apps/web/src/app/api/daywork/[id]/applicants/[crewId]/accept/route.ts` — deterministic key `DAYWORK.ACCEPTED:${crewId}:${dayworkId}` prevents `dayworks.positions_filled` from double-incrementing on retry; (b) `DAYWORK.INVITATION_ACCEPTED` in `apps/web/src/app/api/daywork/invitations/[id]/respond/route.ts` — key `DAYWORK.INVITATION_ACCEPTED:${invitationId}` (invitation IDs are unique); (c-f) all four `ENGAGEMENT.RATED*\*`variants in`apps/web/src/app/api/engagements/[id]/rate/route.ts`— each keyed`${eventType}:${engagementId}`, so the partial unique index scoped on `(person_id, idempotency_key)`prevents a duplicate rating row even if a retry beats the application-layer pre-check on`engagement_ratings`. Why these six and not all ~30 state-mutating routes: most events are protected by either (a) a payload UUID hitting a PK constraint on insert (DAYWORK.POSTED, VESSEL.CREATED, EXPERIENCE.ADDED, etc.), or (b) a WHERE-clause guard in the projection that makes the second run a no-op (state-transition events like SHORTLISTED, SELECTED, WORK_STARTED that check `status in (...)`before transitioning). The six wired here are the ones with neither shield — they increment counters or INSERT without ON CONFLICT. Remaining routes can opt in later via the same helper API. New stress test`scripts/stress-test-d1-idempotency.ts`exercises the live remote DB end-to-end: fires`DAYWORK.ACCEPTED`5x concurrently with the same key against a fresh fixture daywork, asserts (i) all 5 RPC calls returned the same event_id, (ii) the`events`table has exactly 1 row with that key, (iii)`dayworks.positions_filled`incremented by exactly 1 (not 5), then runs the negative control — same event 3x WITHOUT a key — and asserts 3 distinct event_ids +`positions_filled = 3`to demonstrate the bug the dedup is preventing. 6/6 checks pass. Existing Wave B (37/37) + D-2 (3/3) stress tests re-run post-D-1: still passing.`appendEvent`helper change is backwards-compatible — every existing route that doesn't pass`idempotencyKey` continues to work identically. Schema v123 → v124. Second of the two P0 fixes flagged by the 2026-04-27 end-to-end audit.

- [Fix 256] `apply_projection` null-safety hardening — Risk D-2. Migration `00123_apply_projection_null_safety.sql` re-creates `apply_projection` with the full 00121 body plus explicit null guards on three daywork handlers. The bug being fixed: in `DAYWORK.ACCEPTED` (line 367-378 in 00121), `DAYWORK.INVITATION_ACCEPTED` (line 392-403), and `DAYWORK.POSITIONS_UPDATED` (line 349-355), each handler runs a `SELECT positions_filled, positions_available INTO v_filled, v_available FROM dayworks WHERE id = ...` (or an `UPDATE ... RETURNING ... INTO` equivalent) and then compares the variables in `IF v_filled >= v_available THEN`. If the daywork row no longer exists (concurrent admin deletion, future cleanup process, race with cancellation), the SELECT/UPDATE produces no row and the variables stay NULL — `NULL >= NULL` evaluates to NULL which an IF coerces to FALSE, allowing the handler to proceed and (for ACCEPTED + INVITATION_ACCEPTED) insert orphan `applications` + `active_engagements` rows referencing a missing `daywork_id`. Latent today since no code path deletes dayworks (we soft-cancel via `DAYWORK.CANCELLED_BY_EMPLOYER`), but the failure mode is silent state corruption that would surface as customer-facing "ghosted-engagement" complaints once any cleanup process or admin hard-delete is added. Each handler now does `IF v_available IS NULL THEN RAISE EXCEPTION 'DAYWORK.X: daywork % no longer exists'` immediately after the SELECT. Two-line addition per handler; no other behaviour change. New `scripts/stress-test-d2-null-safety.ts` fires each of the three event types against a non-existent daywork UUID via `append_event` RPC and asserts the call returns the expected exception — 3/3 pass. Existing Wave B stress test re-run post-migration: 37/37 still pass (no regression in any VESSEL.\* handler, projection body intact). Lessons-mandated `apply_projection` replacement protocol followed: handler count 71 → 71 (verified by `grep -c "    when '"`), `$$` count 2 in function body (verified by `grep -n '\$\$'`), function ends with `end if; end; $$;` matching 00121 structure. Rollback NOTICEs operators to re-apply 00121 (defensive guards only — un-applying does not corrupt data). Schema v122 → v123. First of the two P0 fixes flagged by the 2026-04-27 end-to-end audit; D-1 (event idempotency) follows.

- [Fix 255] Vessels V2 — Wave F follow-up: date-overlap historical name display. New helper `apps/web/src/lib/vessels/historical-names.ts` (`resolveHistoricalVesselNames`) batch-fetches `vessel_names` rows for an array of `(vessel_id, start_date)` pairs and returns the name whose `[effective_from, effective_to]` interval covers each start_date — one DB roundtrip regardless of how many experiences. Three GET endpoints enriched: `/api/experiences` (own profile), `/api/profile/[personId]` crew branch (view-only profile via ProfileOverlay), `/api/profile/[personId]` agent branch (`maritime_background`). Each adds a `historical_vessel_name` field on every experience row — only set when the resolved historical name differs from the denormalised `vessels.name` ("now"). UI updated in two places: `profile-experience-section.tsx` (own profile experience cards) and `profile-overlay.tsx` (view-only crew + agent maritime background) — both render `historical_vessel_name ?? vessel_name` with a hover/title attribute reading "Now M/Y \<current_name\>" when a historical name is set, so users see the vessel as they knew it during the experience window with one-glance access to the current canonical name. Skipped: chat summary cards, mine page cards, applicant cards — these display engagement-time data (booking dates), not crew-experience-time data, and don't need historical resolution. Flag history (`vessel_flag_states`) intentionally NOT auto-derived — `crew_experiences.flag_state` is a free-text field captured per experience and overrides any vessel-level history. 6 new tests for the helper covering open-ended current name, mid-history match, predates-history fallback, multi-vessel batch, and same-day-rename zero-length interval. Existing experiences API test updated with a follow-up `vessel_names` mock + 1 new test asserting the renamed-vessel case surfaces the historical name. 1286 tests pass (+7). Type-check + lint clean. No schema changes.

- [Fix 254] Vessels V2 — Wave F follow-up: filter pending + hidden from `get_vessel_public` and `get_vessels_public_batch`. Migration `00122_vessel_history_public_filter.sql` re-creates both RPCs with a WHERE-clause filter excluding `source = 'pending' OR hidden_at IS NOT NULL` rows for non-owner non-engaged callers. Owner exception: `v.owner_person_id = auth.uid()` always returns the row. Engagement exception: an active daywork or permanent engagement (crew or employer side) on a posting that references the vessel also returns the row — preserves the operational case where admin hides a vessel after an engagement was already booked, so crew can still see vessel data on their engagement card. Both functions retain the existing NDA/owner/engagement reveal logic for `imo_number` + `name` masking — the new filter is upstream of that, deciding whether the row is returned at all. Schema v121 → v122. Five callsites consume these RPCs (`/api/daywork/discover`, `/api/daywork/applications`, `/api/daywork/invitations`, `/api/permanent/applications`, `/api/permanent/discover`) — none required code changes; they already build a `Map<id, row>` and handle missing entries gracefully (same as for deleted vessels). Stress-test script `scripts/stress-test-vessels-v2-wave-f-rpc.ts` exercises the live remote DB: inserts curated + pending + hidden sentinel vessels owned by an arbitrary other person, calls both RPCs as service-role (auth.uid() = null = non-owner), asserts only curated rows come back. 6/6 stress checks pass. Why pending+hidden filter on side doors matters: a hidden vessel that was previously a posting subject would still surface on every daywork/permanent discover card the moment a non-owner refreshed; without this migration the moderation action only covers the front-door lookup. Owner reveal + engagement reveal verified by code inspection (their structure mirrors the 00083 NDA reveal which existing tests exercise).

- [Fix 253] Vessels V2 — Wave F (partial): filter `source='pending'` and `hidden_at IS NOT NULL` rows out of `/api/vessels/lookup` for non-owners. Both code paths updated: (1) **Partial search (4–6 digit IMO prefix)** — SELECT now includes `source, hidden_at, owner_person_id`; the DB query fetches up to 20 rows then a JS filter rejects any vessel where `hidden_at IS NOT NULL` (unless caller is the owner) or `source='pending'` (unless caller is the owner); the visible set is sliced to the previous 5-result cap. (2) **Exact search (7 digit IMO)** — refactored from `.eq().limit().maybeSingle()` to `.eq()` returning the full match array, then `find()` for the first row matching `!hidden_at && (curated || user_submitted || owner_person_id === caller)`. Submitter still sees their own pending vessels (so they can attach a follow-on experience entry / posting before admin approval). Everyone else gets `found: false` for hidden-from-them rows — same UX as a vessel that genuinely doesn't exist. **Why this is integrity-critical:** Wave C creates vessels with `source='pending'` and the `vessels.imo_number` partial unique index is `(imo_number, owner_person_id)` (not just `imo_number`), so two crew can submit pending requests for the same IMO without collision; without this filter the second submitter would see the first submitter's vessel name + LOA + flag in the lookup, leaking PII attached to the still-private submission. 4 new tests in `vessel-lookup.test.ts`: partial search filters out other-user pending + hidden rows; partial search includes the caller's own pending submission; exact lookup hides another user's pending vessel from the caller; exact lookup returns the caller's own pending vessel; exact lookup hides hidden vessels from non-owners. 3 existing tests updated to the new mock pattern (`.eq()` returning array + new fields on fixture rows). 1279 tests pass (+4). **Deferred follow-ups:** `get_vessel_public` and `get_vessels_public_batch` RPCs (used by chat / discover / mine summaries) still need the same filter applied so a hidden vessel stops appearing on cards downstream of acceptance; date-overlap historical name + flag display logic (resolve which `vessel_names` / `vessel_flag_states` row was current during a given experience window). Tracked separately because they're display-only and don't leak unapproved data.

- [Fix 222m] Telegram broadcast pipe — query consolidation. `apps/web/src/lib/push-triggers/broadcast.ts` previously fired ~15 DB queries per single-posting daywork broadcast: same `cityId` looked up 5 times for city name, same `dayworkId` queried 3 times for different field subsets, separate `yacht_roles` lookup, `getJobNumber()` called 3 times, `notification_channels` and `user_preferences` queried twice each (once for Telegram, once for WhatsApp). Refactored into a single `Promise.all` with: (1) one `cities` query, (2) one rich `dayworks` query joining `yacht_roles:role_id(name)` and selecting all fields needed by push body + WA template + Telegram body in one shot, (3) merged `notification_channels` query using `.in('channel_type', ['telegram', 'whatsapp'])` then split client-side, (4) merged `user_preferences` query selecting both flags. Single-posting broadcast: ~15 → 5 queries. Collapsed broadcast (multiple postings): ~7 → 4 queries. Inline `formatJobNumber` + `formatDayMonth` helpers replace per-call `getJobNumber` and date formatting, computed once and reused. Push notification body, WhatsApp template variables, and Telegram message body now derive from a single `DayworkContext` struct. Two new regression-guard tests in `push-triggers.test.ts` count `from()` calls per table during a broadcast and assert each table is hit at most once. 1192 tests pass (+2). Behavior unchanged: same notification content, same dispatch priority (Telegram > WhatsApp > push), same poster exclusion, same 60s collapse window.

- [Fix 222n] Lazy-load ImageCropper to keep `react-easy-crop` (~24.6 KB chunk) out of the eager `/profile` and `/onboarding` bundles. `avatar-upload.tsx` previously did a static `import { ImageCropper } from '@/components/image-cropper'`, which pulled `react-easy-crop` into the page bundle even though the cropper only renders after the user picks a file. Switched to `next/dynamic` with `ssr: false` — `react-easy-crop` now lives in its own chunk (`283c470e46e07fc9.js`, 24.6 KB confirmed via `react-loadable-manifest.json` for `/profile` and `/onboarding` routes). On first paint of those routes the chunk is no longer downloaded; it loads only when the user clicks the camera icon and selects a file. No behavior change, no API change, no test changes — `dynamic()` resolves to the same component with the same props. 1192 tests still pass. Future bundle-size wins identified but deferred: `framer-motion` is imported by 5 files on hot routes (messages, discover card, daywork review tabs) — usage is lightweight per file, lower priority. `dompurify` is already route-scoped to `/docky` via Next.js code splitting, no action needed.

- [Fix 252] Vessels V2 — Wave E: admin curation queue. New admin-only routes: `GET /api/admin/vessels/pending` returns pending vessels with parent context (size band label, flag state name, every admin-enrichment column) plus the submitter's display name via a single batched profiles lookup; `POST /api/admin/vessels/pending/[id]` accepts `{ action: 'approve' | 'merge' | 'hide', mergeToId?, name? }`. **Approve** flips `vessels.source` from `'pending'` to `'curated'` AND promotes every still-pending row in `vessel_names` and `vessel_flag_states` in the same call (so admins don't have to chase the history tables manually); optional `name` rewrites both `vessels.name` and the seeded vessel_names row. **Hide** stamps `hidden_at = now()` so the submitter keeps seeing the vessel via their FK but everyone else's lookup excludes it (Wave F's `get_vessel_public` filter). **Merge** validates the target is curated or user-submitted (refuses pending → pending), re-points three FKs (`crew_experiences.vessel_id`, `dayworks.vessel_id`, `permanent_postings.vessel_id`) to the canonical row, then deletes the pending row — CASCADE on `vessel_id` sweeps `vessel_names` + `vessel_flag_states` automatically. New `/admin/vessels/pending` page lists each pending vessel with M/Y or S/Y prefix, IMO, builder, full specs row (LOA / size band / flag / year / GT), submitter, recency, and three action buttons with hover-tooltips spelling out exactly what each does. Approve uses `prompt()` so admins can fix typos in one step. Merge opens a sub-dialog wrapping `/api/vessels/lookup?imo=` so admins find canonical matches by typing the IMO; pending row is filtered out of the candidate list. Sidebar gets a "Pending vessels" entry. 12 new tests in `admin-vessels-pending.test.ts` covering 403 / invalid UUID / unknown action / 409-when-not-pending / approve no-rename / approve with name override / hide / merge requires-mergeToId / merge ripple in correct order / merge rejects pending target. 1275 tests pass (+12). No schema or projection changes — Wave E is application-layer only on top of Wave A schema and Wave B projection.

- [Fix 251] Vessels V2 — Wave D: wire `<AddVesselDialog>` into entry points. `ImoLookupSection` extended with optional `onAddManually` prop; when provided AND the IMO lookup explicitly returns "Not found" or "No vessels found", the section renders an "Add it manually" button below the search input. Two parent pages wired this session: (1) `/vessels` standalone manager — `CreateVesselForm` opens the dialog on the manual-add button and refreshes the vessel list on success; (2) `/profile/add-experience` — the dialog's `onSubmitted` callback adopts the new (pending) vessel into the experience form as if it had been a canonical-search hit (sets `existingVesselId`, `useExisting=true`, fills name/type/LOA + handles ft/m unit conversion). Daywork-post and permanent-post pages already redirect to `/vessels` via the existing `saveFormAndCreateVessel` pattern, so they inherit the manual-fallback flow indirectly through the updated `/vessels` page (one extra click vs. an in-place dialog — acceptable for v1; a future commit can wire the dialog directly into those pages). 1263 tests pass. Lint + build clean. No schema or API changes.

- [Fix 250] Vessels V2 — Wave C: manual request flow. New `POST /api/vessels/request` accepts `{ imo_number, name, vessel_type, loa_meters, nda_flag?, flag_state_id?, year_built?, builder?, gross_tonnage?, beam_meters? }`, validates each field (IMO 7 digits, vessel_type ∈ {motor, sail}, LOA 1–200, year_built 1850–2100, gross_tonnage > 0, beam_meters 0–100, builder length cap, optional flag_state FK validated), per-user IMO uniqueness via `(imo_number, owner_person_id)`, derives the size band from LOA. Then it fires `VESSEL.CREATED` with `source='pending'` (the projection seeds `vessel_names` + stamps `submitted_by`); if a flag is provided fires `VESSEL.REFLAGGED` with `source='pending'`; if any enrichment fields are set fires a single bundled `VESSEL.METADATA_UPDATED`. Finally fans an in-app notification out to every active admin (type `admin_vessel_pending`, deep-linked to `/admin/vessels/pending`) via the new `notifyAdminsOfVesselRequest` helper — fire-and-forget, mirrors the locations pattern. New `<AddVesselDialog>` (`apps/web/src/components/add-vessel-dialog.tsx`) renders an overlay form with IMO + name + vessel type + LOA at the top and an "Add more details" toggle revealing flag state, year built, builder, gross tonnage, beam — keeps the common path short. Loads `flag_states` once on first open, prefills IMO from `initialImoNumber`, ESC closes, on success calls `onSubmitted({ id, imoNumber, name, vesselType, loaMeters })` so the parent at each entry point (Wave D) can hand the new vessel id straight back into its existing form. 10 new unit tests covering 401 / 400 (IMO / name / type / LOA / year_built) / 409 (duplicate) / happy path / REFLAGGED-when-flag-given / METADATA_UPDATED-when-enrichment-given. 1263 tests pass (+10). No schema changes — relies on Wave A schema and Wave B projection handlers.

- [Fix 249] Vessels V2 — Wave B: projection wiring + event types. Migration `00121_vessel_history_projection.sql` adds three new handlers to `apply_projection`. **VESSEL.RENAMED** closes the previously-open `vessel_names` row with `effective_to = effective_from - 1 day` and inserts a new (open-ended unless explicit `effective_to` for admin back-fills) row, then updates the denormalised `vessels.name` if the new row is current. **VESSEL.REFLAGGED** is the same pattern for `vessel_flag_states`. **VESSEL.METADATA_UPDATED** writes the four enrichment columns with `case when payload ? 'field' then ... else field end` semantics — explicit null clears, omitted preserves. **VESSEL.CREATED** extended to write the new `source` + `submitted_by` columns and seed an open-ended `vessel_names` row so the timeline always has data from day one. Both close-the-previous-row UPDATEs use `GREATEST(effective_from, new_from - 1 day)` so a same-day rename produces a zero-length interval instead of underflowing the `effective_to >= effective_from` CHECK constraint (caught by the stress test the first time around). Lessons-mandated apply_projection checks pass: `$$` count = 2, function ends with `end case; end; $$;`, handler count went from 68 (in 00116) to 71. `packages/types/src/events.ts` extended: three new EventType union members + payload shapes for RENAMED (`{name, effective_from?, effective_to?, source?}`), REFLAGGED (`{flag_state_id, effective_from?, effective_to?, source?}`), METADATA_UPDATED (`{gross_tonnage?, beam_meters?, year_built?, builder?}` — all nullable to support the explicit-null clear semantics). VESSEL.CREATED payload extended with optional `source`. New `scripts/stress-test-vessels-v2-wave-b.ts` exercises every handler against the live remote DB end-to-end via `append_event` RPC: VESSEL.CREATED writes vessels + seeds vessel_names + sets source/submitted_by, RENAMED handles different-day (close=day-before-new) AND same-day (GREATEST clamp produces zero-length) cases, REFLAGGED writes flag history with proper closure, METADATA_UPDATED handles full update + partial preservation + explicit-null clearing. 37/37 stress checks pass. Rollback for 00121 emits a NOTICE pointing operators at re-applying 00116 (the schema additions are owned by 00120's rollback). 1253 tests still pass. Schema v120 → v121.

- [Fix 248] Vessels V2 — Wave A: schema prep. Migration `00120_vessel_history_v1.sql` adds the temporal name + flag layer the IMO-as-anchor invariant requires. New columns on `vessels`: `gross_tonnage`, `beam_meters` (CHECK > 0 and < 100), `year_built` (CHECK 1850–2100), `builder`, `flag_state_id` (denormalised "current" flag, FK to `flag_states.id text`), `source` (`curated`/`user_submitted`/`pending`, default `curated`, CHECK), `hidden_at`, `submitted_by` (FK to `persons.id`, ON DELETE SET NULL). Two new history tables `vessel_names` and `vessel_flag_states` — each row is one (vessel, value, [effective_from, effective_to)) interval; `effective_to is null` is the current value. Interval CHECK rejects `effective_to < effective_from`. RLS: authenticated SELECT; writes flow through `apply_projection` (Wave B). Indexes: per-vessel `(effective_from desc, effective_to)` for overlap lookup (Wave F's display path), trigram GIN on `vessel_names(name)` for fuzzy historical search ("Sea Wolf" finds a hull currently named "Black Pearl"), partial indexes on `(created_at desc) where source='pending'` on all three tables (Wave E admin queue). Existing 19 vessels back-filled with one open-ended `vessel_names` record each so display logic has data immediately; flag history left empty since current `vessels` row has no flag column populated. New `scripts/stress-test-vessels-v2-wave-a.ts` exercises the live remote DB end-to-end: column existence, source CHECK accepting all 3 values + rejecting garbage, year_built / gross_tonnage / beam_meters CHECKs, flag_state_id FK enforcement, vessel_names interval CHECK, vessel_names + vessel_flag_states column shape, CASCADE delete on `vessel_id` FK sweeps history. 23/23 pass. Schema v119 → v120. **Wave A is the foundation only.** Waves B (`VESSEL.RENAMED` / `VESSEL.REFLAGGED` / `VESSEL.METADATA_UPDATED` event types + projection migration 00121), C (`/api/vessels/request` route + admin notification + manual-add modal), D (wire `<AddVesselDialog>` into the four entry points), E (`/admin/vessels/pending` queue with Approve / Merge / Hide), and F (display logic — resolve vessel name + flag by experience-date overlap) follow. 1253 tests pass.

- [Fix 247] Date + time picker visual upgrades. (1) **Date picker** — new `apps/web/src/components/ui/calendar.tsx` (~200 LOC, no new dependency) renders a click-to-select month grid with prev/next navigation, today marker, selected-day highlight, and disabled days outside `[min, max]`. `DateInput` now wraps the calendar icon in a `Popover` that anchors a `<Calendar>` in `PopoverContent`. The dd/mm/yyyy text input is preserved as the keyboard path (typed entry still emits `onChange` only after a complete valid date); the icon flips into an overlay calendar instead of a hidden native `<input type="date">`. All 11 existing `DateInput` callsites inherit the change automatically (daywork post, permanent post, experience forms, onboarding, discover filters, postponement overlay, etc.). Radix Popover handles collision avoidance / flip placement out of the box. Decision rationale: avoided `react-day-picker + date-fns` (~100 KB combined) because the calendar lands on every form route and a 5-feature month grid is small. (2) **Time picker** — new `apps/web/src/components/ui/time-picker.tsx` (~210 LOC, no new dependency) replaces the single `<input type="time">` callsite at `checklist-form-overlay.tsx:215`. iOS-style scroll-wheel UX via CSS `scroll-snap-y mandatory` — two side-by-side scrollable columns (hours 0–23, minutes by configurable `minuteStep`, default 15). Centerline highlight band, top + bottom spacers so the first / last value can sit centered, scroll listener reads the centered row index and emits `HH:mm` (debounced 100ms so momentum-scrolling doesn't fire intermediate values). Output format unchanged so the rest of the checklist form / event payload doesn't need to know about the upgrade. 12 new tests (6 calendar — month rendering, day selection, min/max disable, prev/next nav, external value sync; 6 time picker — column rendering, minuteStep filtering, debounced scroll → onChange, no-op when value matches, malformed-value clamping). 1253 tests pass (+12). Production build clean. No schema changes, no API changes.

- [Fix 246] Locations V1 follow-ups (Stage 217 audit list, final three items). (1) **OSM port search sanity pass** — `scripts/sanity-test-osm-port-search.ts` samples 20 random ports with `osm_id IS NOT NULL` from the live DB, runs each name through `search_locations`, and asserts the port appears with non-null `parent_name` (city) + `country_code`. 20/20 pass against production. Catches the silent-failure mode where the picker would render a marina with no surrounding context. (2) **Marina alias config externalised** — `supabase/seed/marina_aliases.json` now holds the `country_code_fixes` + `town_aliases` tables (1 fix + 41 aliases across 8 countries) that `scripts/marina-extraction/3c_normalize.py` previously hard-coded. The Python script reads the JSON via a new `load_aliases()` helper. Future hub additions are a one-file diff with no Python changes. (3) **Admin OSM-merge runbook** — `tasks/admin-locations-runbook.md` documents Approve / Merge / Hide semantics for `/admin/locations/pending`, lists the recurring near-duplicates (Bodrum districts, UAE districts, Caribbean district names, Antibes-Juan-les-Pins, Palma de Mallorca → Palma) admins should merge rather than approve as separate canonical rows, calls out the Muğla ambiguity (province spans Bodrum / Marmaris / Fethiye / Göcek — Hide and ask user to resubmit), and includes the failure-mode list from Wave D's known limitations (ports `(city_id, name)` unique violation, agent_placement_cities `(person_id, city_id)` violation, non-transactional ripple). 1241 tests still pass. No schema changes.

- [Fix 245] Locations V2 — stress-test pass + canonicalize hardening. Stress-test script `scripts/stress-test-locations-v2.ts` exercises the live remote DB end-to-end: schema columns, CHECK constraint accepting `'pending'`, default values, all three RPCs (`search_locations` excluding pending, `top_locations` excluding pending ports, `get_locations_by_ids` intentionally NOT excluding pending so submitter FKs resolve), `hidden_at` filter, and the nested PostgREST embed shape used by `/api/admin/locations/pending` (`cities → regions`, `ports → cities → regions`). 25/25 checks pass against the live database. The script is idempotent — inserts a sentinel-named pending city + port, asserts behaviour, deletes them. Catches the class of drift mocked unit tests can't see (real CHECK constraints, real RLS, real RPC bodies, real FK ripples). **Hardening:** `/api/locations/canonicalize` now caps `name` at 200 chars, caps `country_name` at 200 chars, and rejects out-of-range `latitude` / `longitude` (defence-in-depth — Nominatim returns sane values, but a buggy / malicious client must not be able to insert megabyte names or impossible coords). 2 new unit tests for the new validation. 1241 tests pass (+2). Production build clean (8 new routes registered: `/admin/locations/pending` page + 7 API routes). No schema changes.

- [Fix 244] Locations V2 — Wave D: admin curation queue. Migration `00119_locations_admin_queue_columns.sql` adds `created_at timestamptz default now()` and nullable `submitted_by uuid references persons(id) on delete set null` to `cities` and `ports`, plus partial indexes `(created_at desc) where source='pending'` on both. Schema v118 → v119. `/api/locations/request` now stamps `submitted_by` on every pending insert and fires a fan-out admin notification via the new `notifyAdminsOfLocationRequest` helper (mirrors `notifyAdminsOfSupport` from Fix 231) — type `admin_location_pending`, deep_link `/admin/locations/pending`, body `'<submitter> added <name>[ (<port>)] in <country>'`. Notification fan-out is fire-and-forget, swallowed if it fails. New admin-gated routes: `GET /api/admin/locations/pending` returns `{ cities, ports }` with parent-chain joins (region → city → region) and a single batched lookup of submitter `display_name`s; `POST /api/admin/locations/pending/cities/[id]` and `/ports/[id]` accept `{ action: 'approve' | 'merge' | 'hide', mergeToId?: string, name?: string }` — all three actions guard the row's current `source='pending'` state and return 409 otherwise. Approve flips `source` to `'curated'` (with optional `name` rewrite). Hide stamps `hidden_at = now()`. Merge requires `mergeToId` of a curated/OSM row, then re-points every FK in dependency order — for cities: `ports.city_id` → `availability_windows.city_id` → `profiles.location_city_id` → `agent_placement_cities.city_id` → DELETE pending; for ports: `profiles.location_port_id` → `dayworks.location_port_id` → `daywork_templates.location_port_id` → `availability_windows.port_id` → `permanent_postings.port_id` → `permanent_templates.port_id` → DELETE pending. New page `/admin/locations/pending` (under the existing `(admin)/admin/` route group + sidebar nav entry "Pending locations") renders two tables (Cities, Ports) with parent chain, submitter, recency, and three buttons per row — Approve uses a `prompt()` so the admin can fix typos, Hide uses `confirm()`, Merge opens a sub-dialog wrapping `/api/locations/search` to pick the canonical match (filtered by row kind + excluding the pending row itself). 13 new tests in `admin-locations-pending.test.ts` (2 GET, 11 cities/ports POST): forbidden, invalid UUID/action, 409-when-not-pending, approve flips source, approve accepts name override, hide stamps timestamp, merge requires `mergeToId`, merge re-points the four-table city ripple in order then deletes, merge re-points the six-table port ripple in order then deletes. 1239 tests pass (+13). Locations V2 (Waves A–D) complete.

- [Fix 243] Locations V2 — Wave C: manual "Request this location" flow. Migration `00118_locations_pending_search_filter.sql` replaces `search_locations` and `top_locations` with versions that filter cities + ports to `source IN ('curated', 'osm')` AND `hidden_at IS NULL`, so any pending submission stays invisible to everyone except the submitter (whose FK still resolves through the unfiltered `get_locations_by_ids` RPC). Schema v117 → v118. New `POST /api/locations/request` accepts `{ country_code, country_name?, city_name, port_name?, notes? }`, validates inputs (2-letter country code, length caps, required fields), then service-role: (1) finds-or-creates a region by `country_code` (sort_order 999 for new regions), (2) finds-or-creates a city under that region by `(region_id, ilike(name))` — reuses an existing curated/OSM/pending row if it matches, otherwise inserts with `source='pending'`, (3) optionally finds-or-creates a port under the city with the same find-or-create + `source='pending'` logic. Returns `{ cityId, portId? }`. New `GET /api/locations/regions` returns the canonical regions list ordered by sort_order then name — used by the modal's country picker. New `<LocationRequestModal>` component (`apps/web/src/components/location-request-modal.tsx`) renders an overlay form with country select, city input, port input (mode-gated required/optional), notes textarea, plus inline validation. `LocationPicker` now renders an "Add it manually" CTA whenever canonical AND OSM fallbacks both come back empty (or in `port-required` mode where canonical comes back empty — OSM is skipped there because v1 canonicalize creates cities, not ports). On submit, the picker primes the label cache from the modal's display label so the trigger renders the user's typed text immediately, then calls `onValueChange` with the returned `{ cityId, portId? }`. 9 new tests in `locations-request.test.ts` (7 request, 2 regions): unauth, missing/invalid country code, missing city, region+city+port creation chain, region reuse + city reuse, regions ordering. 1226 tests pass (+9). Wave D follows: admin notification on every pending insert + `/admin/locations/pending` queue with Approve / Merge / Hide actions.

- [Fix 242] Locations V2 — Wave B: OSM Nominatim live fallback. New `GET /api/locations/search-external?q=` calls `https://nominatim.openstreetmap.org/search` with a custom `User-Agent` (per Nominatim usage policy), `addressdetails=1`, `limit=8`, `accept-language=en`, normalises the response to `{ osm_id, osm_type, name, country_code, country_name, latitude, longitude, place_type, display_name }`, filters to supported place types (`city` / `town` / `village` / `harbour` / `port` / `marina`), enforces a best-effort 1-req/sec in-memory rate limit per function instance, and swallows network/parse/timeout errors to an empty result set so the picker never blocks. New `POST /api/locations/canonicalize` accepts that normalised shape and idempotently upserts via service-role: (1) finds or creates a `regions` row by `country_code` (sort_order 999 for new regions; falls back to a singleton "Unknown" region when no country code is present), (2) looks the city up first by `osm_place_id` (formatted `R12345` / `W12345` / `N12345`), then by `(region_id, ilike(name))`, returning the existing id without mutating curated rows, (3) inserts a new city with `source='osm'`, `osm_place_id`, lat/lng + sort_order 999 if neither lookup hits — with a 23505 race guard that re-queries by `osm_place_id` if a concurrent insert wins. `LocationPicker` extended: when canonical search returns zero hits AND query is ≥3 chars AND mode is `port-optional`, the picker fires the external search and renders a "Found in OpenStreetMap" section under the "no match" empty state. Click → POST canonicalize → primes the label cache from the OSM result (no `/by-ids` round-trip needed) → calls `onValueChange({ cityId })` and closes. `port-required` mode skips the OSM fallback because v1 canonicalize creates cities, not ports — Wave C's manual request flow will handle new port submissions. 11 new tests in `locations-external.test.ts` (5 search-external, 6 canonicalize): unauth, empty query, place-type normalisation + filtering, fetch-error swallowing, rate-limit cooldown, invalid body, idempotent osm_place_id match, idempotent (region, name) match, fresh region+city insert, "Unknown" region fallback. 1217 tests pass (+11). No schema changes — Wave B is API + UI only.

- [Fix 241] Locations V2 — Wave A: schema prep. First wave of a three-layer fallback for "user can't find their location" (canonical search is Layer 0, already live). Migration `00117_locations_pending_v1.sql` extends `cities.source` and `ports.source` CHECKs to allow `'pending'` (alongside the existing `'curated'` and `'osm'`), and adds `hidden_at timestamptz null` to both tables. Pending rows live in the same canonical tables — the search RPC's existing `source != 'pending'` filter (added explicitly in the upcoming Wave B) excludes them from other users' searches, while the submitting user's FK keeps resolving normally. `hidden_at` supports the admin "Hide" action in Wave D — keeps the row resolvable for the submitter without surfacing it elsewhere. Rollback self-contained (drops `hidden_at`, restores two-value CHECKs); any rows with `source='pending'` must be cleaned up before rollback or the CHECK re-add will fail. Schema v116 → v117. No application code changes in this wave; Wave B (OSM Nominatim live fallback routes + picker integration), Wave C (manual "Request this location" form + route), and Wave D (admin notification + `/admin/locations/pending` curation queue) follow.

- [Fix 240] Multi-nationality phase 2 — full wiring. Phase 1 (Fix 234a) added the `profiles.nationality_ids uuid[]` column + GIN index + backfill but no readers/writers used it; users with British + South African passports could only show one flag. This phase wires the full stack so dual-passport holders see both flags everywhere. **Schema/projection** (migration `00116_multi_nationality_projection.sql`): CREATE OR REPLACE `apply_projection` based on the 00108 body, with PROFILE.CREATED + PROFILE.UPDATED handlers updated to write `nationality_ids` (accepts either new array or legacy single field; dual-writes the legacy `nationality_id` column as the first element for backward compat during the transition). **Event types** (`packages/types/src/events.ts`): added `nationality_ids?: string[]` to PROFILE.CREATED and PROFILE.UPDATED payloads; legacy `nationality_id` field marked `@deprecated`. **API** — `/api/onboarding` accepts `nationalityIds: string[]` (preferred) or legacy `nationalityId`; `/api/profile` GET attaches `nationalities_all` (batch lookup of all rows referenced by `nationality_ids`) so the client renders multiple flags without an extra round-trip; PATCH validates and accepts the array; `/api/profile/[personId]` adds the same `nationalities_all` enrichment for cross-user views; `/api/permanent/[id]/review` adds `nationality_flags: string[]` per applicant via a single batched `nationalities` query inside the existing Promise.all (zero extra round-trip); `/api/daywork/[id]/applicants` and `/api/daywork/[id]/available-crew` extend their SELECTs to include `nationality_ids` (multi-flag rendering on those routes deferred to a follow-up — column available, render code unchanged). **UI** — `SearchableNationalitySelect` rewritten as multi-select: `value: string[]`, removable chip pills above the search input, click-to-toggle in the dropdown, optional `max` cap. Profile page state changed `nationalityId` → `nationalityIds`. Edit form, onboarding profile-step, profile summary section all updated; summary section renders multiple flag emojis side-by-side with a "Nationalities" label (singular/plural switched). Onboarding validation requires at least one nationality (was: required `nationalityId` non-empty; now: `nationalityIds.length > 0`). **Tests**: profile.test.ts asserts `nationalities_all: []` in the GET response shape (new field always emitted). 1206 tests pass. Schema v115 → v116. Legacy `nationality_id` column kept until all clients confirmed migrated; future migration drops it.

- [Fix 239] Applicant card cert "extras" indicator (third state). User feedback: green/orange covers match/missing — but a candidate holding _bonus_ certs above the spec deserves its own signal. Especially relevant on permanent (where the hard-gate ensures every visible candidate is green for cert_match), so green-only renders the same for everyone. Added `cert_extras: number` to the `/api/permanent/[id]/review` response — count of candidate certs that are NEITHER directly required NOR a bundle covering a required component. Computation runs per-applicant: `usedForRequired` set collects every candidate cert that contributes to a requirement (direct match OR bundle whose components include a required cert); extras = remaining candidate certs. Rendered as a third badge `"+ N additional"` in `--primary` (brand blue), inline with the existing green/orange match badge. Both badges can render together (candidate exceeds spec) or independently (no required certs but candidate has bonus certs). Test mock updated to assert `cert_extras: 0` for the empty-required scenario. 1206 tests pass.

- [Fix 238] Permanent applicant card enrichment. User feedback: card on `/permanent/[id]/review` was "way too thin and completely indistinguishable in terms of evaluation compared to the next candidate" — only role + experience-bracket + nationality flag + language count + shore-experience tags rendered. Forced employers to open the profile overlay for every one of 100+ candidates. Card now shows five additional triage signals (most are zero-data-cost — already returned by the API or trivial to add). (1) **Total sea time** — `computeTotalExperience` from `@dockwalker/shared` over a batched `crew_experiences` query (`.in('person_id', crewIds)`), rendered as `"6y 3m sea time"` in the header row; falls back to the auto-derived bracket label if the candidate has no experience entries. (2) **Vessel-size exposure range** — `vessel_size_exposure_ids` (already in API SELECT, just unused) computed against `useLookups().sizeBands` via a `vesselSizeRange` helper that mirrors the profile-summary-section behaviour (`"30-90m"`, `"90m+"` for open-ended top tier). (3) **Location** — `port_name` + `city_name` (already in API) rendered with a 📍 prefix under the header. (4) **Bio preview** — `bio` (already in API, just unused), rendered above the application message via the existing `ExpandableText` component, max 2 lines. (5) **Cert match indicator** — bundle-aware (Fix 236): API runs `meetsRequirements(candidateCerts, requiredCerts, bundlesMap)` per applicant. Card renders either `"✓ All N required certs"` (success-tinted) or `"⚠ M/N certs · K missing"` (warning-tinted). Bundle attribution from the `satisfiedVia` map preserved server-side for a future "via your AEC 1+2" footnote. API additions: `crew_experiences` batch query and `certification_components` bundle fetch run in `Promise.all` alongside the existing `shore_experiences` query so total round-trip is unchanged. The bundles fetch is gated on `requiredCerts.length > 0` so postings with no cert requirements skip the query. Test mocks for `permanent-review.test.ts` updated to mock the new queries; `posting` row mock now includes `required_certification_ids: []`. 1206 tests pass.

- [Fix 237] Profile overlay "Profile unavailable" — relationship check #4 PostgREST embed-or quirk. User reported clicking the profile icon on a permanent applicant card opened the overlay with "Profile unavailable" + Close. Cause: `checkRelationshipContext` in `apps/web/src/app/api/profile/[personId]/route.ts` check #4 (permanent application context) used `or()` filter with embedded-table column references (`permanent_postings.employer_person_id.eq.X`) inside `and(...)` clauses. PostgREST's combination of `!inner` embed + `or` containing `embed.field.eq.value` filters is fragile in production — appears to silently miss matches in some scenarios, returning `[]` even when valid permanent application rows exist. Refactored to a two-step explicit query: (1) fetch any permanent applications involving either party (no embed), (2) fetch the postings for those applications, (3) JS-side cross-pair check (`app.crew = target AND posting.employer = requester`, OR `app.crew = requester AND posting.employer = target`). More verbose but reliably matches production state. 1206 tests pass.

- [Fix 236] Certification bundles — launch-blocker fix. **Real maritime training is sometimes sold as a single bundled certificate covering multiple individual STCW competencies.** Examples: AEC 1+2 (single MCA cert covering both AEC 1 and AEC 2 modules); STCW 95 / STCW 2010 (single basic safety cert covering five A-VI competencies). Without bundle awareness, a candidate holding the bundle cannot apply to a job listing the components separately — direct UUID intersection fails. User reported this in practice: "Why should I look at a job that says AEC1, AEC2 individually, I have stated I have AEC1+2 (because they are bundled as a two-part 'AEC' certificate) but I cannot apply and therefore to assert truth that is already there I now have to put AEC1, AEC2, AEC1+2 all in my profile. It's not only inflating my certificate count - it is duplicating truth." Fixed: (1) migration `00115_certification_bundles_v1.sql` adds `certification_components` junction table, seeded with AEC 1+2 → AEC 1, AEC 2 and STCW 95/2010 → 5 components per A-VI Code (PST, FPFF, EFA, PSSR, Medical First Aid). (2) Shared util `packages/shared/src/cert-matching.ts` exports `meetsRequirements(candidate, required, bundles)`, `expandCertCoverage`, `expandCertForFilter`. Strict v1 direction: bundle → components only (a candidate holding a bundle satisfies any required component, but holding only the components separately does NOT auto-satisfy a required bundle — explicit). The result includes a `satisfiedVia` map (required cert id → bundle cert id that covers it) for UI attribution like "AEC 1 ✓ (via your AEC 1+2)" — wiring the UI footnote is deferred. (3) Permanent apply hard-gate (`apps/web/src/app/api/permanent/[id]/apply/route.ts`) refactored from direct `Set.has` intersection to `meetsRequirements` — fetches `certification_components` in parallel with profile via `Promise.all`. Cert block 403 now correctly excludes candidates whose bundles cover the missing components. (4) Discover feed filters (`/api/permanent/discover` and `/api/daywork/discover`) — when a candidate filters by a bundle cert, the query expands to bundle + components and uses `.overlaps` instead of `.contains` so jobs requiring any of those certs surface. New unit-test file `__tests__/lib/cert-matching.test.ts` covers direct match, bundle-expansion, strict direction, mixed, legitimate miss, empty inputs, direct-takes-precedence-over-attribution. Permanent-apply integration test gains a `'cert hard-gate satisfied via bundle'` case. Daywork-discover tests updated to mock the new `certification_components` query and assert `.overlaps` instead of `.contains`. Schema v114 → v115. 1206 tests pass (+14 new).

- [Fix 235] Two small UI fixes from user feedback. (1) **HierarchicalPills cross-department highlight asymmetry**: when a role belongs to multiple departments (`Deck/Engineer` is `department='deck_engineering'` in `yacht_roles`, which `rolesToGroups` splits into both `deck` and `engineering` groups), selecting it from the **Engineering** tab highlighted both pills, but selecting from **Deck** highlighted only Deck. Cause: `selectedGroupId` (single string) returned the first alphabetical group containing the role (`deck` < `engineering`). When user clicked from the Engineering tab, `expandedGroup='engineering'` lit it via `isExpanded` AND `selectedGroupId='deck'` lit Deck via `hasSelection`. From the Deck tab, `expandedGroup='deck'` lit only Deck because `selectedGroupId='deck'` doesn't match `'engineering'`. Fixed by replacing the single string with `selectedGroupIds: Set<string>` containing every group that holds the role; `hasSelection` now checks `.has(group.id)`. Symmetric in either direction. (2) **Vessel-size summary rendering "NaN-NaNm"**: Fix 233 widened `SizeBandLookup` to include `min_meters`/`max_meters` and the SELECT in `apps/web/src/hooks/use-lookups.tsx` to fetch them, but didn't bump the `localStorage` cache key (`dw-lookups-v3`). Existing browser caches still held the v3 shape (id+label only) → `s.min_meters` resolved to `undefined` → `Math.min(undefined)` = NaN → summary rendered `"NaN-NaNm"`. Bumped to `dw-lookups-v4` so stale clients drop the cache on next mount. Also added a defensive guard in `vesselSizeRange` to skip rows with non-numeric min/max — protects against future schema drift causing the same class of bug. 1192 tests pass.

- [Fix 234a] Multi-nationality schema prep — phase 1 of 2. User feedback: dual-passport holders (e.g. British + South African) want both flags shown on their profile, not just one. Single `profiles.nationality_id` column doesn't model that. Tonight ships the safe slice: migration `00114_multi_nationality_v1.sql` adds `nationality_ids uuid[]` column, backfills from existing `nationality_id`, adds GIN index for future filtering. Legacy `nationality_id` kept for backward compat — projection + read-path migration deferred to phase 2 because `apply_projection` is a 490-line function and CREATE OR REPLACE requires the full body, too risky to grind through end-of-day. Phase 2 plan written to `tasks/todo.md` (projection update via 00115, event payload types `PROFILE.CREATED`/`PROFILE.UPDATED` adding `nationality_ids?: string[]`, API routes, multi-select UI, tests). Schema v113 → v114. No code changes tonight beyond the migration — all reads still use the legacy single-value column until phase 2 lands.

- [Fix 233] Discover scroll reset + profile summary refresh — two small UX fixes from user feedback. (1) **Scroll reset on discover mode/tab change**: a user with iOS Safari (address bar at top) reported that switching from Daywork→Permanent in `/discover` left the lone Permanent card scrolled past its title — the longer Daywork list's scroll offset carried over. The role title was rendered correctly; the page just landed at the wrong Y position. Added `window.scrollTo({ top: 0, behavior: 'auto' })` to both the SegmentedToggle `onChange` (Daywork↔Permanent) and the UnderlineTabs `onChange` (Browse/Invitations/Applied) in `apps/web/src/app/(app)/discover/page.tsx`. Defensive — handles scroll restoration, mode-switch retention, and address-bar-position viewport differences regardless of which exact iOS behaviour caused it. (2) **Profile summary line refresh**: previous summary was `"Third Engineer · 1-2 years · 1y 6m · Gocek"` — the auto-derived bracket ("1-2 years") was redundant once precise time onboard ("1y 6m") was tracked. Replaced bracket with computed vessel-size exposure range (e.g. `30-90m`) derived from the union of the user's `vessel_size_exposure_ids` — more actionable signal for employers than the bracket. New summary: `"Third Engineer · 1y 6m · 30-90m · Gocek"`. Edge cases handled: open-ended top tier (max_meters null) renders as `"90m+"`; single band collapses to `"50m"` not `"50-50m"`; no exposure set omits the segment. Required extending `SizeBandLookup` in `apps/web/src/hooks/use-lookups.tsx` to include `min_meters` + `max_meters`, and the SELECT to fetch them; new `sizeBandRanges` memoised map plumbed from `profile/page.tsx` into `ProfileSummarySection`. Bracket still rendered in the EXPANDED summary (different section, still useful). 1192 tests pass.

- [Fix 232b] Onboarding-gate audit follow-on — two more routes with the same trap. After Fix 232 traced the location-picker silent failures to `requireDomainUser` returning 409 mid-onboarding, an audit of every `requireDomainUser` import surfaced two more routes hit by the onboarding flow itself. (1) **`/api/vessels/lookup`** (called by `ImoLookupSection` on the onboarding vessel-experience step — experienced crew typing an IMO) — straight swap to `requireAuthSession`; the route already uses `serviceClient` to bypass vessel RLS so no further changes needed. (2) **`/api/profile/avatar`** POST + DELETE (called by `AvatarUpload` on the onboarding profile step) — trickier because the existing flow read `person.current_hat` and fired a `PROFILE.UPDATED` event, both of which assume an onboarded user. Refactored to a new inline `getCurrentHat(serviceClient, userId)` helper that returns the hat if the persons row exists with one, else null. Pre-onboarding callers get the storage upload + URL return only; the `appendEvent` is gated on `roleContext` being non-null. Post-onboarding behaviour unchanged. The avatar URL flows into the onboarding RPC's initial profile insert via the onboarding page's existing `avatarUrl` state (already wired up at `apps/web/src/app/onboarding/page.tsx:87`), so the skipped `PROFILE.UPDATED` doesn't lose data — the URL is captured by the `PROFILE.CREATED` event instead. `requireAuthSession` extended to also return `serviceClient` (matches `requireDomainUser`'s shape minus the domain-state fields). Test mocks updated: `vessel-lookup` and `profile-avatar` now stub `requireAuthSession`; `profile-avatar` adds a `makePersonsLookup` helper to mock the inline current-hat lookup. 1192 tests pass. Audit confirmed no other onboarding-flow API routes are gated incorrectly: canonical lookups (yacht_roles, certifications, brackets, size_bands, flag_states, nationalities, entry_rights) are read directly from Supabase via the JS client, bypassing the API layer entirely; layout-level pollers (notification counts, push tokens, bell badge) live under `(app)/layout.tsx` which `/onboarding` doesn't share, so they don't run during onboarding.

- [Fix 232] Location picker silent failure during onboarding — root cause + gap-fill cities. **3 users reported failed searches (Valletta, Cape Town, Tortola, Porto Antico).** Vercel logs surfaced the smoking gun: bursts of HTTP **409** on `/api/locations/search` from a single user, then 200s once they completed onboarding. Cause: the three location read routes (`search`, `top`, `by-ids`) all called `requireDomainUser()`, which short-circuits with `409 "Complete onboarding before using this feature"` for any signed-in user without a complete `persons` + `profile` row + `current_hat`. **The location picker is used DURING onboarding** — circular blocker. `safeFetch` returns `{ ok: false }` on 409, picker calls `setSearchResults([])`, UI renders "No match — try the nearest city or port instead." User concludes "fuzzy is broken / city is missing." Real cause: the gate rejected them before the RPC ran. The same explains every prior symptom — desktop worked because that user had finished onboarding already; mobile failed because the test was on a fresh signup mid-onboarding. Fix: new lightweight `apps/web/src/lib/auth/require-auth-session.ts` guard that mirrors `requireDomainUser`'s middleware fast-path (reads `x-user-id` from proxy-set headers) but skips every domain-state check (onboarding/profile/persons/hat). `search`, `top`, `by-ids` swapped over. Cities/ports/regions RLS still requires `authenticated`, which mid-onboarding users satisfy (they have a Supabase session, just no domain rows yet). **Also bundled** (already-shipped seed expansion under same Fix number): migration `00113_locations_osm_fallback_v1.sql` adds `source`/`latitude`/`longitude`/`osm_place_id` columns to `cities` and `source` to `ports` (prep for future OSM-source rows), plus 11 explicit gap-fill cities reported missing or commonly typed (Valletta MT, Tortola VG, Porto Cervo, Portofino, Capri, Split, Phuket, Monte Carlo, La Condamine, Vilamoura). Schema v112 → v113. Test mocks updated (`__tests__/api/locations.test.ts` now stubs `requireAuthSession` instead of `requireDomainUser`, simpler guard payload — no `person`/`profile`/`serviceClient`). 1192 tests pass. Live OSM Nominatim fallback (originally planned as part of the same fix) deferred — gate removal alone resolves every reported case, OSM is now a long-tail nice-to-have rather than a launch blocker.

- [Fix 231] Support thread notifications — both directions. Bug: when an admin sent a support reply or opened a thread to a user, no notification fired (in-app, Telegram, push, or email) — users only saw the reply if they happened to revisit `/support`. Symmetric gap on the other side: when a user opened a thread or replied, admins had no in-app signal. Cause: the four support routes called `appendEvent(SUPPORT.*)` but no handler existed in the notification dispatch chain — `event-router.ts` had no case for SUPPORT events, so `notifyOnEvent` was never invoked. Fix shipped both directions, with the admin direction taking the simpler shortcut per launch decision (admins only need in-app, no Telegram/email/push). (1) **Admin → User (full notification chain)**: created `apps/web/src/lib/push-triggers/support-handlers.ts` exporting `handleSupportThreadOpened` (gates on `is_admin_initiated === true`) and `handleSupportMessageSent` (gates on `is_platform === true`). Both resolve recipient hat via `current_hat` lookup with safe fallback. Wired into `event-router.ts` switch. Added `'SUPPORT.THREAD_OPENED' → 'support_opened'` and `'SUPPORT.MESSAGE_SENT' → 'support_reply'` mappings to `notification-mapper.ts` plus deep-link templates resolving to `/support/${thread_id}`. Telegram dispatcher case added before default (renders `💬 Message from DockWalker` / `💬 New reply from DockWalker` heading + content preview + CTA button to thread). Email dispatcher gained `SUPPORT.*` to `EMAIL_EVENT_TYPES` and a `supportMessageEmail` template. Admin support routes (`/api/admin/support/initiate`, `/api/admin/support/[threadId]`) replaced direct notification INSERTs with `notifyOnEvent` calls, and added `content_preview` (200 chars) to event payload so handlers can render rich bodies. (2) **User → Admin (in-app only, fire-and-forget)**: created `apps/web/src/lib/push-triggers/support-admin-notify.ts` exporting `notifyAdminsOfSupport` — looks up active admins (`is_admin = true`, not deactivated/blocked), inserts a row into `notifications` for each, distinct `type` (`support_thread_opened_admin` vs `support_reply_admin`) and deep-links to `/admin/support/${thread_id}`. Failures swallowed silently to avoid breaking the user-side route response (event ledger still records SUPPORT.\* for audit). User support routes (`/api/support`, `/api/support/[threadId]`) call `notifyAdminsOfSupport` after `appendEvent`. (3) **Type widening**: `EventPayloadMap` in `packages/types/src/events.ts` extended with optional `content_preview?: string` on both SUPPORT event payloads. `NotifyContext.roleContext` widened to include `'agent'` (was `'crew' | 'employer'`) since support reaches all hats. 1192 tests pass.

- [Fix 230] Document attachment renders via Realtime without refresh. When a crew uploads a document in chat, a `MESSAGE.SENT` event fires with `message_type: 'documents'`. Previously the Realtime listener in `apps/web/src/app/(app)/messages/[engagementId]/page.tsx` only appended the new message to local state — the actual document records (in `engagement_documents`) were fetched separately on page mount + after own upload, so the recipient saw the "Shared 1 document(s)" text bubble but no attachment card until manual refresh. Fix: widened `RealtimeMessage` interface in `use-realtime-messages.ts` to include the `message_type` column (Postgres Realtime already sends it; just wasn't typed). Chat page callback now checks `newMsg.message_type === 'documents'` and calls the existing `loadDocuments()` fetcher. Attachment renders within ~1 frame of the Realtime message arriving. No new dependencies, no API changes. 1192 tests pass.

- [Fix 229] Launch-infrastructure milestones (no code changes — operational/dashboard work). (1) **Vercel project consolidation**: deleted dead duplicate project (`dockwalker_v1_claude_code` — auto-created when the repo was first imported, accumulated only 4 Supabase env vars all duplicated on the keeper). Renamed the keeper from misleadingly-named `dockwalker-staging` to `dockwalker-prod` (the production project hosting `www.dockwalker.io`). Single-project setup: one build per push, one source of truth for env vars, no drift risk. (2) **Resend transactional email live**: added `dockwalker.io` to Resend (Ireland region — closest to Supabase Frankfurt), added 4 DNS records to Namecheap (DKIM TXT, SPF TXT, MX, DMARC TXT — DMARC `p=none` for monitor-only), all 4 verified green in Resend, generated Production API key, scoped to Production-only Sensitive. Live email delivery confirmed end-to-end via the `MESSAGE.SENT` notification chain. (3) **Google Sign-In live**: created OAuth client in Google Cloud Console under nautalink.io org (granted `Project Creator` IAM role first, since Workspace super admin status doesn't auto-translate to Cloud project creation rights). Configured consent screen branding (DockWalker app name + dockwalker.io authorized domain — Google rejected `supabase.co` as an authorized domain since it's not on Google's allowlist; safe to skip per Stripe-style argument that the OAuth client's redirect URI list is what actually validates). Pasted Client ID + Secret into Supabase Auth → Providers → Google, enabled. Identity linking enabled. End-to-end sign-in flow verified. Google account picker shows `hwpcuehqawullzqbmcdv.supabase.co` instead of `DockWalker` — known limitation of Supabase auth callback architecture; fix requires Custom Domain feature ($10/mo Supabase Pro feature, intentionally deferred). (4) **Stripe live cutover**: created live-mode Crew Pro (€4.99) + Employer Pro (€14.99) products + recurring monthly prices in Workbench live mode. Created live webhook endpoint subscribing to `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`. Updated 4 Vercel env vars to live values (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, both price IDs), all marked Sensitive. Hit the apex-vs-www landmine again (`tasks/lessons.md` had documented it from previous occurrence) — webhook URL was `https://dockwalker.io/...` instead of `https://www.dockwalker.io/...`, Stripe got 307s and refused to follow, so first live test charge succeeded but DB never updated. Fixed URL, resent the failed event, app reflected Pro state. Then ran a clean end-to-end cycle (no manual webhook resends): Apple Pay subscribe → real-time webhook → app shows Pro within seconds → Stripe cancel → real-time webhook → app shows Free. Both upgrade and downgrade paths validated with fresh webhook delivery. Refund-without-cancel correctly leaves subscription active until period end (Stripe design, not a bug — documented for support runbook). (5) **One bug fix dropped today as part of testing**: `RESEND_API_KEY` initially scoped to Preview-only by accident — production deployment had no Resend client → emails silently no-op'd. Re-scoped to Production. Documented for future env-var work.

- [Fix 228] Two small post-launch-prep fixes. (1) **Agent notification badge count** — `/api/notifications/count` for `identity_type === 'agent'` users filtered notifications by `role_context = 'agent'`, but several handlers (`handleDayworkApplied`, `handleDayworkWithdrawn`, `handleDayworkRelisted`, `handlePermanentApplied`, etc.) hardcode `roleContext: 'employer'` since agents act as employers. Result: agents always saw 0 in the bell badge despite notifications existing on the notifications page (which doesn't filter by role_context). Fix: removed the role_context filter from the agent branch — agents have one hat with no alt context, so counting all unread is correct. (2) **Telegram salary formatting** — `telegram-dispatcher.ts` had three identical salary blocks rendering `€4000–€4000/monthly` when `salary_min === salary_max`. Now collapses to `€4000/monthly` when min equals max. Used `replace_all` since the three blocks were byte-identical. 1192 tests pass.

- [Fix 227] Discover sub-tabs IA cleanup — three issues bundled. (1) **Invitations doesn't apply to Permanent**: the data model has no permanent invitations (only daywork employers send invitations to crew), but the Discover page showed `Browse · Invitations · Applied` regardless of parent mode. On Permanent, Invitations was a misleading dead-end. Fixed by making sub-tabs conditional on `browseMode`: `Browse · Invitations · Applied` for daywork, `Browse · Applied` for permanent. If user is on Invitations and switches to Permanent, they auto-redirect to Browse. (2) **Empty header on Invitations/Applied**: the Daywork/Permanent toggle was hidden when `activeTab !== 'browse'`, leaving white space. Toggle now persists across all sub-tabs as the page anchor. (3) **Applied list and count didn't filter by parent mode**: `applications` state was a merged daywork+permanent array; `applications.length` was the total. Added `filteredApplications` derived state that filters by `a.type === browseMode`; passed to `AppliedTab` and used as the count badge. So Permanent + Applied now shows only permanent applications (and the count matches). (4) **Counts didn't show on first load**: `loadApplications` and `loadInvitations` were lazy-loaded only when their tab became active, so the `Applied (2)` badge appeared only after clicking Applied. Both now load on mount — counts visible immediately. Filters/Clear buttons remain Browse-only (correct — filters only apply to browsing). 1192 tests pass.

- [Fix 226] Docky scroll-past-content — second pass with iOS Safari fix. Fix 225's `h-svh` change wasn't enough on iOS Safari: `svh` represents the SMALL viewport (with bars visible), so when Safari hides the URL bar during scroll, content can extend past the original layout. The user could still scroll past the input on first load and see Safari's address bar peek out below. Ported the exact mobile pattern from the chat page (`messages/[engagementId]/page.tsx`): `fixed inset-x-0 top-0 bottom-0 z-10` on mobile (locks to viewport edges regardless of which Safari bars are visible) plus `md:static md:h-dvh` on desktop (normal flow with dynamic viewport height). The `pb-[calc(var(--nav-height)+env(safe-area-inset-bottom))] md:pb-0` from Fix 225 is preserved so the input doesn't hide behind the bottom-nav. 1192 tests pass.

- [Fix 225] Docky cross-device sync + scroll-past-content fixes. Two issues. (1) **Assistant message persistence**: `/api/advisor/thread/messages/route.ts` saved the assistant message via fire-and-forget `completion.then(...)` after returning the SSE response. On Vercel serverless, the function terminates as soon as the response stream closes — killing the in-flight INSERT. Result: assistant responses appeared on the originating device (streamed to the browser successfully) but were missing from the database, so other devices fetching the same thread saw only user messages. Fix: wrap the post-stream save in `after()` from `next/server` (Next 15+) which keeps the function alive until the work completes. Three test files (`advisor-messages`, `advisor-usage`, `advisor-personalised`) updated with a `vi.mock('next/server', ...)` stub for `after` since vitest doesn't provide the request context the real implementation needs. (2) **Layout overflow on Docky page**: `<main className="flex min-h-svh flex-col">` allowed the body to grow taller than the viewport, letting the user scroll past the bottom of the messages container into empty white space. Per `tasks/lessons.md` ("Flexbox overflow: always add `min-h-0` on flex-col children that must scroll"), changed `min-h-svh` → `h-svh` (fixed viewport height) and added `min-h-0` to the messages scroll container so flex shrinking works. The bottom-nav offset that previously rode on `sticky bottom-[calc(var(--nav-height)+...)]` on the input wrapper moved to `pb-[calc(var(--nav-height)+...)]` on `main` — sticky positioning had no scroll context once the page stopped scrolling, so the input was hiding behind the bottom-nav. Input wrapper is now a normal `shrink-0` flex child. 1192 tests pass.

- [Fix 224] Auto-growing chat + Docky message input. Both `apps/web/src/app/(app)/messages/[engagementId]/_components/chat-footer.tsx` and `apps/web/src/app/(app)/docky/page.tsx` previously used single-line `<input type="text">` for the message composer — long messages scrolled horizontally inside a one-line box, hiding earlier text and making it impossible to scan or edit a multi-paragraph message before sending. Added shared `apps/web/src/components/ui/auto-grow-textarea.tsx` — a `<textarea>` that resets to `height: auto` then sizes to `scrollHeight`, capped at `maxRows × line-height + padding` (defaults to 6 rows). Once cap is reached, vertical scroll engages. Both consumer files swap input → AutoGrowTextarea with `maxRows={6}` and `resize-none` Tailwind. Chat footer adds an explicit `onKeyDown` handler (Enter without Shift → `form.requestSubmit()`) since textareas don't trigger form submit on Enter natively; Shift+Enter inserts a newline. Docky's existing `handleKeyDown` already had this exact pattern and works unchanged. `rounded-full` swapped to `rounded-2xl` since a fully-rounded multi-line textarea looks ridiculous when grown. 1192 tests pass.

- [Fix 223] My Jobs UI consistency + permanent mine 403 bug fix. Three concerns combined. (1) `/api/permanent/mine` previously had a `current_hat ∈ ('employer','agent')` guard that returned 403, while `/api/daywork/mine` had no such guard — only RLS + `poster_person_id = user.id` filtering. Result: a user who posted permanent jobs as employer, then switched to crew hat, would see a "Failed to load postings" toast on /daywork/mine?mode=permanent while daywork postings loaded fine. Removed the guard; route now matches the daywork pattern (filters by `employer_person_id = user.id`). All write routes (cancel, reject, select, shortlist, apply, post, etc.) retain their hat checks — read is permissive, write is gated. (2) Three tab primitives existed in the codebase: `SegmentedToggle` (custom, pill-shaped), `UnderlineTabs` (custom, bottom border), and radix shadcn `Tabs` (also pill-shaped). Daywork and Permanent mine sub-tabs used radix `Tabs`, while Messages, Reviews, and Discover sub-tabs used `UnderlineTabs` — inconsistent. Converted both mine pages to `UnderlineTabs` so all sub-tabs across the app use the same primitive. Hierarchy is now: pill = top-level mode switch (Daywork/Permanent), underline = secondary navigation within a mode. (3) `SegmentedToggle` and `UnderlineTabs` active-state colors changed to `--primary` (brand blue) — previously SegmentedToggle's active pill used `--card` (white-ish) and UnderlineTabs used `--foreground` (dark). Brand blue makes the "you are here" state pop. `SegmentedToggle` also gained `w-full` to span its container — previously it was content-width and looked awkwardly narrow. `UnderlineTabs` gained `overflow-x-auto` + `whitespace-nowrap` + `min-w-fit` so 5+ sub-tabs scroll horizontally on narrow screens instead of cramping (matters for permanent mine's Active/Negotiating/Filled/Cancelled/Templates row). Vertical spacing between daywork and permanent mine sections equalized (`pt-4` on permanent section to match daywork's `py-4`). Test for `/api/permanent/mine` updated: the "returns 403 when hat is crew" assertion replaced with "returns 200 with own postings regardless of hat" — same crew-hat setup, different expected behaviour. 1192 tests pass.

## In Progress

(none — see `tasks/todo.md` Queue for the next batch of work)

## Next Up

(Ordered queue — reorder as priorities shift)
